# @polyplug/abi

polyplug ABI type definitions for JavaScript/TypeScript — the frozen C ABI
mirrored as TS types (HostApi, StringView, AbiError, AbiErrorCode, built-in-type
helpers). Shared foundation for `@polyplug/host` and `@polyplug/guest`. Pure
TypeScript, no runtime dependencies.
