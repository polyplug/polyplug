# @polyplug/guest

polyplug guest library for authoring plugin bundles in JavaScript/TypeScript.
Provides the registration and dispatch helpers that generated guest bindings
build on, threading the host pointer and per-call arena explicitly (no globals).
