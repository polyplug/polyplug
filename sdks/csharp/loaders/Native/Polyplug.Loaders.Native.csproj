<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <AssemblyName>Polyplug.Loaders.Native</AssemblyName>
    <RootNamespace>Polyplug.Loaders.Native</RootNamespace>
    <IsPackable>true</IsPackable>
    <PackageId>Polyplug.Loaders.Native</PackageId>
    <Version>0.1.2</Version>
    <Description>Polyplug native library loader for loading native (C ABI) plugins. Native libraries for Linux, macOS, and Windows are embedded.</Description>
    <PackageTags>polyplug;plugin;runtime;ffi;native;loader</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\host\Polyplug.Host.csproj" />
  </ItemGroup>

  <!--
    Embed the per-platform loader native libraries into the NuGet package. The
    release workflow populates runtimes/<rid>/native/ before `dotnet pack`.
  -->
  <ItemGroup>
    <Content Include="runtimes\**\*" Pack="true" PackagePath="runtimes" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>
</Project>