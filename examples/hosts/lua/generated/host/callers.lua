-- THIS FILE IS AUTO-GENERATED BY polyplugc
-- DO NOT EDIT BY HAND
-- Re-generate with: polyplugc generate --api <api.toml> --lang lua --out <dir>

local ffi = require("ffi")

-- ABI constants
local ABI_OK = 0
local ABI_ERROR_GENERIC = 1

-- Contract ID constants
local PIPELINE_DECODER_CONTRACT_ID = 0x12F3C106B0C3DC1EULL
local DATA_TRANSFORMER_CONTRACT_ID = 0x3D53C682F3F5A9EFULL
local PIPELINE_ENCODER_CONTRACT_ID = 0x127D1703C6EFB432ULL
local DATA_REPORTER_CONTRACT_ID = 0x81D41D43E511D297ULL
local PIPELINE_VALIDATOR_CONTRACT_ID = 0xA553FAB5D11C7AF0ULL

local M = {}

function M.pipeline_Decoder_decode(vtable, input)
    local input_val = ffi.new("StringView", input)
    local args_ptr = ffi.cast("const void*", input_val)
    local out_val = ffi.new("StringView")
    local out_ptr = ffi.cast("void*", out_val)
    local fn_ptr = vtable.functions[0]
    if fn_ptr == nil then
        error("missing function pointer", 2)
    end
    local fn = ffi.cast("uint32_t (*)(const void*, void*)", fn_ptr)
    local err = fn(args_ptr, out_ptr)
    if err ~= 0 then
        error("polyplug call failed", 2)
    end
    return out_val
end

function M.data_Transformer_transform(vtable, data)
    local data_val = ffi.new("StringView", data)
    local args_ptr = ffi.cast("const void*", data_val)
    local out_val = ffi.new("StringView")
    local out_ptr = ffi.cast("void*", out_val)
    local fn_ptr = vtable.functions[0]
    if fn_ptr == nil then
        error("missing function pointer", 2)
    end
    local fn = ffi.cast("uint32_t (*)(const void*, void*)", fn_ptr)
    local err = fn(args_ptr, out_ptr)
    if err ~= 0 then
        error("polyplug call failed", 2)
    end
    return out_val
end

function M.pipeline_Encoder_encode(vtable, data)
    local data_val = ffi.new("StringView", data)
    local args_ptr = ffi.cast("const void*", data_val)
    local out_val = ffi.new("StringView")
    local out_ptr = ffi.cast("void*", out_val)
    local fn_ptr = vtable.functions[0]
    if fn_ptr == nil then
        error("missing function pointer", 2)
    end
    local fn = ffi.cast("uint32_t (*)(const void*, void*)", fn_ptr)
    local err = fn(args_ptr, out_ptr)
    if err ~= 0 then
        error("polyplug call failed", 2)
    end
    return out_val
end

function M.data_Reporter_report(vtable, data)
    local data_val = ffi.new("StringView", data)
    local args_ptr = ffi.cast("const void*", data_val)
    local out_val = ffi.new("StringView")
    local out_ptr = ffi.cast("void*", out_val)
    local fn_ptr = vtable.functions[0]
    if fn_ptr == nil then
        error("missing function pointer", 2)
    end
    local fn = ffi.cast("uint32_t (*)(const void*, void*)", fn_ptr)
    local err = fn(args_ptr, out_ptr)
    if err ~= 0 then
        error("polyplug call failed", 2)
    end
    return out_val
end

function M.pipeline_Validator_validate(vtable, data)
    local data_val = ffi.new("StringView", data)
    local args_ptr = ffi.cast("const void*", data_val)
    local out_val = ffi.new("StringView")
    local out_ptr = ffi.cast("void*", out_val)
    local fn_ptr = vtable.functions[0]
    if fn_ptr == nil then
        error("missing function pointer", 2)
    end
    local fn = ffi.cast("uint32_t (*)(const void*, void*)", fn_ptr)
    local err = fn(args_ptr, out_ptr)
    if err ~= 0 then
        error("polyplug call failed", 2)
    end
    return out_val
end

return M
