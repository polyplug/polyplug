// THIS FILE IS AUTO-GENERATED BY polyplugc
// DO NOT EDIT BY HAND
// Runtime: js-deno (host-side callers)

// ABI constants
export const ABI_OK = 0;
export const ABI_ERROR_GENERIC = 1;

// Contract ID constants
export const ContractIds = {
  PIPELINE_DECODER_CONTRACT_ID: 0x12F3C106B0C3DC1En,
  DATA_TRANSFORMER_CONTRACT_ID: 0x3D53C682F3F5A9EFn,
  PIPELINE_ENCODER_CONTRACT_ID: 0x127D1703C6EFB432n,
  DATA_REPORTER_CONTRACT_ID: 0x81D41D43E511D297n,
  PIPELINE_VALIDATOR_CONTRACT_ID: 0xA553FAB5D11C7AF0n,
} as const;

/** Host caller for contract `pipeline.Decoder` with hot-reload support. */
export class PipelineDecoderContract {
    #guard: any;

    private constructor(guard: any) {
        this.#guard = guard;
    }

    /** Factory method - creates instance or null if not found. */
    static create(rt: any, minVersion: number = 0): PipelineDecoderContract | null {
        const handle = rt.findByContract(ContractIds.PIPELINE_DECODER_CONTRACT_ID, minVersion);
        if (handle === null || handle === undefined) {
            return null;
        }
        const guard = rt.getGuard(handle);
        if (!guard) {
            return null;
        }
        return new PipelineDecoderContract(guard);
    }

    /** Check if this caller instance is still valid. */
    isValid(): boolean {
        return this.#guard !== null && this.#guard !== undefined;
    }

    /** Explicitly release the guard reference. */
    reset(): void {
        this.#guard = null;
    }

    /** Call `decode` */
    decode(input: { ptr: bigint; len: number }): { ptr: bigint; len: number } {
        const vtable = this.#guard?.vtable?.();
        if (!vtable) throw new Error('caller is not valid');
        const argsPtr = input;
        const fnPtr = vtable.functions[0];
        if (!fnPtr) throw new Error('function not available');
        const fn = fnPtr as unknown as (args: any, out: any) => { code: number; _pad: number; message_ptr: bigint; message_len: number };
        const outVal = { value: 0n };
        const result = fn(argsPtr, outVal);
        if (result.code !== 0) throw new Error('call failed');
        return outVal.value;
    }

}

/** Host caller for contract `data.Transformer` with hot-reload support. */
export class DataTransformerContract {
    #guard: any;

    private constructor(guard: any) {
        this.#guard = guard;
    }

    /** Factory method - creates instance or null if not found. */
    static create(rt: any, minVersion: number = 0): DataTransformerContract | null {
        const handle = rt.findByContract(ContractIds.DATA_TRANSFORMER_CONTRACT_ID, minVersion);
        if (handle === null || handle === undefined) {
            return null;
        }
        const guard = rt.getGuard(handle);
        if (!guard) {
            return null;
        }
        return new DataTransformerContract(guard);
    }

    /** Check if this caller instance is still valid. */
    isValid(): boolean {
        return this.#guard !== null && this.#guard !== undefined;
    }

    /** Explicitly release the guard reference. */
    reset(): void {
        this.#guard = null;
    }

    /** Call `transform` */
    transform(input: { ptr: bigint; len: number }): { ptr: bigint; len: number } {
        const vtable = this.#guard?.vtable?.();
        if (!vtable) throw new Error('caller is not valid');
        const argsPtr = input;
        const fnPtr = vtable.functions[0];
        if (!fnPtr) throw new Error('function not available');
        const fn = fnPtr as unknown as (args: any, out: any) => { code: number; _pad: number; message_ptr: bigint; message_len: number };
        const outVal = { value: 0n };
        const result = fn(argsPtr, outVal);
        if (result.code !== 0) throw new Error('call failed');
        return outVal.value;
    }

}

/** Host caller for contract `pipeline.Encoder` with hot-reload support. */
export class PipelineEncoderContract {
    #guard: any;

    private constructor(guard: any) {
        this.#guard = guard;
    }

    /** Factory method - creates instance or null if not found. */
    static create(rt: any, minVersion: number = 0): PipelineEncoderContract | null {
        const handle = rt.findByContract(ContractIds.PIPELINE_ENCODER_CONTRACT_ID, minVersion);
        if (handle === null || handle === undefined) {
            return null;
        }
        const guard = rt.getGuard(handle);
        if (!guard) {
            return null;
        }
        return new PipelineEncoderContract(guard);
    }

    /** Check if this caller instance is still valid. */
    isValid(): boolean {
        return this.#guard !== null && this.#guard !== undefined;
    }

    /** Explicitly release the guard reference. */
    reset(): void {
        this.#guard = null;
    }

    /** Call `encode` */
    encode(input: { ptr: bigint; len: number }): { ptr: bigint; len: number } {
        const vtable = this.#guard?.vtable?.();
        if (!vtable) throw new Error('caller is not valid');
        const argsPtr = input;
        const fnPtr = vtable.functions[0];
        if (!fnPtr) throw new Error('function not available');
        const fn = fnPtr as unknown as (args: any, out: any) => { code: number; _pad: number; message_ptr: bigint; message_len: number };
        const outVal = { value: 0n };
        const result = fn(argsPtr, outVal);
        if (result.code !== 0) throw new Error('call failed');
        return outVal.value;
    }

}

/** Host caller for contract `data.Reporter` with hot-reload support. */
export class DataReporterContract {
    #guard: any;

    private constructor(guard: any) {
        this.#guard = guard;
    }

    /** Factory method - creates instance or null if not found. */
    static create(rt: any, minVersion: number = 0): DataReporterContract | null {
        const handle = rt.findByContract(ContractIds.DATA_REPORTER_CONTRACT_ID, minVersion);
        if (handle === null || handle === undefined) {
            return null;
        }
        const guard = rt.getGuard(handle);
        if (!guard) {
            return null;
        }
        return new DataReporterContract(guard);
    }

    /** Check if this caller instance is still valid. */
    isValid(): boolean {
        return this.#guard !== null && this.#guard !== undefined;
    }

    /** Explicitly release the guard reference. */
    reset(): void {
        this.#guard = null;
    }

    /** Call `report` */
    report(input: { ptr: bigint; len: number }): { ptr: bigint; len: number } {
        const vtable = this.#guard?.vtable?.();
        if (!vtable) throw new Error('caller is not valid');
        const argsPtr = input;
        const fnPtr = vtable.functions[0];
        if (!fnPtr) throw new Error('function not available');
        const fn = fnPtr as unknown as (args: any, out: any) => { code: number; _pad: number; message_ptr: bigint; message_len: number };
        const outVal = { value: 0n };
        const result = fn(argsPtr, outVal);
        if (result.code !== 0) throw new Error('call failed');
        return outVal.value;
    }

}

/** Host caller for contract `pipeline.Validator` with hot-reload support. */
export class PipelineValidatorContract {
    #guard: any;

    private constructor(guard: any) {
        this.#guard = guard;
    }

    /** Factory method - creates instance or null if not found. */
    static create(rt: any, minVersion: number = 0): PipelineValidatorContract | null {
        const handle = rt.findByContract(ContractIds.PIPELINE_VALIDATOR_CONTRACT_ID, minVersion);
        if (handle === null || handle === undefined) {
            return null;
        }
        const guard = rt.getGuard(handle);
        if (!guard) {
            return null;
        }
        return new PipelineValidatorContract(guard);
    }

    /** Check if this caller instance is still valid. */
    isValid(): boolean {
        return this.#guard !== null && this.#guard !== undefined;
    }

    /** Explicitly release the guard reference. */
    reset(): void {
        this.#guard = null;
    }

    /** Call `validate` */
    validate(input: { ptr: bigint; len: number }): { ptr: bigint; len: number } {
        const vtable = this.#guard?.vtable?.();
        if (!vtable) throw new Error('caller is not valid');
        const argsPtr = input;
        const fnPtr = vtable.functions[0];
        if (!fnPtr) throw new Error('function not available');
        const fn = fnPtr as unknown as (args: any, out: any) => { code: number; _pad: number; message_ptr: bigint; message_len: number };
        const outVal = { value: 0n };
        const result = fn(argsPtr, outVal);
        if (result.code !== 0) throw new Error('call failed');
        return outVal.value;
    }

}

