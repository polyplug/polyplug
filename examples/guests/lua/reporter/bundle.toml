# bundle.toml — Lua reporter guest plugin
# Run: polyplugc generate --bundle bundle.toml --lang lua --out generated/

[bundle]
name    = "lua_reporter"
version = "1.0.0"
api     = "../../../api.toml"

[[plugin]]
name       = "lua_reporter"
version    = "1.0.0"
implements = ["data.Reporter@1.0"]
