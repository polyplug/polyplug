# bundle.toml — Lua transformer guest plugin
# Run: polyplugc generate --bundle bundle.toml --lang lua --out generated/

[bundle]
name    = "lua_transformer"
version = "1.0.0"
runtime = "lua"
file    = "reverse_transformer.lua"
api     = "../../../api.toml"

[[plugin]]
name       = "lua_transformer"
version    = "1.0.0"
implements = ["data.Transformer@1.0"]
