# bundle.toml — JS/Deno transformer guest plugin
# Run: polyplugc generate --bundle bundle.toml --lang js-deno --out generated/

[bundle]
name    = "js_deno_transformer"
version = "1.0.0"
api     = "../../../api.toml"

[[plugin]]
name       = "js_deno_transformer"
version    = "1.0.0"
implements = ["data.Transformer@1.0"]
