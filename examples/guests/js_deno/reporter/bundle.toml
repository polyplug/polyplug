# bundle.toml — JS/Deno reporter guest plugin
# Run: polyplugc generate --bundle bundle.toml --lang js-deno --out generated/

[bundle]
name    = "js_deno_reporter"
version = "1.0.0"
api     = "../../../api.toml"

[[plugin]]
name       = "js_deno_reporter"
version    = "1.0.0"
implements = ["data.Reporter@1.0"]
