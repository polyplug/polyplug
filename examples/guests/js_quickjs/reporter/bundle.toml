# bundle.toml — JS/QuickJS reporter guest plugin
# Run: polyplugc generate --bundle bundle.toml --lang js-quickjs --out generated/

[bundle]
name    = "js_quickjs_reporter"
version = "1.0.0"
runtime = "js-quickjs"
file    = "bundle.js"
api     = "../../../api.toml"

[[plugin]]
name       = "js_quickjs_reporter"
version    = "1.0.0"
implements = ["data.Reporter@1.0"]
