# bundle.toml — JS/QuickJS encoder guest plugin
# Run: polyplugc generate --bundle bundle.toml --lang js-quickjs --out generated/

[bundle]
name    = "js_quickjs_encoder"
version = "1.0.0"
runtime = "js-quickjs"
file    = "bundle.js"
api     = "../../../api.toml"

[[plugin]]
name       = "js_quickjs_encoder"
version    = "1.0.0"
implements = ["pipeline.Encoder@1.0"]
