# bundle.toml — Rust transformer guest plugin
# Run: polyplugc generate --bundle bundle.toml --lang rust --out generated/

[bundle]
name    = "rust_transformer"
version = "1.0.0"
api     = "../../../api.toml"

[[plugin]]
name       = "rust_transformer"
version    = "1.0.0"
implements = ["data.Transformer@1.0"]
