# bundle.toml — Rust reporter guest plugin
# Run: polyplugc generate --bundle bundle.toml --lang rust --out generated/

[bundle]
name    = "rust_reporter"
version = "1.0.0"
api     = "../../../api.toml"

[[plugin]]
name       = "rust_reporter"
version    = "1.0.0"
implements = ["data.Reporter@1.0"]
