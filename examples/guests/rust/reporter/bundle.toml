[bundle]
name = "rust_reporter"
version = "1.0.0"
api = "../../../api.toml"
loader = "native"

[bundle.file]
linux.x86_64 = "libreporter.so"

[[plugin]]
name = "reporter"
implements = ["data.Reporter@1.0"]
