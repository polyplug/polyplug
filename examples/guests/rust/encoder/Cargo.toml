[package]
name    = "csv_encoder"
version = "1.0.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

# IMPORTANT: NO polyplug dependency — cdylib cannot depend on it (circular dep)
# All ABI types are mirrored inline (same pattern as csv_decoder)

[workspace]
