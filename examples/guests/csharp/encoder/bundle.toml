# bundle.toml — C# encoder guest plugin

[bundle]
name    = "csharp_encoder"
version = "1.0.0"
runtime = "dotnet"
file    = "CsvEncoder.dll"
api     = "../../../api.toml"

[[plugin]]
name       = "csharp_encoder"
version    = "1.0.0"
implements = ["pipeline.Encoder@1.0"]
