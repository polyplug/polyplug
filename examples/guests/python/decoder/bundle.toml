# bundle.toml — Python transformer guest plugin
# Run: polyplugc generate --bundle bundle.toml --lang python --out generated/

[bundle]
name    = "python_transformer"
version = "1.0.0"
api     = "../../../api.toml"

[[plugin]]
name       = "python_transformer"
version    = "1.0.0"
implements = ["data.Transformer@1.0"]
