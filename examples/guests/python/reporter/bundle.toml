# bundle.toml — Python reporter guest plugin

[bundle]
name    = "python_reporter"
version = "1.0.0"
runtime = "python"
file    = "reporter.py"
api     = "../../../api.toml"

[[plugin]]
name       = "python_reporter"
version    = "1.0.0"
implements = ["data.Reporter@1.0"]
