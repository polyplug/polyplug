# bundle.toml — Python encoder guest plugin

[bundle]
name    = "python_encoder"
version = "1.0.0"
runtime = "python"
file    = "encoder.py"
api     = "../../../api.toml"

[[plugin]]
name       = "python_encoder"
version    = "1.0.0"
implements = ["pipeline.Encoder@1.0"]
