# bundle.toml — Python validator guest plugin

[bundle]
name    = "python_validator"
version = "1.0.0"
runtime = "python"
file    = "validator.py"
api     = "../../../api.toml"

[[plugin]]
name       = "python_validator"
version    = "1.0.0"
implements = ["pipeline.Validator@1.0"]
