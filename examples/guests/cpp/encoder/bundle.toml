[bundle]
name = "cpp_encoder"
version = "1.0.0"
api = "../../../api.toml"
loader = "native"

[bundle.file]
linux.x86_64 = "libencoder.so"

[[plugin]]
name = "encoder"
implements = ["pipeline.Encoder@1.0"]
