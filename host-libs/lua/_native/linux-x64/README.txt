Linux x64 native library placeholder.

CI will download libpolyplug.so from GitHub Releases:
https://github.com/polyplug/polyplug/releases/download/v{version}/libpolyplug-linux-x64.so

Place the downloaded file in this directory as: libpolyplug.so
