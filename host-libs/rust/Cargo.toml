[package]
name         = "polyplug-host"
version      = "0.1.0"
edition.workspace      = true
license.workspace      = true
rust-version.workspace = true
description  = "Host-side Rust bindings for the polyplug plugin runtime"

[lib]
name = "polyplug_host"
path = "src/lib/mod.rs"

[dependencies]
polyplug-runtime = { path = "../../crates/polyplug-runtime" }

[lints]
workspace = true
