---
phase: 10-sdk-cleanup-completion
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - crates/polyplug_abi/build/extractor.rs
  - sdks/cpp/guest/polyplug/guest.hpp
  - sdks/csharp/guest/AbiSizeTests.cs
autonomous: true
requirements: []  # This work addresses success criteria 3-4, not specific requirement IDs
must_haves:
  truths:
    - "C++ guest.hpp uses RuntimeAbi not HostInterface in function signatures"
    - "C# AbiSizeTests.cs uses RuntimeAbi not HostInterface"
    - "SDK build extractor uses RuntimeAbi in ABI_TYPES list"
  artifacts:
    - path: "sdks/cpp/guest/polyplug/guest.hpp"
      provides: "C++ guest SDK host vtable storage"
      contains: "RuntimeAbi"
    - path: "sdks/csharp/guest/AbiSizeTests.cs"
      provides: "C# ABI size verification tests"
      contains: "RuntimeAbi"
  key_links:
    - from: "crates/polyplug_abi/src/host/runtime_abi.rs"
      to: "SDK ABI files"
      via: "build script extraction"
      pattern: "RuntimeAbi"
---

<objective>
Fix HostInterface → RuntimeAbi naming in C++ guest SDK and C# test file to match polyplug_abi naming convention.

Purpose: Complete the naming consistency cleanup so all SDK types match polyplug_abi.
Output: Updated C++ guest.hpp and C# AbiSizeTests.cs using RuntimeAbi naming.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Reference: Rust SDK type alias pattern (backward compatibility)
@sdks/rust/guest/src/lib.rs

# Source of truth for RuntimeAbi definition
@crates/polyplug_abi/src/host/runtime_abi.rs
</context>

<interfaces>
<!-- Key types from polyplug_abi that SDK files should use -->
From crates/polyplug_abi/src/host/runtime_abi.rs:
```rust
#[repr(C)]
pub struct RuntimeAbi {
    pub register_contract: unsafe extern "C" fn(...),
    pub alloc: unsafe extern "C" fn(...),
    pub free: unsafe extern "C" fn(...),
    pub find_by_contract: unsafe extern "C" fn(...),
    pub find_all_by_contract: unsafe extern "C" fn(...),
    pub resolve_contract: unsafe extern "C" fn(...),
    pub call_method: unsafe extern "C" fn(...),
    pub get_host_contract: unsafe extern "C" fn(...),
}
```

From sdks/rust/guest/src/lib.rs (backward compatibility pattern):
```rust
pub use polyplug_abi::RuntimeAbi;
/// Legacy alias for RuntimeAbi (backward compatibility).
pub type HostInterface = RuntimeAbi;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update C++ guest.hpp to use RuntimeAbi naming</name>
  <files>sdks/cpp/guest/polyplug/guest.hpp</files>
  <read_first>
    - sdks/cpp/guest/polyplug/guest.hpp (current state)
    - sdks/rust/guest/src/lib.rs (reference: type alias pattern)
    - crates/polyplug_abi/src/host/runtime_abi.rs (source of truth for RuntimeAbi)
  </read_first>
  <action>Update `sdks/cpp/guest/polyplug/guest.hpp` to use `RuntimeAbi` instead of `HostInterface`:

1. Lines 134-145: Rename all `HostInterface` occurrences to `RuntimeAbi`:
   - `inline const HostInterface*& host_vtable_storage()` → `inline const RuntimeAbi*& host_vtable_storage()`
   - `static const HostInterface* stored` → `static const RuntimeAbi* stored`
   - `inline void store_host_vtable(const HostInterface* vtable)` → `inline void store_host_vtable(const RuntimeAbi* vtable)`
   - `inline const HostInterface* get_host_vtable()` → `inline const RuntimeAbi* get_host_vtable()`

2. Add a backward compatibility typedef after the RuntimeAbi functions (following Rust SDK pattern):
   ```cpp
   // Legacy alias for backward compatibility
   using HostInterface = RuntimeAbi;
   ```

3. Update the section comment from "Host VTable Storage" to "RuntimeAbi Storage"</action>
  <verify>
    <automated>grep -c "RuntimeAbi" sdks/cpp/guest/polyplug/guest.hpp && grep -c "using HostInterface = RuntimeAbi" sdks/cpp/guest/polyplug/guest.hpp</automated>
  </verify>
  <done>C++ guest.hpp uses RuntimeAbi in function signatures with backward compatibility typedef</done>
</task>

<task type="auto">
  <name>Task 2: Update C# AbiSizeTests.cs to use RuntimeAbi naming</name>
  <files>sdks/csharp/guest/AbiSizeTests.cs</files>
  <read_first>
    - sdks/csharp/guest/AbiSizeTests.cs (current state)
    - sdks/csharp/guest/RuntimeAbiStorage.cs (reference: correct naming already exists here)
  </read_first>
  <action>Update `sdks/csharp/guest/AbiSizeTests.cs` to use `RuntimeAbi` instead of `HostInterface`:

1. Line 46: Change comment from `// HostInterface: 8 x IntPtr(8) = 64 bytes` to `// RuntimeAbi: 8 x IntPtr(8) = 64 bytes`
2. Line 47: Change `Marshal.SizeOf<HostInterface>()` to `Marshal.SizeOf<RuntimeAbi>()`
3. Line 48: Change error message from `$"HostInterface size mismatch:` to `$"RuntimeAbi size mismatch:`

Note: This assumes RuntimeAbi struct exists in the Polyplug.Abi namespace. If it doesn't, the test will fail to compile, indicating the SDK ABI generation needs to be updated first (see Task 3).</action>
  <verify>
    <automated>grep -c "RuntimeAbi" sdks/csharp/guest/AbiSizeTests.cs</automated>
  </verify>
  <done>C# AbiSizeTests.cs uses RuntimeAbi naming</done>
</task>

<task type="auto">
  <name>Task 3: Update SDK build extractor to use RuntimeAbi</name>
  <files>crates/polyplug_abi/build/extractor.rs</files>
  <read_first>
    - crates/polyplug_abi/build/extractor.rs (current state)
    - crates/polyplug_abi/src/host/runtime_abi.rs (source of truth)
  </read_first>
  <action>Update `crates/polyplug_abi/build/extractor.rs` to extract `RuntimeAbi` instead of `HostInterface`:

1. Line 33: Change `"HostInterface"` to `"RuntimeAbi"` in the ABI_TYPES array

This change ensures that when the SDK build script runs, it extracts the `RuntimeAbi` struct from the Rust source and generates SDK files with the correct naming.

The build script in `crates/polyplug_abi/build/main.rs` reads the Rust source and extracts types matching names in ABI_TYPES. With this change, it will extract `RuntimeAbi` instead of looking for `HostInterface`.</action>
  <verify>
    <automated>grep -c '"RuntimeAbi"' crates/polyplug_abi/build/extractor.rs && test $(grep -c '"HostInterface"' crates/polyplug_abi/build/extractor.rs) -eq 0</automated>
  </verify>
  <done>SDK build extractor uses RuntimeAbi in ABI_TYPES list</done>
</task>

</tasks>

<verification>
- C++ guest.hpp contains `RuntimeAbi` in function signatures
- C++ guest.hpp contains backward compatibility typedef `using HostInterface = RuntimeAbi`
- C# AbiSizeTests.cs uses `RuntimeAbi` in Marshal.SizeOf calls
- SDK build extractor ABI_TYPES contains "RuntimeAbi" not "HostInterface"
</verification>

<success_criteria>
- All three files updated with RuntimeAbi naming
- Backward compatibility maintained via type alias/typedef
- Build extractor ready for SDK regeneration
</success_criteria>

<output>
After completion, create `.planning/phases/10-sdk-cleanup-completion/10-02-SUMMARY.md`
</output>