---
phase: 05-sdk-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [SDK-01, SDK-06]
user_setup: []

must_haves:
  truths:
    - "Rust host SDK imports RuntimeConfig from polyplug_abi"
    - "Rust host SDK imports ReloadPhaseType from polyplug_abi"
    - "Rust host SDK has no duplicate RuntimeConfig definition"
    - "Rust host SDK does not define PluginGuard"
  artifacts:
    - path: "sdks/rust/host/src/lib.rs"
      provides: "Rust host SDK entry point"
      exports: ["manifest", "scanner"]
  key_links:
    - from: "sdks/rust/host/src/lib.rs"
      to: "polyplug_abi"
      via: "workspace dependency"
      pattern: "polyplug_abi::"
---

<objective>
Verify Rust host SDK uses types from polyplug_abi without duplicates.

Purpose: Rust SDK is part of workspace and should import ABI types from polyplug_abi crate, not define duplicates.
Output: Verified Rust SDK with no duplicate types, imports from polyplug_abi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## polyplug_abi Runtime Types

From crates/polyplug_abi/src/runtime/mod.rs:
```rust
pub use compatibility::Compatibility;
pub use runtime_config::RuntimeConfig;
pub use reload_phase_data::{ReloadPhaseData, ReloadPhaseType};
```

RuntimeConfig fields (#[repr(C)], 24 bytes):
- hot_reload_enabled: bool
- hot_reload_max_retries: u32
- hot_reload_retry_interval_ms: u64
- hot_reload_abort_on_max_retries: bool
- compatibility: Compatibility (#[repr(u32)])
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Rust SDK imports from polyplug_abi</name>
  <files>sdks/rust/host/src/lib.rs</files>
  <read_first>
    - sdks/rust/host/src/lib.rs (current state)
    - crates/polyplug_abi/src/lib.rs (exports)
    - sdks/rust/host/Cargo.toml (dependencies)
  </read_first>
  <action>Read the Rust host SDK files to verify current imports. The Rust SDK is part of the Cargo workspace and should import types from polyplug_abi. Check:
1. lib.rs exports - should NOT define RuntimeConfig, ReloadPhase, or PluginGuard locally
2. Cargo.toml dependencies - should have polyplug_abi as dependency if needed
3. If SDK needs RuntimeConfig or ReloadPhase types, they MUST come from polyplug_abi via `use polyplug_abi::{RuntimeConfig, ReloadPhaseType};`

If any duplicates exist, add imports from polyplug_abi and remove local definitions. The SDK currently only exports manifest and scanner modules, which is correct.
</action>
  <behavior>
    - Test 1: lib.rs does NOT contain `struct RuntimeConfig` or `pub struct RuntimeConfig`
    - Test 2: lib.rs does NOT contain `pub struct PluginGuard` or `struct PluginGuard`
    - Test 3: If RuntimeConfig is used, grep finds `polyplug_abi::RuntimeConfig` in SDK files
  </behavior>
  <verify>
    <automated>grep -E "struct RuntimeConfig|struct PluginGuard" sdks/rust/host/src/*.rs 2>/dev/null | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>
    - Rust SDK lib.rs contains no duplicate RuntimeConfig or PluginGuard definitions
    - Any needed ABI types are imported from polyplug_abi
    - cargo check --workspace passes for Rust SDK
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Rust SDK documentation</name>
  <files>sdks/rust/host/README.md</files>
  <read_first>
    - sdks/rust/host/src/lib.rs
    - crates/polyplug_abi/src/lib.rs
  </read_first>
  <action>If README.md exists, verify it documents that ABI types come from polyplug_abi crate. If README.md does NOT exist, skip this task (no action needed). The Rust SDK uses workspace types directly.
</action>
  <verify>
    <automated>test -f sdks/rust/host/README.md && grep -q "polyplug_abi" sdks/rust/host/README.md || echo "README not required"</automated>
  </verify>
  <done>
    - If README exists, it mentions polyplug_abi as source of ABI types
    - If README does not exist, task skipped (no action)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK -> polyplug_abi | Workspace dependency, trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Rust SDK imports | accept | Workspace dependency - same trust level |
</threat_model>

<verification>
Verify Rust SDK has no duplicate type definitions:
```bash
grep -E "struct RuntimeConfig|struct PluginGuard|struct ReloadPhase" sdks/rust/host/src/*.rs 2>/dev/null
```
Expected: No matches (empty output)
</verification>

<success_criteria>
- Rust host SDK imports RuntimeConfig and ReloadPhaseType from polyplug_abi when needed
- No duplicate RuntimeConfig, PluginGuard, or ReloadPhase definitions in Rust SDK
- cargo check --workspace passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-sdk-updates/05-01-SUMMARY.md`
</output>