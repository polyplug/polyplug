---
phase: 17-refactor-contractregistry-to-unified-runtimestore
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polyplug/src/registry/contract_registry.rs
  - crates/polyplug/src/registry/mod.rs
  - crates/polyplug/src/runtime.rs
  - crates/polyplug/src/reload.rs
  - crates/polyplug/src/ffi.rs
  - crates/polyplug/tests/stress_error.rs
  - crates/polyplug/tests/stress_hot_reload.rs
  - crates/polyplug/tests/integration_dispatch.rs
  - crates/polyplug/tests/integration_cross_plugin.rs
  - crates/polyplug/tests/integration_codegen_cpp.rs
  - crates/polyplug/tests/registry_edge_cases.rs
  - crates/polyplug/tests/integration_ffi_robustness.rs
  - crates/polyplug/tests/stress_concurrent_registry.rs
  - crates/polyplug/tests/hot_reload_safety.rs
  - crates/polyplug/tests/stress_memory.rs
  - crates/polyplug/tests/integration_graph.rs
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "ContractRegistry type renamed to RuntimeStore everywhere"
    - "All Registry* types renamed to Plugin* or RuntimeStore*"
    - "All method names use guest_contract or bundle prefix"
    - "All internal fields renamed per D-17 to D-19"
    - "All tests pass after renaming"
  artifacts:
    - path: "crates/polyplug/src/registry/runtime_store.rs"
      provides: "RuntimeStore, PluginEntry, PluginSlot, RuntimeStoreData"
      contains: "pub struct RuntimeStore"
    - path: "crates/polyplug/src/registry/mod.rs"
      provides: "Public export of RuntimeStore"
      contains: "pub use runtime_store::RuntimeStore"
    - path: "crates/polyplug/src/runtime.rs"
      provides: "Runtime using RuntimeStore"
      contains: "registry: Arc<RuntimeStore>"
    - path: "crates/polyplug/src/reload.rs"
      provides: "Reload uses renamed methods"
      contains: "get_bundle_plugin_slots"
  key_links:
    - from: "crates/polyplug/src/runtime.rs"
      to: "crate::registry::RuntimeStore"
      via: "use statement"
      pattern: "use crate::registry::RuntimeStore"
    - from: "crates/polyplug/src/reload.rs"
      to: "RuntimeStore.get_bundle_plugin_slots"
      via: "method call"
      pattern: "registry\\.get_bundle_plugin_slots"
---

<objective>
Pass 1: Rename all ContractRegistry types, methods, and internal fields to RuntimeStore naming convention. This is a pure rename pass — no new features added. All existing tests must pass after renaming.

Purpose: Prepare codebase for Pass 2 structural changes by establishing canonical RuntimeStore naming.
Output: All files use RuntimeStore, PluginEntry, PluginSlot, RuntimeStoreData, and renamed methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-refactor-contractregistry-to-unified-runtimestore/17-CONTEXT.md
@.planning/phases/17-refactor-contractregistry-to-unified-runtimestore/17-RESEARCH.md
@AGENTS.md
</context>

<interfaces>
<!-- Key types and contracts from existing codebase that executor needs -->

From crates/polyplug/src/registry/contract_registry.rs:
```rust
pub struct ContractRegistry {
    data: RwLock<RegistryData>,
}

pub(crate) struct RegistryEntry {
    pub descriptor: PluginDescriptor,
    pub contract_name: String,
    pub bundle_id: BundleId,
}

pub(crate) struct RegistrySlot {
    pub entry: Option<RegistryEntry>,
    pub interface: Option<Arc<GuestContractInterface>>,
}

struct RegistryData {
    slots: Vec<RegistrySlot>,
    contract_index: HashMap<GuestContractId, Vec<u32>>,
    bundle_index: HashMap<BundleId, u32>,
    declared_deps: HashMap<BundleId, HashSet<GuestContractId>>,
}
```

Key methods to rename (per CONTEXT.md D-05 to D-16):
- `find_by_contract` -> `find_guest_contract`
- `find_all_by_contract` -> `find_all_guest_contracts`
- `count_by_contract` -> `count_guest_contracts`
- `find_by_bundle` -> `find_guest_contract_by_bundle`
- `find_slots_by_bundle` -> `get_bundle_plugin_slots`
- `resolve` -> `resolve_guest_contract`
- `swap_interface` -> `swap_guest_contract_interface`
- `register` -> `register_guest_contract`
- `declare_deps` -> `declare_bundle_dependencies`
- `is_dependency_declared` -> `is_bundle_dependency_declared`
- `get_slot_contract_id` -> `get_slot_guest_contract_id`
- `get_interface_arc` -> `get_guest_contract_interface_arc`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rename registry file and update mod.rs exports</name>
  <files>
    crates/polyplug/src/registry/contract_registry.rs (rename to runtime_store.rs)
    crates/polyplug/src/registry/mod.rs
  </files>
  <read_first>
    crates/polyplug/src/registry/contract_registry.rs
    crates/polyplug/src/registry/mod.rs
    .planning/phases/17-refactor-contractregistry-to-unified-runtimestore/17-CONTEXT.md
  </read_first>
  <action>
    Rename the file `contract_registry.rs` to `runtime_store.rs` using `git mv` or file rename.

    Update `mod.rs` to:
    ```rust
    pub mod runtime_store;
    
    pub use runtime_store::RuntimeStore;
    ```

    Per D-01: ContractRegistry -> RuntimeStore
    Per D-02: RegistryEntry -> PluginEntry
    Per D-03: RegistrySlot -> PluginSlot  
    Per D-04: RegistryData -> RuntimeStoreData
  </action>
  <verify>
    <automated>cargo check -p polyplug --lib 2>&1 | grep -E "contract_registry|ContractRegistry" || echo "File renamed successfully"</automated>
  </verify>
  <acceptance_criteria>
    - File crates/polyplug/src/registry/runtime_store.rs exists
    - File crates/polyplug/src/registry/contract_registry.rs does NOT exist
    - crates/polyplug/src/registry/mod.rs contains `pub mod runtime_store`
    - crates/polyplug/src/registry/mod.rs contains `pub use runtime_store::RuntimeStore`
    - crates/polyplug/src/registry/mod.rs does NOT contain `ContractRegistry`
  </acceptance_criteria>
  <done>
    Registry module exports RuntimeStore from runtime_store.rs file. No ContractRegistry references remain in mod.rs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename all types, methods, and fields in runtime_store.rs</name>
  <files>crates/polyplug/src/registry/runtime_store.rs</files>
  <read_first>
    crates/polyplug/src/registry/runtime_store.rs
    .planning/phases/17-refactor-contractregistry-to-unified-runtimestore/17-CONTEXT.md
    AGENTS.md
  </read_first>
  <action>
    Rename all types per D-01 to D-04:
    - `ContractRegistry` -> `RuntimeStore` (struct name and all references)
    - `RegistryEntry` -> `PluginEntry` (struct name and field references)
    - `RegistrySlot` -> `PluginSlot` (struct name and field references)
    - `RegistryData` -> `RuntimeStoreData` (struct name and RwLock type annotation)

    Rename all methods per D-05 to D-16:
    - `find_by_contract` -> `find_guest_contract`
    - `find_all_by_contract` -> `find_all_guest_contracts`
    - `find_all_by_contract_packed` -> `find_all_guest_contracts_packed`
    - `find_all_by_contract_into` -> `find_all_guest_contracts_into`
    - `count_by_contract` -> `count_guest_contracts`
    - `find_by_bundle` -> `find_guest_contract_by_bundle`
    - `find_slots_by_bundle` -> `get_bundle_plugin_slots`
    - `resolve` -> `resolve_guest_contract`
    - `swap_interface` -> `swap_guest_contract_interface`
    - `register` -> `register_guest_contract`
    - `declare_deps` -> `declare_bundle_dependencies`
    - `is_dependency_declared` -> `is_bundle_dependency_declared`
    - `get_slot_contract_id` -> `get_slot_guest_contract_id`
    - `get_interface_arc` -> `get_guest_contract_interface_arc`
    - `clear_for_test` -> unchanged (test-only method)
    - `find` -> unchanged (keep as alias to find_guest_contract for compatibility, but delegate internally)
    - `new` -> unchanged
    - `default` -> unchanged (Default impl)

    Rename internal fields per D-17 to D-19:
    - `contract_index` -> `guest_contract_index` (field name and all HashMap accesses)
    - `bundle_index` -> `bundle_slots_index` (field name and all HashMap accesses)
    - `declared_deps` -> `bundle_declared_deps` (field name and all HashMap accesses)

    Update all doc comments to use RuntimeStore naming:
    - Module doc comment: `//! Registry —` -> `//! RuntimeStore —`
    - All `ContractRegistry` references in doc comments -> `RuntimeStore`
    - Update method doc comments to reference renamed methods

    Update test module:
    - All test functions using `ContractRegistry::new()` -> `RuntimeStore::new()`
    - All type annotations `ContractRegistry` -> `RuntimeStore`
    - All method calls use renamed names

    Per AGENTS.md §3: Add explicit type annotations where inference was previously relied upon (e.g., `let data: std::sync::RwLockReadGuard<'_, RuntimeStoreData> = ...`).

    Per AGENTS.md §16: Do NOT create type aliases. Use canonical names directly.
  </action>
  <verify>
    <automated>cargo check -p polyplug --lib 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains `pub struct RuntimeStore`
    - File does NOT contain `ContractRegistry` (except in old doc comment references being updated)
    - File contains `pub(crate) struct PluginEntry`
    - File contains `pub(crate) struct PluginSlot`
    - File contains `struct RuntimeStoreData`
    - File contains method `pub fn find_guest_contract`
    - File contains method `pub fn get_bundle_plugin_slots`
    - File contains field `guest_contract_index: HashMap<GuestContractId, Vec<u32>>`
    - File contains field `bundle_slots_index: HashMap<BundleId, u32>`
    - File contains field `bundle_declared_deps: HashMap<BundleId, HashSet<GuestContractId>>`
    - `cargo check -p polyplug --lib` passes without errors
  </acceptance_criteria>
  <done>
    runtime_store.rs contains only RuntimeStore, PluginEntry, PluginSlot, RuntimeStoreData types with all methods and fields renamed. Compilation passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update runtime.rs imports and method calls</name>
  <files>crates/polyplug/src/runtime.rs</files>
  <read_first>
    crates/polyplug/src/runtime.rs
    crates/polyplug/src/registry/runtime_store.rs
    .planning/phases/17-refactor-contractregistry-to-unified-runtimestore/17-CONTEXT.md
  </read_first>
  <action>
    Update imports:
    - `use crate::registry::ContractRegistry;` -> `use crate::registry::RuntimeStore;`

    Update struct field:
    - `pub(crate) registry: Arc<ContractRegistry>` -> `pub(crate) registry: Arc<RuntimeStore>`

    Update method return type:
    - `pub fn registry(&self) -> &Arc<ContractRegistry>` -> `pub fn registry(&self) -> &Arc<RuntimeStore>`

    Update all method calls on registry:
    - `registry.find_by_contract(...)` -> `registry.find_guest_contract(...)`
    - `registry.find_by_bundle(...)` -> `registry.find_guest_contract_by_bundle(...)`
    - `registry.find_slots_by_bundle(...)` -> `registry.get_bundle_plugin_slots(...)`
    - `registry.resolve(...)` -> `registry.resolve_guest_contract(...)`
    - `registry.swap_interface(...)` -> `registry.swap_guest_contract_interface(...)`
    - `registry.register(...)` -> `registry.register_guest_contract(...)`
    - `registry.declare_deps(...)` -> `registry.declare_bundle_dependencies(...)`
    - `registry.is_dependency_declared(...)` -> `registry.is_bundle_dependency_declared(...)`
    - `registry.get_slot_contract_id(...)` -> `registry.get_slot_guest_contract_id(...)`
    - `registry.get_interface_arc(...)` -> `registry.get_guest_contract_interface_arc(...)`

    Update all local variable type annotations:
    - `let registry: &ContractRegistry` -> `let registry: &RuntimeStore`
    - `let registry: &Arc<ContractRegistry>` -> `let registry: &Arc<RuntimeStore>`

    Update function parameter types:
    - `registry: &crate::registry::ContractRegistry` -> `registry: &crate::registry::RuntimeStore`

    Update test functions that construct registry:
    - `ContractRegistry::new()` -> `RuntimeStore::new()`
  </action>
  <verify>
    <automated>cargo check -p polyplug --lib 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains `use crate::registry::RuntimeStore`
    - File does NOT contain `ContractRegistry`
    - File contains `registry: Arc<RuntimeStore>`
    - File contains `pub fn registry(&self) -> &Arc<RuntimeStore>`
    - All registry method calls use renamed method names (find_guest_contract, get_bundle_plugin_slots, etc.)
    - `cargo check -p polyplug --lib` passes without errors
  </acceptance_criteria>
  <done>
    runtime.rs uses RuntimeStore with all renamed methods. No ContractRegistry references remain.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update reload.rs method calls</name>
  <files>crates/polyplug/src/reload.rs</files>
  <read_first>
    crates/polyplug/src/reload.rs
    crates/polyplug/src/registry/runtime_store.rs
    .planning/phases/17-refactor-contractregistry-to-unified-runtimestore/17-CONTEXT.md
  </read_first>
  <action>
    Update all registry method calls in reload_bundle():
    - Line 114: `self.registry.find_slots_by_bundle(bundle_id)` -> `self.registry.get_bundle_plugin_slots(bundle_id)`
    - Lines 129, 166: `self.registry.get_interface_arc(*slot_idx)` -> `self.registry.get_guest_contract_interface_arc(*slot_idx)`
    - Line 152-154: `self.registry.get_slot_contract_id(*slot_idx)` -> `self.registry.get_slot_guest_contract_id(*slot_idx)`
    - Line 160-161: `self.registry.find_by_contract(contract_id, 0)` -> `self.registry.find_guest_contract(contract_id, 0)`
    - Line 171: `self.registry.swap_interface(*slot_idx, new_interface)` -> `self.registry.swap_guest_contract_interface(*slot_idx, new_interface)`

    Update refresh_handle() method (line 205):
    - `self.registry().find_by_contract(...)` -> `self.registry().find_guest_contract(...)`

    Per AGENTS.md §3: Ensure explicit type annotations on local bindings (already present in most cases).
  </action>
  <verify>
    <automated>cargo check -p polyplug --lib 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains `self.registry.get_bundle_plugin_slots(bundle_id)`
    - File contains `self.registry.get_guest_contract_interface_arc`
    - File contains `self.registry.get_slot_guest_contract_id`
    - File contains `self.registry.find_guest_contract`
    - File contains `self.registry.swap_guest_contract_interface`
    - File does NOT contain `find_slots_by_bundle`
    - File does NOT contain `get_slot_contract_id` (except in renamed form)
    - `cargo check -p polyplug --lib` passes without errors
  </acceptance_criteria>
  <done>
    reload.rs uses all renamed RuntimeStore methods. Compilation passes.
  </done>
</task>

<task type="auto">
  <name>Task 5: Update ffi.rs function names and registry calls</name>
  <files>crates/polyplug/src/ffi.rs</files>
  <read_first>
    crates/polyplug/src/ffi.rs
    crates/polyplug/src/registry/runtime_store.rs
    .planning/phases/17-refactor-contractregistry-to-unified-runtimestore/17-CONTEXT.md
  </read_first>
  <action>
    Rename FFI function names to match new registry method names:
    - `polyplug_runtime_find_by_contract` -> `polyplug_runtime_find_guest_contract` (function name at line 320)
    - `polyplug_runtime_find_by_bundle` -> `polyplug_runtime_find_guest_contract_by_bundle` (function name at line 342)
    - `polyplug_runtime_resolve_plugin` -> `polyplug_runtime_resolve_guest_contract` (function name at line 411)

    Update internal method calls in FFI functions:
    - `runtime.0.find_by_contract(...)` -> `runtime.0.find_guest_contract(...)` (line 331)
    - `.find_by_bundle(...)` -> `.find_guest_contract_by_bundle(...)` (line 356)
    - `runtime.0.registry().resolve(handle)` -> `runtime.0.registry().resolve_guest_contract(handle)` (line 426)

    Update test functions in ffi.rs that call FFI functions:
    - `polyplug_runtime_find_by_contract(...)` -> `polyplug_runtime_find_guest_contract(...)` (all test calls)

    NOTE: Do NOT rename loader-related functions like `polyplug_runtime_register_loader` — these are not registry methods.
  </action>
  <verify>
    <automated>cargo check -p polyplug --lib 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains function `pub unsafe extern "C" fn polyplug_runtime_find_guest_contract`
    - File contains function `pub unsafe extern "C" fn polyplug_runtime_find_guest_contract_by_bundle`
    - File contains function `pub unsafe extern "C" fn polyplug_runtime_resolve_guest_contract`
    - File does NOT contain `polyplug_runtime_find_by_contract` as function definition
    - File does NOT contain `polyplug_runtime_find_by_bundle` as function definition
    - File contains `.find_guest_contract(...)` method call
    - File contains `.find_guest_contract_by_bundle(...)` method call
    - File contains `.resolve_guest_contract(...)` method call
    - `cargo check -p polyplug --lib` passes without errors
  </acceptance_criteria>
  <done>
    ffi.rs exports renamed FFI functions and uses renamed registry methods internally. Compilation passes.
  </done>
</task>

<task type="auto">
  <name>Task 6: Update all test files to use RuntimeStore</name>
  <files>
    crates/polyplug/tests/stress_error.rs
    crates/polyplug/tests/stress_hot_reload.rs
    crates/polyplug/tests/integration_dispatch.rs
    crates/polyplug/tests/integration_cross_plugin.rs
    crates/polyplug/tests/integration_codegen_cpp.rs
    crates/polyplug/tests/registry_edge_cases.rs
    crates/polyplug/tests/integration_ffi_robustness.rs
    crates/polyplug/tests/stress_concurrent_registry.rs
    crates/polyplug/tests/hot_reload_safety.rs
    crates/polyplug/tests/stress_memory.rs
    crates/polyplug/tests/integration_graph.rs
  </files>
  <read_first>
    crates/polyplug/tests/stress_error.rs
    crates/polyplug/tests/registry_edge_cases.rs
    crates/polyplug/src/registry/runtime_store.rs
    .planning/phases/17-refactor-contractregistry-to-unified-runtimestore/17-CONTEXT.md
  </read_first>
  <action>
    For each test file, perform systematic replacements:

    1. Import path:
       - `use polyplug::registry::contract_registry::ContractRegistry;` -> `use polyplug::registry::runtime_store::RuntimeStore;`
       OR if using module-level import:
       - `use polyplug::registry::ContractRegistry;` -> `use polyplug::registry::RuntimeStore;`

    2. Static/thread-local declarations:
       - `RefCell<ContractRegistry>` -> `RefCell<RuntimeStore>`
       - `RefCell::new(ContractRegistry::new())` -> `RefCell::new(RuntimeStore::new())`
       - `static ... : RefCell<ContractRegistry>` -> `static ... : RefCell<RuntimeStore>`

    3. Local variable types:
       - `let registry: ContractRegistry` -> `let registry: RuntimeStore`
       - `let registry: Arc<ContractRegistry>` -> `let registry: Arc<RuntimeStore>`
       - `core::cell::Ref<'_, ContractRegistry>` -> `core::cell::Ref<'_, RuntimeStore>`

    4. Constructor calls:
       - `ContractRegistry::new()` -> `RuntimeStore::new()`

    5. Method calls (all files):
       - `.find_by_contract(...)` -> `.find_guest_contract(...)`
       - `.find_by_bundle(...)` -> `.find_guest_contract_by_bundle(...)`
       - `.find_slots_by_bundle(...)` -> `.get_bundle_plugin_slots(...)`
       - `.resolve(...)` -> `.resolve_guest_contract(...)`
       - `.swap_interface(...)` -> `.swap_guest_contract_interface(...)`
       - `.register(...)` -> `.register_guest_contract(...)`
       - `.declare_deps(...)` -> `.declare_bundle_dependencies(...)`
       - `.is_dependency_declared(...)` -> `.is_bundle_dependency_declared(...)`
       - `.get_slot_contract_id(...)` -> `.get_slot_guest_contract_id(...)`
       - `.get_interface_arc(...)` -> `.get_guest_contract_interface_arc(...)`
       - `.count_by_contract(...)` -> `.count_guest_contracts(...)`
       - `.find_all_by_contract(...)` -> `.find_all_guest_contracts(...)`

    6. Test function names: Keep unchanged (test names don't need to match method names)

    Per AGENTS.md §3: Ensure explicit type annotations remain on local bindings.
  </action>
  <verify>
    <automated>cargo test -p polyplug --lib 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "ContractRegistry" crates/polyplug/tests/ returns no matches
    - grep -r "contract_registry" crates/polyplug/tests/ returns no matches (except in old module path context)
    - grep -r "RuntimeStore" crates/polyplug/tests/ returns matches in all updated files
    - grep -r "find_guest_contract" crates/polyplug/tests/ returns matches
    - grep -r "get_bundle_plugin_slots" crates/polyplug/tests/ returns matches
    - `cargo test -p polyplug --lib` passes
  </acceptance_criteria>
  <done>
    All test files use RuntimeStore and renamed methods. All tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal Rust code | Pure refactor — no FFI boundary changes in this pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | Manifest parsing | accept | No changes to manifest parsing in Pass 1 |
| T-17-02 | Tampering | Registry methods | accept | Method signatures unchanged — only names changed |
</threat_model>

<verification>
After all tasks complete:
1. Run `cargo check --workspace` — must pass with zero warnings
2. Run `cargo test -p polyplug` — all tests must pass
3. Run `grep -r "ContractRegistry" crates/polyplug/src/` — must return zero matches (except doc comments)
4. Run `grep -r "contract_index" crates/polyplug/src/` — must return zero matches (should be guest_contract_index)
5. Run `cargo clippy --workspace -- -D warnings` — must pass
</verification>

<success_criteria>
1. ContractRegistry type renamed to RuntimeStore everywhere in crates/polyplug/src/
2. All Registry* types renamed to Plugin* or RuntimeStore*
3. All public methods use guest_contract or bundle prefix
4. All internal fields renamed (guest_contract_index, bundle_slots_index, bundle_declared_deps)
5. All tests pass: `cargo test -p polyplug` exits 0
6. All clippy warnings resolved: `cargo clippy --workspace -- -D warnings` exits 0
7. No ContractRegistry references remain in source files (verified by grep)
</success_criteria>

<output>
After completion, create `.planning/phases/17-refactor-contractregistry-to-unified-runtimestore/17-01-SUMMARY.md`
</output>