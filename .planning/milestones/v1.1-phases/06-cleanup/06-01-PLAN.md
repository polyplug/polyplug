---
phase: 06-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polyplug_abi/src/lib.rs
  - crates/polyplug/benches/vtable_dispatch.rs
  - sdks/csharp/guest/HostInterfaceStorage.cs
  - sdks/cpp/guest/polyplug/contract.hpp
  - crates/polyplug/src/registry/plugin_registry.rs
  - crates/polyplug_native/src/loader.rs
  - crates/polyplugc/src/generators/rust.rs
  - crates/polyplugc/src/generators/cpp.rs
  - crates/polyplugc/src/generators/python.rs
  - crates/polyplugc/src/generators/csharp.rs
  - crates/polyplugc/src/generators/lua.rs
  - crates/polyplugc/src/generators/js_quickjs.rs
autonomous: true
requirements: [CLN-01]
user_setup: []

must_haves:
  truths:
    - "No vtable terminology remains in source code"
    - "Legacy aliases removed from polyplug_abi"
    - "Benchmark file renamed to contract_dispatch.rs"
    - "SDK methods renamed from vtable() to interface()"
  artifacts:
    - path: "crates/polyplug_abi/src/lib.rs"
      provides: "No legacy aliases"
      contains: "!pub type GuestContractInterface"
    - path: "crates/polyplug/benches/contract_dispatch.rs"
      provides: "Renamed benchmark file"
      min_lines: 500
    - path: "sdks/csharp/guest/RuntimeAbiStorage.cs"
      provides: "Renamed C# storage class"
      contains: "RuntimeAbiStorage"
  key_links:
    - from: "crates/polyplug_abi/src/lib.rs"
      to: "All crate imports"
      via: "pub use GuestContractInterface, RuntimeAbi, DispatchMechanisms"
---

# Plan 06-01: Remove All VTable Naming

**Objective:** Remove all "vtable", "VTable", "VTABLE" terminology from codebase, including legacy aliases, variable names, comments, and method names.

## Tasks

### Task 1: Remove Legacy Aliases from polyplug_abi

<read_first>
- crates/polyplug_abi/src/lib.rs
- crates/polyplug/src/ffi.rs
</read_first>

<action>
1. Remove the legacy alias block (lines 46-58) from `crates/polyplug_abi/src/lib.rs`:
   - DELETE: `pub type GuestContractInterface = GuestContractInterface;`
   - DELETE: `pub type HostInterface = RuntimeAbi;`
   - DELETE: `pub type PluginDispatch = DispatchMechanisms;`
   - DELETE: The doc comments preceding each alias
   
2. Update all imports in `crates/polyplug/src/ffi.rs` from legacy to new names:
   - Change `use polyplug_abi::GuestContractInterface;` to `use polyplug_abi::GuestContractInterface;`
   - Change `use polyplug_abi::HostInterface;` to `use polyplug_abi::RuntimeAbi;`
   - Change `use polyplug_abi::PluginDispatch;` to `use polyplug_abi::DispatchMechanisms;`

3. Update all type references in the same file:
   - Replace `GuestContractInterface` with `GuestContractInterface`
   - Replace `HostInterface` with `RuntimeAbi`
   - Replace `PluginDispatch` with `DispatchMechanisms`
</action>

<acceptance_criteria>
- `grep -n "pub type GuestContractInterface" crates/polyplug_abi/src/lib.rs` returns 0 matches
- `grep -n "pub type HostInterface" crates/polyplug_abi/src/lib.rs` returns 0 matches
- `grep -n "pub type PluginDispatch" crates/polyplug_abi/src/lib.rs` returns 0 matches
- `grep -n "GuestContractInterface" crates/polyplug/src/ffi.rs` returns only matches in comments explaining the rename
- `cargo build -p polyplug_abi` exits with code 0
</acceptance_criteria>

---

### Task 2: Rename Benchmark File and Update Variables

<read_first>
- crates/polyplug/benches/vtable_dispatch.rs
- crates/polyplug/Cargo.toml
</read_first>

<action>
1. Rename the benchmark file:
   ```bash
   git mv crates/polyplug/benches/vtable_dispatch.rs crates/polyplug/benches/contract_dispatch.rs
   ```

2. Update imports in the renamed file (lines 20-26):
   - Change `use polyplug_abi::HostInterface;` to `use polyplug_abi::RuntimeAbi;`
   - Change `use polyplug_abi::GuestContractInterface;` to `use polyplug_abi::GuestContractInterface;`

3. Rename all variable and parameter names containing "vtable":
   - `LAST_VTABLE` thread_local -> `LAST_INTERFACE`
   - `vtable` variables -> `interface`
   - `vtable_ptr` -> `interface_ptr`
   - `host_vtable` -> `host_abi` or `runtime_abi`

4. Update comments:
   - Line 49: "captured vtable state" -> "captured interface state"
   - Line 257: "Retrieve the dispatch function...from the last registered vtable" -> "from the last registered interface"
   - Line 289: "Measures the cost of a single vtable function call" -> "single contract interface function call"

5. Update type references:
   - `HostInterface {` -> `RuntimeAbi {`
   - `*const GuestContractInterface` -> `*const GuestContractInterface`
   - `&GuestContractInterface` -> `&GuestContractInterface`

6. Update Cargo.toml bench reference if needed.
</action>

<acceptance_criteria>
- `ls crates/polyplug/benches/vtable_dispatch.rs` fails (file does not exist)
- `ls crates/polyplug/benches/contract_dispatch.rs` succeeds
- `grep -c "vtable|VTable" crates/polyplug/benches/contract_dispatch.rs` returns 0 (excluding comments about rename history)
- `grep -c "interface|Interface" crates/polyplug/benches/contract_dispatch.rs` returns >= 20
- `cargo bench -p polyplug --bench contract_dispatch -- --list` exits with code 0
</acceptance_criteria>

---

### Task 3: Rename C# HostInterfaceStorage to RuntimeAbiStorage

<read_first>
- sdks/csharp/guest/HostInterfaceStorage.cs
- sdks/csharp/guest/*.cs (for import patterns)
</read_first>

<action>
1. Rename the file:
   ```bash
   git mv sdks/csharp/guest/HostInterfaceStorage.cs sdks/csharp/guest/RuntimeAbiStorage.cs
   ```

2. Update class name inside the file:
   - Change `public static class HostInterfaceStorage` to `public static class RuntimeAbiStorage`

3. Rename method names:
   - `StoreHostInterface` -> `StoreRuntimeAbi`
   - `GetHostInterface` -> `GetRuntimeAbi`

4. Rename private field:
   - `s_hostVTable` -> `s_runtimeAbi`

5. Update parameter names:
   - `hostVTable` -> `runtimeAbi`

6. Search for and update any references to this class in other C# files:
   ```bash
   grep -r "HostInterfaceStorage" sdks/csharp/ --include="*.cs"
   ```
   Update each found reference to use `RuntimeAbiStorage`.
</action>

<acceptance_criteria>
- `ls sdks/csharp/guest/HostInterfaceStorage.cs` fails
- `ls sdks/csharp/guest/RuntimeAbiStorage.cs` succeeds
- `grep -r "HostInterfaceStorage" sdks/csharp/ --include="*.cs"` returns 0 matches
- `grep -r "RuntimeAbiStorage" sdks/csharp/ --include="*.cs"` returns >= 1 match
- File contains class declaration: `public static class RuntimeAbiStorage`
</acceptance_criteria>

---

### Task 4: Update C++ contract.hpp vtable() Method to interface()

<read_first>
- sdks/cpp/guest/polyplug/contract.hpp
- sdks/cpp/abi/polyplug/abi.hpp (for type definitions)
</read_first>

<action>
1. Rename the virtual method `vtable()` to `interface()`:
   - Change line 32: `const GuestContractInterface* vtable() const noexcept override` to `const GuestContractInterface* interface() const noexcept override`
   - Change line 45: `virtual const GuestContractInterface* vtable() const noexcept = 0;` to `virtual const GuestContractInterface* interface() const noexcept = 0;`

2. Update the doc comment (line 42-44):
   - Change "Returns a pointer to the statically-allocated GuestContractInterface" to "Returns a pointer to the statically-allocated GuestContractInterface"
   - Change "never return a pointer to a stack-allocated VTable" to "never return a pointer to a stack-allocated interface"

3. Update the header comment (line 5-7):
   - Change "provide a static vtable()" to "provide a static interface()"
   - Change "returns a pointer to a statically-allocated GuestContractInterface" to "returns a pointer to a statically-allocated GuestContractInterface"

4. Update the example code (lines 30-37):
   - Change `const GuestContractInterface* vtable()` to `const GuestContractInterface* interface()`
   - Change `static const GuestContractInterface kVTable;` to `static const GuestContractInterface kInterface;` (note: variable name in example)
   - Update comment "static VTable" to "static interface"

5. Update type reference in doc comment:
   - `GuestContractInterface` -> `GuestContractInterface` (if abi.hpp exports this name)
</action>

<acceptance_criteria>
- `grep -n "vtable()" sdks/cpp/guest/polyplug/contract.hpp` returns 0 matches
- `grep -n "interface()" sdks/cpp/guest/polyplug/contract.hpp` returns >= 2 matches (declaration and example)
- `grep -n "VTable" sdks/cpp/guest/polyplug/contract.hpp` returns 0 matches
- `grep -n "interface" sdks/cpp/guest/polyplug/contract.hpp` returns >= 5 matches
</acceptance_criteria>

---

### Task 5: Update Codegen Generator Comments and Variable Names

<read_first>
- crates/polyplugc/src/generators/rust.rs
- crates/polyplugc/src/generators/cpp.rs
- crates/polyplugc/src/generators/python.rs
- crates/polyplugc/src/generators/csharp.rs
- crates/polyplugc/src/generators/lua.rs
- crates/polyplugc/src/generators/js_quickjs.rs
</read_first>

<action>
For each generator file in `crates/polyplugc/src/generators/`:

1. Update comments containing "vtable" terminology:
   - "vtable" -> "interface" or "contract interface"
   - "VTable" -> "Interface" or "ContractInterface"
   - "plugin vtable" -> "guest contract interface"

2. Rename local variables containing "vtable":
   - `vtable` -> `interface`
   - `vtable_ptr` -> `interface_ptr`
   - `build_vtable` -> `build_interface`

3. Update function names if they contain "vtable" (be careful - some may be generating actual vtable-related code for guests):
   - Check if function generates host-side code -> rename to use "interface"
   - Check if function generates guest-side vtable -> keep but update comments to clarify it's a "contract interface"

4. Ensure generated code templates use correct terminology:
   - Template strings with "vtable" -> "interface"
   - Template comments -> update terminology

5. For `rust.rs` specifically:
   - Update any doc comments explaining vtable generation
   - Rename any internal helper functions

6. For `cpp.rs` specifically:
   - Update to generate `interface()` method instead of `vtable()` in generated classes
</action>

<acceptance_criteria>
- `grep -c "vtable" crates/polyplugc/src/generators/*.rs` returns 0 for all files (excluding explanatory comments about rename history)
- `grep -c "interface" crates/polyplugc/src/generators/*.rs` returns >= 1 for each file
- `cargo build -p polyplugc` exits with code 0
</acceptance_criteria>

---

### Task 6: Update Loader Crate Comments and Variables

<read_first>
- crates/polyplug_native/src/loader.rs
- crates/polyplug_python/src/lib.rs
- crates/polyplug_lua/src/loader.rs
- crates/polyplug_js/src/loader.rs
- crates/polyplug_dotnet/src/lib.rs
- crates/polyplug/src/registry/plugin_registry.rs
</read_first>

<action>
For each loader file and registry file:

1. Update comments containing "vtable" terminology:
   - "vtable" -> "interface"
   - "vtable pointer" -> "interface pointer"
   - "register vtable" -> "register interface"

2. Rename local variables and parameters:
   - `vtable` -> `interface`
   - `vtable_ptr` -> `interface_ptr`

3. Update doc comments:
   - Function docs referencing "vtable" -> "contract interface"

4. In `plugin_registry.rs` specifically:
   - Update struct doc comments
   - Update method doc comments
   - Rename any internal variables named `vtable`

5. Do NOT change:
   - Type imports (already using correct names from polyplug_abi)
   - FFI function signatures (use what polyplug_abi exports)
</action>

<acceptance_criteria>
- `grep -c "vtable" crates/polyplug_native/src/loader.rs` returns 0 (excluding history comments)
- `grep -c "vtable" crates/polyplug/src/registry/plugin_registry.rs` returns 0 (excluding history comments)
- `cargo build -p polyplug_native -p polyplug_python -p polyplug_lua -p polyplug_js -p polyplug_dotnet` exits with code 0
</acceptance_criteria>

---

## Verification

After all tasks complete:

```bash
# Verify no vtable terminology in source files (excluding doc history)
grep -ri "vtable|VTable|VTABLE" crates/ sdks/ --include="*.rs" --include="*.cs" --include="*.py" --include="*.lua" --include="*.js" --include="*.hpp" | grep -v "renamed from|was called|previously known"

# Expected: 0 matches (or only matches in explanatory comments about rename history)

# Build verification
cargo build --workspace

# Expected: success
```

## must_haves

This plan delivers:
- Legacy aliases removed (no GuestContractInterface, HostInterface, PluginDispatch aliases)
- Benchmark file renamed (contract_dispatch.rs exists, vtable_dispatch.rs does not)
- SDK classes/methods renamed (RuntimeAbiStorage, interface() method)
- Generator code updated (produces interface terminology)
- Loader/registry comments updated (clear terminology)

---

<output>
After completion, create `.planning/phases/06-cleanup/06-01-SUMMARY.md`
</output>