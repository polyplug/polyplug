---
phase: 06-cleanup
plan: 07
type: execute
wave: 2
depends_on: [06-05, 06-06]
files_modified:
  - sdks/python/host/polyplug/runtime.py
  - sdks/lua/host/polyplug/runtime.lua
  - sdks/cpp/host/polyplug/runtime.hpp
  - sdks/csharp/host/Runtime.cs
  - sdks/js/host/polyplug/mod.js
  - sdks/rust/guest/src/lib.rs
autonomous: true
gap_closure: true
requirements: [CLN-01]
user_setup: []

must_haves:
  truths:
    - "SDK host files use HostContractInterface type (not HostContractVTable)"
    - "SDK host files use interface terminology in function signatures"
    - "SDK files compile/load successfully"
  artifacts:
    - path: "sdks/python/host/polyplug/runtime.py"
      provides: "Updated Python SDK"
      contains: "HostContractInterface"
    - path: "sdks/lua/host/polyplug/runtime.lua"
      provides: "Updated Lua SDK"
      contains: "HostContractInterface"
  key_links:
    - from: "SDK host files"
      to: "polyplug_abi types"
      via: "FFI cdef / ctypes"
---

# Plan 06-07: Update SDK Host Files to Interface Terminology

**Objective:** Rename HostContractVTable to HostContractInterface in all SDK host files and update related variable/function names.

## Tasks

### Task 1: Update Python SDK Host File

<read_first>
- sdks/python/host/polyplug/runtime.py
- sdks/python/polyplug_abi/polyplug_abi/__init__.py
</read_first>

<action>
Update Python SDK to use interface terminology:

1. Rename ctypes structures:
   - `HostContractVTableHeader` -> `HostContractInterfaceHeader`
   - `HostContractVTable` -> `HostContractInterface`

2. Rename field names in structures (if needed):
   - `vtable_version` -> `interface_version`

3. Rename method/function parameters:
   - `vtable` parameter -> `interface`

4. Rename internal storage:
   - `_host_contract_vtables` -> `_host_contract_interfaces`

5. Update docstrings and comments.
</action>

<acceptance_criteria>
- `grep -c "HostContractVTable" sdks/python/host/polyplug/runtime.py` returns 0
- `grep -c "HostContractInterface" sdks/python/host/polyplug/runtime.py` returns >= 2
- Python SDK loads without errors
</acceptance_criteria>

---

### Task 2: Update Lua SDK Host File

<read_first>
- sdks/lua/host/polyplug/runtime.lua
- sdks/lua/abi/polyplug_abi.lua
</read_first>

<action>
Update Lua SDK to use interface terminology:

1. Rename FFI cdef structures:
   - `HostContractVTableHeader` -> `HostContractInterfaceHeader`
   - `HostContractVTable` -> `HostContractInterface`

2. Rename function parameters:
   - `vtable` -> `interface` in polyplug_runtime_register_host_contract

3. Update doc comments.
</action>

<acceptance_criteria>
- `grep -c "HostContractVTable" sdks/lua/host/polyplug/runtime.lua` returns 0
- `grep -c "HostContractInterface" sdks/lua/host/polyplug/runtime.lua` returns >= 2
- `grep -c "HostContractVTable" sdks/lua/abi/polyplug_abi.lua` returns 0
</acceptance_criteria>

---

### Task 3: Update C++ SDK Host File

<read_first>
- sdks/cpp/host/polyplug/runtime.hpp
- sdks/cpp/abi/polyplug/abi.hpp
</read_first>

<action>
Update C++ SDK to use interface terminology:

1. Rename function parameters:
   - `const HostContractVTable* vtable` -> `const HostContractInterface* interface`

2. Rename struct types if defined locally:
   - Check if HostContractVTable is defined in abi.hpp
   - Rename to HostContractInterface

3. Update doc comments.
</action>

<acceptance_criteria>
- `grep -c "HostContractVTable" sdks/cpp/host/polyplug/runtime.hpp` returns 0
- `grep -c "HostContractInterface" sdks/cpp/host/polyplug/runtime.hpp` returns >= 1
</acceptance_criteria>

---

### Task 4: Update C# SDK Host File

<read_first>
- sdks/csharp/host/Runtime.cs
- sdks/csharp/Abi/HostContractInterface.cs (or similar)
</read_first>

<action>
Update C# SDK to use interface terminology:

1. Rename struct/class if defined:
   - `HostContractVTable` -> `HostContractInterface`

2. Rename method parameters:
   - `vtable` -> `interface`

3. Update doc comments.
</action>

<acceptance_criteria>
- `grep -c "HostContractVTable" sdks/csharp/host/Runtime.cs` returns 0
- `grep -c "HostContractInterface" sdks/csharp/host/Runtime.cs` returns >= 1
</acceptance_criteria>

---

### Task 5: Update JS SDK Host File

<read_first>
- sdks/js/host/polyplug/mod.js
- sdks/js/abi/polyplug_abi.ts
</read_first>

<action>
Update JS SDK to use interface terminology:

1. Rename type references:
   - `HostContractVTable` -> `HostContractInterface`

2. Rename function parameters:
   - `vtable` -> `interface` in JSDoc comments

3. Update type definitions.
</action>

<acceptance_criteria>
- `grep -c "HostContractVTable" sdks/js/host/polyplug/mod.js` returns 0
- `grep -c "HostContractInterface" sdks/js/host/polyplug/mod.js` returns >= 1
</acceptance_criteria>

---

### Task 6: Update Rust Guest SDK Legacy Aliases

<read_first>
- sdks/rust/guest/src/lib.rs
</read_first>

<action>
Update Rust guest SDK legacy aliases:

1. Check the HostContractVTable alias:
   - Line 194: `pub type HostContractVTable = polyplug_abi::HostContractInterface;`
   - This alias is provided for backwards compatibility

2. Update the alias to use clearer naming:
   - Keep the alias but update doc comment to clarify it's deprecated
   - OR rename to `HostContractInterface` directly

3. Update any other legacy type aliases:
   - `HostContractVTableHeader` -> clarify or rename

4. Ensure doc comments indicate these are legacy aliases for transition.
</action>

<acceptance_criteria>
- `grep -c "HostContractVTable" sdks/rust/guest/src/lib.rs` returns 0 or only in deprecation comment
- `grep -c "HostContractInterface" sdks/rust/guest/src/lib.rs` returns >= 2
</acceptance_criteria>

---

## Verification

```bash
# Verify no HostContractVTable in SDK host files
grep -rn "HostContractVTable" sdks/python/host/ sdks/lua/host/ sdks/cpp/host/ sdks/csharp/host/ sdks/js/host/

# Expected: 0 matches (or only in explanatory comments)

# Verify HostContractInterface used
grep -rn "HostContractInterface" sdks/

# Expected: >= 5 matches (one per SDK)
```

## must_haves

This plan delivers:
- All SDK host files use HostContractInterface terminology
- No HostContractVTable type names in SDK source files
- SDK files load/compile successfully
- Consistent naming across all 5 SDKs

---

<output>
After completion, create `.planning/phases/06-cleanup/06-07-SUMMARY.md`
</output>