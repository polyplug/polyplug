---
phase: 16-milestone-gap-closure
plan: 03
type: execute
wave: 3
depends_on: [16-02]
files_modified: [crates/polyplugc/src/generators/cpp.rs, crates/polyplugc/src/generators/csharp.rs, crates/polyplugc/src/generators/python.rs]
autonomous: true
gap_closure: true
requirements: [CLN-01]

must_haves:
  truths:
    - "No 'VTable' in generator comments (non-test code)"
    - "Comments use 'Interface' terminology consistently"
  artifacts:
    - path: "crates/polyplugc/src/generators/cpp.rs"
      contains: "// Interface static"
      contains: "// ─── Host Interface Factories Generation"
    - path: "crates/polyplugc/src/generators/csharp.rs"
      contains: "// Interface field and static constructor"
    - path: "crates/polyplugc/src/generators/python.rs"
      contains: "// ─── Host Interface Factories Generation"
  key_links:
    - from: "generators/*.rs"
      to: "generated SDK code"
      via: "code generation"
      pattern: "Interface terminology in comments"
---

<objective>
Fix 5 generator comments that still use "VTable" terminology. Update to use "Interface" terminology consistent with CLN-01 cleanup.

Purpose: Complete the VTable→Interface terminology cleanup that was missed in generator comments
Output: Generators with consistent Interface terminology in comments
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Specific comment locations needing fixes -->
cpp.rs:445: `// VTable static` → `// Interface static`
cpp.rs:1863: `// ─── Host VTable Factories Generation` → `// ─── Host Interface Factories Generation`
csharp.rs:387: `// VTable field and static constructor` → `// Interface field and static constructor`
csharp.rs:540: `// VTable field and static constructor` → `// Interface field and static constructor`
python.rs:2125: `// ─── Host VTable Factories Generation` → `// ─── Host Interface Factories Generation`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix VTable comments in cpp.rs generator</name>
  <files>crates/polyplugc/src/generators/cpp.rs</files>
  <read_first>
    - crates/polyplugc/src/generators/cpp.rs (current comment state at lines 445 and 1863)
  </read_first>
  <action>
    In crates/polyplugc/src/generators/cpp.rs, update two comments:

    1. Line 445:
       Change: `// VTable static`
       To: `// Interface static`

    2. Line 1863:
       Change: `// ─── Host VTable Factories Generation ─────────────────────────────────────────`
       To: `// ─── Host Interface Factories Generation ─────────────────────────────────────────`
  </action>
  <verify>
    <automated>grep "// Interface static" crates/polyplugc/src/generators/cpp.rs | wc -l | grep -q "^1$"</automated>
    <automated>grep "// ─── Host Interface Factories Generation" crates/polyplugc/src/generators/cpp.rs | wc -l | grep -q "^1$"</automated>
    <automated>grep "// VTable" crates/polyplugc/src/generators/cpp.rs | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>
    cpp.rs has no VTable in comments
    Interface terminology used consistently
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix VTable comments in csharp.rs generator</name>
  <files>crates/polyplugc/src/generators/csharp.rs</files>
  <read_first>
    - crates/polyplugc/src/generators/csharp.rs (current comment state at lines 387 and 540)
  </read_first>
  <action>
    In crates/polyplugc/src/generators/csharp.rs, update two comments:

    1. Line 387:
       Change: `// VTable field and static constructor (GCHandle pinning)`
       To: `// Interface field and static constructor (GCHandle pinning)`

    2. Line 540:
       Change: `// VTable field and static constructor (GCHandle pinning)`
       To: `// Interface field and static constructor (GCHandle pinning)`
  </action>
  <verify>
    <automated>grep "// Interface field and static constructor" crates/polyplugc/src/generators/csharp.rs | wc -l | grep -q "^2$"</automated>
    <automated>grep "// VTable" crates/polyplugc/src/generators/csharp.rs | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>
    csharp.rs has no VTable in comments
    Interface terminology used consistently
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix VTable comment in python.rs generator</name>
  <files>crates/polyplugc/src/generators/python.rs</files>
  <read_first>
    - crates/polyplugc/src/generators/python.rs (current comment state at line 2125)
  </read_first>
  <action>
    In crates/polyplugc/src/generators/python.rs, update one comment:

    Line 2125:
    Change: `// ─── Host VTable Factories Generation ─────────────────────────────────────────`
    To: `// ─── Host Interface Factories Generation ─────────────────────────────────────────`
  </action>
  <verify>
    <automated>grep "// ─── Host Interface Factories Generation" crates/polyplugc/src/generators/python.rs | wc -l | grep -q "^1$"</automated>
    <automated>grep "// VTable" crates/polyplugc/src/generators/python.rs | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>
    python.rs has no VTable in comments
    Interface terminology used consistently
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generator → Generated code | Comment terminology affects generated code comments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | N/A | Comment update only | accept | No security impact - terminology consistency |
</threat_model>

<verification>
grep confirms no "VTable" in generator comments (excluding test files)
</verification>

<success_criteria>
- All 5 generator comments updated to use Interface terminology
- grep "// VTable" in generators/ returns 0 matches
- grep "// Interface" shows 5 matches (one in python.rs, two in csharp.rs, two in cpp.rs)
</success_criteria>

<output>
After completion, create `.planning/phases/16-milestone-gap-closure/16-03-SUMMARY.md`
</output>