---
phase: 11-guest-calling-convention-missing-introspection
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - crates/polyplug_abi/src/host/runtime_context.rs (DELETE)
  - crates/polyplug_abi/src/host/host_context.rs (DELETE)
  - crates/polyplug_abi/src/host/mod.rs
  - crates/polyplug_abi/src/lib.rs
  - crates/polyplug/src/runtime.rs
  - crates/polyplug/src/registry/plugin_registry.rs
  - crates/polyplug_abi/src/guest/guest_contract_interface.rs
  - crates/polyplug_abi/src/host/host_contract_interface.rs
autonomous: true
requirements: [D-03]
user_setup: []
must_haves:
  truths:
    - "RuntimeContext and HostContext types no longer exist in polyplug_abi"
    - "HostInterface directly contains runtime: *mut c_void opaque pointer"
    - "All callers of RuntimeContext updated to use opaque pointer or self-passing pattern"
  artifacts:
    - path: "crates/polyplug_abi/src/host/runtime_context.rs"
      provides: "N/A - FILE DELETED"
      exists: false
    - path: "crates/polyplug_abi/src/host/host_context.rs"
      provides: "N/A - FILE DELETED"
      exists: false
  key_links:
    - from: "crates/polyplug_abi/src/host/mod.rs"
      to: "runtime_context, host_context"
      via: "mod declarations removed"
      pattern: "NO runtime_context or host_context mod declarations"
---

<objective>
Delete RuntimeContext and HostContext wrapper types, removing the indirection layer.

Purpose: Per D-03, these wrapper types add confusion without benefit. Interfaces now directly contain the opaque runtime pointer, and functions use self-passing pattern instead of passing rt_ctx separately.
Output: Deleted files, updated imports, updated callers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-guest-calling-convention-missing-introspection/11-CONTEXT.md
@.planning/phases/11-guest-calling-convention-missing-introspection/11-RESEARCH.md

<interfaces>
<!-- Executor should understand what to delete and what patterns replace them. -->

From crates/polyplug_abi/src/host/runtime_context.rs (TO DELETE):
```rust
#[repr(C)]
pub struct RuntimeContext {
    pub data: *mut c_void,  // Wrapped pointer to HostContext
}
// Size: 8 bytes
```

From crates/polyplug_abi/src/host/host_context.rs (TO DELETE):
```rust
#[repr(C)]
pub struct HostContext {
    pub runtime: *mut core::ffi::c_void,
    pub bundle_id: u64,
    pub host_abi_version: u32,
}
// Size: 24 bytes
```

From CONTEXT.md D-03:
- Delete RuntimeContext and HostContext
- Interfaces directly contain runtime: *mut c_void
- Functions take self: *const Interface instead of rt_ctx

Replacement pattern (from Task 1 in Plan 01):
- HostInterface now has runtime: *mut c_void field at offset 0
- Functions use self: *const HostInterface as first parameter
- SDKs hide self-passing from users

Files that import RuntimeContext (from grep analysis):
- crates/polyplug_abi/src/host/runtime_abi.rs (already renamed to host_interface.rs in Plan 01)
- crates/polyplug_abi/src/host/host_contract_interface.rs
- crates/polyplug_abi/src/guest/guest_contract_interface.rs
- crates/polyplug/src/runtime.rs
- crates/polyplug/src/registry/plugin_registry.rs
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete RuntimeContext and HostContext files and update exports</name>
  <files>crates/polyplug_abi/src/host/runtime_context.rs, crates/polyplug_abi/src/host/host_context.rs, crates/polyplug_abi/src/host/mod.rs, crates/polyplug_abi/src/lib.rs</files>
  <read_first>
    - crates/polyplug_abi/src/host/runtime_context.rs (to understand what to delete)
    - crates/polyplug_abi/src/host/host_context.rs (to understand what to delete)
    - crates/polyplug_abi/src/host/mod.rs (current exports)
    - crates/polyplug_abi/src/lib.rs (crate root exports)
    - .planning/phases/11-guest-calling-convention-missing-introspection/11-CONTEXT.md (D-03 specification)
  </read_first>
  <behavior>
    - Test: runtime_context.rs file does NOT exist
    - Test: host_context.rs file does NOT exist
    - Test: mod.rs does NOT contain "pub mod runtime_context" or "pub mod host_context"
    - Test: mod.rs does NOT contain "pub use runtime_context::RuntimeContext"
    - Test: lib.rs does NOT export RuntimeContext
  </behavior>
  <action>
Delete both wrapper files:
1. Delete crates/polyplug_abi/src/host/runtime_context.rs (file delete)
2. Delete crates/polyplug_abi/src/host/host_context.rs (file delete)

Update crates/polyplug_abi/src/host/mod.rs:
Remove lines:
- pub mod runtime_context;
- pub mod host_context;
- pub use runtime_context::RuntimeContext;

Keep:
- pub mod host_interface;
- pub mod runtime_interface;
- pub use host_interface::HostInterface;
- pub use runtime_interface::RuntimeInterface;
- pub mod host_contract_interface;
- pub mod host_contract_instance;
- pub use host_contract_instance::HostContractInstance;
- pub use host_contract_interface::HostContractInterface;

Update crates/polyplug_abi/src/lib.rs:
Remove RuntimeContext from exports:
Change: pub use host::{HostContractInterface, HostContractInstance, RuntimeAbi, RuntimeContext};
To: pub use host::{HostContractInterface, HostContractInstance, HostInterface, RuntimeInterface};

Note: HostContext was never exported from lib.rs (it was internal).
  </action>
  <verify>
    <automated>test ! -f crates/polyplug_abi/src/host/runtime_context.rs && test ! -f crates/polyplug_abi/src/host/host_context.rs && cargo build -p polyplug_abi</automated>
  </verify>
  <done>
    - runtime_context.rs file deleted
    - host_context.rs file deleted
    - mod.rs has no runtime_context or host_context module declarations
    - lib.rs has no RuntimeContext export
    - polyplug_abi crate compiles without RuntimeContext/HostContext references
  </done>
</task>

<task type="auto">
  <name>Task 2: Update GuestContractInterface and HostContractInterface to remove RuntimeContext imports</name>
  <files>crates/polyplug_abi/src/guest/guest_contract_interface.rs, crates/polyplug_abi/src/host/host_contract_interface.rs</files>
  <read_first>
    - crates/polyplug_abi/src/guest/guest_contract_interface.rs (uses RuntimeContext in signatures)
    - crates/polyplug_abi/src/host/host_contract_interface.rs (uses RuntimeContext in signatures)
    - crates/polyplug_abi/src/host/host_interface.rs (HostInterface type from Plan 01)
    - .planning/phases/11-guest-calling-convention-missing-introspection/11-CONTEXT.md (D-12, D-13)
  </read_first>
  <action>
This task prepares the files for the signature changes in Wave 4 (Plan 04).
For Wave 2, just remove RuntimeContext imports but keep placeholder comments.

In guest_contract_interface.rs:
1. Remove: use crate::host::RuntimeContext;
2. Add comment: // TODO(Wave4): create_instance/destroy_instance will take *const HostInterface per D-12
3. Keep signatures unchanged for now (Wave 4 will update them)

In host_contract_interface.rs:
1. Remove: use crate::host::RuntimeContext;
2. Add comment: // TODO(Wave4): create_instance/destroy_instance will take self pointer per D-13
3. Keep signatures unchanged for now (Wave 4 will update them)

The actual signature changes happen in Plan 04. This task just removes the dead import.
  </action>
  <verify>
    <automated>grep -L "RuntimeContext" crates/polyplug_abi/src/guest/guest_contract_interface.rs crates/polyplug_abi/src/host/host_contract_interface.rs && cargo build -p polyplug_abi</automated>
  </verify>
  <done>
    - guest_contract_interface.rs has no RuntimeContext import
    - host_contract_interface.rs has no RuntimeContext import
    - Both files compile successfully
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Runtime and Registry to remove RuntimeContext usage</name>
  <files>crates/polyplug/src/runtime.rs, crates/polyplug/src/registry/plugin_registry.rs</files>
  <read_first>
    - crates/polyplug/src/runtime.rs (imports RuntimeContext, HostContext, has as_context() method)
    - crates/polyplug/src/registry/plugin_registry.rs (test callbacks use RuntimeContext)
    - crates/polyplug/src/host/host_context.rs (check if exists - should NOT per earlier check)
    - crates/polyplug_abi/src/host/host_interface.rs (HostInterface type)
    - .planning/phases/11-guest-calling-convention-missing-introspection/11-CONTEXT.md (D-03)
  </read_first>
  <action>
In crates/polyplug/src/runtime.rs:
1. Remove imports: RuntimeContext, HostContext (if present)
2. Remove or comment out as_context() method (this returned RuntimeContext wrapper)
3. Remove or comment out as_context_ptr() method that created HostContext
4. Update host_abi type from &'static RuntimeAbi to &'static HostInterface (Plan 01 renamed it)
5. Add TODO comments for Wave 4: "// TODO(Wave4): host functions will use self-passing pattern"

In crates/polyplug/src/registry/plugin_registry.rs:
1. Update test callbacks: noop_create_instance, noop_destroy_instance
   - Change parameter from RuntimeContext to placeholder comment
   - For now, use a raw pointer: *mut core::ffi::c_void as placeholder
   - Wave 4 will properly update these to use HostInterface

2. Add comment: "// TODO(Wave4): callbacks will take *const HostInterface"

Note: The actual host functions (host_register_contract, host_alloc, etc.) in runtime.rs
still need the rt_ctx parameter for now. Wave 4 will update these to use self-passing.
The key change here is removing the RuntimeContext TYPE, not changing all signatures yet.
  </action>
  <verify>
    <automated>grep -L "RuntimeContext" crates/polyplug/src/runtime.rs && grep -L "HostContext" crates/polyplug/src/runtime.rs && cargo build -p polyplug</automated>
  </verify>
  <done>
    - runtime.rs has no RuntimeContext import or usage
    - runtime.rs has no HostContext import or usage
    - runtime.rs host_abi field uses HostInterface type (from Plan 01)
    - plugin_registry.rs test callbacks updated for placeholder pointer type
    - polyplug crate compiles
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| host→runtime | Host passes opaque pointer via interface, runtime validates |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | Opaque pointer handling | mitigate | Interface functions validate self pointer is non-null before use |
| T-11-02-02 | DoS | Missing runtime pointer | accept | Caller contract: must pass valid interface pointer |
</threat_model>

<verification>
cargo build -p polyplug_abi && cargo build -p polyplug
grep fails to find RuntimeContext in modified files
grep fails to find HostContext in modified files
</verification>

<success_criteria>
- RuntimeContext and HostContext files deleted
- No RuntimeContext/HostContext exports remain
- polyplug_abi and polyplug crates compile
- HostInterface used where RuntimeAbi was used
</success_criteria>

<output>
After completion, create `.planning/phases/11-guest-calling-convention-missing-introspection/11-02-SUMMARY.md`
</output>