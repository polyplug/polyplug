---
phase: 15-final-cleanup
plan: 03
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - crates/polyplug/src/runtime.rs
autonomous: true
requirements: [CLN-01, CLN-04]
user_setup: []
must_haves:
  truths:
    - "Runtime source uses interface terminology"
    - "Test helper functions renamed appropriately"
    - "All tests pass after updates"
  artifacts:
    - path: "crates/polyplug/src/runtime.rs"
      provides: "Runtime implementation and tests"
      contains: "HostContractInterface"
  key_links:
    - from: "runtime.rs test helpers"
      to: "test assertions"
      via: "function calls"
      pattern: "create_host_contract_interface"
---

<objective>
Update runtime.rs source file to use interface terminology. Focus on test helper functions and comments that reference vtable.

Purpose: Clean up runtime source to match project terminology conventions.
Output: runtime.rs with interface terminology, all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-final-cleanup/15-RESEARCH.md

<interfaces>
<!-- From RESEARCH.md - specific locations to change in runtime.rs -->

Key locations from grep audit:
- Line 1617: `create_host_contract_vtable` function name
- Lines 1656-1790: `vtable` variable names in tests
- Line 1885: `create_counting_host_contract_vtable` function name

Function rename pattern:
- `create_host_contract_vtable` → `create_host_contract_interface`
- `create_counting_host_contract_vtable` → `create_counting_host_contract_interface`
- Variable `vtable` → `interface` (in test contexts)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename test helper functions in runtime.rs</name>
  <files>crates/polyplug/src/runtime.rs</files>
  <read_first>
    - crates/polyplug/src/runtime.rs (lines 1610-2000 for test section)
  </read_first>
  <behavior>
    - Function `create_host_contract_vtable` renamed to `create_host_contract_interface`
    - Function `create_counting_host_contract_vtable` renamed to `create_counting_host_contract_interface`
    - All call sites updated
    - Tests pass after renaming
  </behavior>
  <action>
    1. Read runtime.rs test section (lines 1610-2000).
    2. Rename function `create_host_contract_vtable` at line 1617:
       - Change function name to `create_host_contract_interface`
       - Update all call sites that reference this function
    3. Rename function `create_counting_host_contract_vtable` at line 1885:
       - Change function name to `create_counting_host_contract_interface`
       - Update all call sites in tests (lines 1920, 1989, 2058, 2060)
    4. Run tests to verify changes don't break functionality:
       ```bash
       cargo test -p polyplug runtime_host_contracts -q
       ```
  </action>
  <verify>
    <automated>grep -n "create_host_contract_vtable\|create_counting_host_contract_vtable" crates/polyplug/src/runtime.rs | wc -l</automated>
    Expected: 0
  </verify>
  <done>Test helper functions renamed to use interface terminology. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update variable names and comments in runtime.rs tests</name>
  <files>crates/polyplug/src/runtime.rs</files>
  <read_first>
    - crates/polyplug/src/runtime.rs (lines 1650-1800)
  </read_first>
  <behavior>
    - Variable `vtable` renamed to `interface` in test contexts
    - Variable `found_vtable` renamed to `found_interface`
    - SAFETY comments updated
    - Tests pass
  </behavior>
  <action>
    1. Read runtime.rs test section (lines 1650-1800).
    2. Rename variables:
       - Line 1656: `let vtable:` → `let interface:`
       - Line 1659: `runtime.register_host_contract(contract_id, vtable)` → `runtime.register_host_contract(contract_id, interface)`
       - Line 1664: `let found_vtable:` → `let found_interface:`
       - Line 1666: `assert_eq!(found_vtable.contract_id.id(), contract_id)` → `assert_eq!(found_interface.contract_id.id(), contract_id)`
       - Lines 1676-1677: similar renames for `vtable1`, `vtable2`
       - Lines 1702, 1734, 1787: similar renames
    3. Run affected tests:
       ```bash
       cargo test -p polyplug -q
       ```
  </action>
  <verify>
    <automated>grep -n "let vtable\|found_vtable\|vtable1\|vtable2" crates/polyplug/src/runtime.rs | wc -l</automated>
    Expected: 0
  </verify>
  <done>Runtime test variables renamed. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test helpers → Test assertions | Variable rename must propagate to all call sites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Tampering | Variable renames | mitigate | Run full test suite after changes |
| T-15-03-02 | Denial | Test failures | mitigate | Incremental testing with cargo test |
</threat_model>

<verification>
Run `cargo test -p polyplug -q` to verify all runtime tests pass after changes.
</verification>

<success_criteria>
- runtime.rs contains no vtable terminology (except ABI fields if any)
- Test helper functions renamed to interface terminology
- All runtime tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-final-cleanup/15-03-SUMMARY.md`
</output>