---
phase: 15-final-cleanup
plan: 04
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - crates/polyplug/tests/*.rs (28 files)
autonomous: true
requirements: [CLN-01, CLN-04]
user_setup: []
must_haves:
  truths:
    - "Test files use interface terminology"
    - "Test functions renamed appropriately"
    - "Static test constants renamed"
    - "All tests pass"
  artifacts:
    - path: "crates/polyplug/tests/*.rs"
      provides: "Integration and stress tests"
  key_links:
    - from: "test files"
      to: "test assertions"
      via: "function calls and statics"
      pattern: "INTERFACE_V1, INTERFACE_V2"
---

<objective>
Update all test files in crates/polyplug/tests/ to use interface terminology. Rename test helper functions, static constants, and variables.

Purpose: Ensure test suite uses consistent interface terminology throughout.
Output: 28 test files updated, all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-final-cleanup/15-RESEARCH.md

<interfaces>
<!-- From RESEARCH.md - test file patterns -->

Key patterns to replace:
- `init_*_vtable()` functions → `init_*_interface()`
- `VTABLE_V1`, `VTABLE_V2` statics → `INTERFACE_V1`, `INTERFACE_V2`
- `vtable_ptr` variables → `interface_ptr`
- Comments: "Static vtables" → "Static interfaces"
- Error messages: "vtable must be resolvable" → "interface must be resolvable"
- Comments: "vtable pointer" → "interface pointer"

Files with most occurrences:
- hot_reload_safety.rs: Static vtables, function names
- integration_codegen_cpp.rs: Variable names, comments
- ffi_edge_cases.rs: Variable names
- stress_concurrent_registry.rs: Statics
- registry_edge_cases.rs: Statics
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename static vtable constants to interface in test files</name>
  <files>crates/polyplug/tests/hot_reload_safety.rs, crates/polyplug/tests/stress_concurrent_registry.rs, crates/polyplug/tests/registry_edge_cases.rs</files>
  <read_first>
    - crates/polyplug/tests/hot_reload_safety.rs (current state)
    - crates/polyplug/tests/stress_concurrent_registry.rs (current state)
    - crates/polyplug/tests/registry_edge_cases.rs (current state)
  </read_first>
  <behavior>
    - `VTABLE_V1` → `INTERFACE_V1`
    - `VTABLE_V2` → `INTERFACE_V2`
    - Comments updated: "Static vtables" → "Static interfaces"
    - All references updated
  </behavior>
  <action>
    1. Read hot_reload_safety.rs and identify static constant definitions (line 21 comment).
    2. Rename:
       - Comment at line 21: "Static vtables for testing" → "Static interfaces for testing"
       - Test function at line 83: `test_swap_interface_changes_vtable` → `test_swap_interface_changes_interface_pointer`
       - Variables `vtable_ptr_before`, `vtable_ptr_after` → `interface_ptr_before`, `interface_ptr_after`
       - SAFETY comments: "vtable pointer" → "interface pointer"
    3. Read stress_concurrent_registry.rs and registry_edge_cases.rs.
    4. Rename any `VTABLE_V*` static constants to `INTERFACE_V*`.
    5. Update all references to renamed constants.
  </action>
  <verify>
    <automated>grep -rn "VTABLE_V\|Static vtables" crates/polyplug/tests/*.rs | wc -l</automated>
    Expected: 0
  </verify>
  <done>Static vtable constants renamed to interface. Test files updated.</done>
</task>

<task type="auto">
  <name>Task 2: Update integration_codegen_cpp.rs variable names and comments</name>
  <files>crates/polyplug/tests/integration_codegen_cpp.rs</files>
  <read_first>
    - crates/polyplug/tests/integration_codegen_cpp.rs (current state)
  </read_first>
  <behavior>
    - Variable names: `vtable_ptr` → `interface_ptr`, `vtable` → `interface`
    - Comments updated
    - Error messages: "vtable must be resolvable" → "interface must be resolvable"
    - Test function comment at line 5: "vtable when TEST_PLUGIN_CPP_SO" → "interface when..."
  </behavior>
  <action>
    1. Read integration_codegen_cpp.rs.
    2. Update:
       - Line 5: Comment "C++ test plugin vtable" → "C++ test plugin interface"
       - Line 61: Comment "stores vtable entries" → "stores interface entries"
       - Line 280: Comment "g++ compile of vtables.hpp" → "g++ compile of interfaces.hpp"
       - Line 373: Comment "Look up vtable" → "Look up interface"
       - Lines 380-395: Variable `vtable_ptr` → `interface_ptr`, `vtable` → `interface`
       - Lines 481-495, 579-589: Similar variable renames
       - Error messages: "vtable must be resolvable" → "interface must be resolvable"
       - SAFETY comments: "vtable_ptr is valid" → "interface_ptr is valid"
    3. Run C++ codegen tests to verify:
       ```bash
       cargo test -p polyplug integration_codegen_cpp -q
       ```
  </action>
  <verify>
    <automated>grep -n "vtable_ptr\|let vtable\|vtable must be resolvable" crates/polyplug/tests/integration_codegen_cpp.rs | wc -l</automated>
    Expected: 0
  </verify>
  <done>integration_codegen_cpp.rs uses interface terminology. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Update remaining test files variable names and comments</name>
  <files>crates/polyplug/tests/ffi_edge_cases.rs, crates/polyplug/tests/*.rs (remaining 25 files)</files>
  <read_first>
    - crates/polyplug/tests/ffi_edge_cases.rs (current state)
  </read_first>
  <behavior>
    - Variable names updated
    - Comments updated
    - All tests pass
  </behavior>
  <action>
    1. Update ffi_edge_cases.rs:
       - Lines 37-112: Variables `vtable` → `interface`
       - Comments updated
    2. Scan remaining test files for vtable occurrences:
       ```bash
       grep -rn "vtable" crates/polyplug/tests/*.rs | grep -v "vtable_version"
       ```
    3. Update each occurrence:
       - Variable names: `vtable` → `interface`
       - Comments: "vtable" → "interface"
       - Preserve SDK function names like `store_host_vtable` if they're FFI imports
    4. Run full test suite:
       ```bash
       cargo test -p polyplug -q
       ```
  </action>
  <verify>
    <automated>grep -rn "vtable" crates/polyplug/tests/*.rs | grep -v "vtable_version" | grep -v "store_host_vtable" | grep -v "get_host_vtable" | wc -l</automated>
    Expected: 0
  </verify>
  <done>All test files use interface terminology. Full test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test constants → Test assertions | Rename must propagate to all references |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Tampering | Static renames | mitigate | Run affected tests after each file |
| T-15-04-02 | Denial | Test failures | mitigate | Incremental testing with cargo test -p polyplug |
</threat_model>

<verification>
Run `cargo test -p polyplug -q` to verify all polyplug tests pass after changes.
</verification>

<success_criteria>
- All 28 test files use interface terminology
- Static constants renamed: VTABLE_V* → INTERFACE_V*
- Variable names renamed: vtable_ptr → interface_ptr
- Comments updated: "vtable" → "interface" where appropriate
- Full polyplug test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-final-cleanup/15-04-SUMMARY.md`
</output>