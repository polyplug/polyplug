---
phase: 19
plan: 04
type: execute
wave: 4
depends_on: ["19-02", "19-03"]
files_modified:
  - sdks/python/host/polyplug/runtime.py
  - sdks/csharp/host/NativeMethods.cs
  - sdks/lua/host/polyplug/runtime.lua
  - sdks/js/host/polyplug/mod.js
  - sdks/cpp/host/polyplug/runtime.hpp
  - sdks/python/polyplug_abi/polyplug_abi/abi.py
  - sdks/lua/abi/polyplug_abi.lua
  - sdks/js/abi/polyplug_abi.ts
autonomous: true
requirements: [D-10, D-11, D-26, D-27, D-28, D-34]

must_haves:
  truths:
    - "No hand-written FFI struct definitions remain in any SDK host file"
    - "All SDK host files import struct types from auto-generated abi.* files"
    - "Python polyplug_abi shared package imports from auto-generated abi.py"
    - "All SDK host files compile/work with the imported types"
    - "No broken placeholder polyplug_abi files remain"
  artifacts:
    - path: "sdks/python/host/polyplug/runtime.py"
      provides: "Python host runtime importing from abi.py"
    - path: "sdks/csharp/host/NativeMethods.cs"
      provides: "C# host runtime importing from Abi.cs"
    - path: "sdks/lua/host/polyplug/runtime.lua"
      provides: "Lua host runtime importing from abi.lua"
    - path: "sdks/js/host/polyplug/mod.js"
      provides: "JS host runtime importing from abi.ts"
    - path: "sdks/cpp/host/polyplug/runtime.hpp"
      provides: "C++ host runtime importing from abi.hpp"
  key_links:
    - from: "sdks/python/host/polyplug/runtime.py"
      to: "sdks/python/abi/abi.py"
      via: "Python import statement"
      pattern: "from.*abi import|import.*abi"
    - from: "sdks/csharp/host/NativeMethods.cs"
      to: "sdks/csharp/abi/Abi.cs"
      via: "C# using statement"
      pattern: "using.*Polyplug.Abi"
    - from: "sdks/lua/host/polyplug/runtime.lua"
      to: "sdks/lua/abi/abi.lua"
      via: "Lua require"
      pattern: "require.*abi"
    - from: "sdks/js/host/polyplug/mod.js"
      to: "sdks/js/abi/abi.ts"
      via: "JS import"
      pattern: "import.*abi"
    - from: "sdks/cpp/host/polyplug/runtime.hpp"
      to: "sdks/cpp/abi/polyplug/abi.hpp"
      via: "C++ #include"
      pattern: '#include.*abi'
---

<objective>
Delete all hand-written FFI struct definitions from SDK host files and replace them with imports from auto-generated abi.* files.

Purpose: The SDK host files contain hand-written struct definitions that are often WRONG (wrong sizes, stale fields like `generation` in GuestContractHandle, missing `function_count` in NativeDispatch). These must be replaced by imports from the auto-generated canonical source.

Output: Clean SDK host files that import types from auto-generated abi.* files with no hand-written struct definitions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-fix-abi-build-script-extractor-rs-to-auto-generate-sdk-struc/19-CONTEXT.md
@.planning/phases/19-fix-abi-build-script-extractor-rs-to-auto-generate-sdk-struc/19-RESEARCH.md
@.planning/phases/19-fix-abi-build-script-extractor-rs-to-auto-generate-sdk-struc/19-01-SUMMARY.md
@.planning/phases/19-fix-abi-build-script-extractor-rs-to-auto-generate-sdk-struc/19-02-SUMMARY.md
@.planning/phases/19-fix-abi-build-script-extractor-rs-to-auto-generate-sdk-struc/19-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete hand-written FFI structs from Python and C# SDK host files</name>
  <files>
    sdks/python/host/polyplug/runtime.py
    sdks/python/polyplug_abi/polyplug_abi/abi.py
    sdks/csharp/host/NativeMethods.cs
  </files>
  <read_first>
    sdks/python/host/polyplug/runtime.py
    sdks/python/polyplug_abi/polyplug_abi/abi.py
    sdks/csharp/host/NativeMethods.cs
    sdks/python/abi/abi.py
    sdks/csharp/abi/Abi.cs
  </read_first>
  <acceptance_criteria>
    - sdks/python/host/polyplug/runtime.py has no ctypes.Structure subclass definitions
    - sdks/python/host/polyplug/runtime.py imports types from the auto-generated abi module
    - sdks/python/polyplug_abi/polyplug_abi/abi.py imports from sdks/python/abi/abi.py (per D-28)
    - sdks/csharp/host/NativeMethods.cs has no [StructLayout] struct definitions
    - sdks/csharp/host/NativeMethods.cs imports types from Polyplug.Abi namespace
    - grep -c '_fields_' sdks/python/host/polyplug/runtime.py returns 0
    - grep -c 'StructLayout' sdks/csharp/host/NativeMethods.cs returns 0
    - Both files still function correctly (Runtime class still instantiable, methods still work)
  </acceptance_criteria>
  <action>
    Per D-26, D-27, D-28:

    1. PYTHON - sdks/python/host/polyplug/runtime.py:
       - READ the full file first.
       - DELETE all `ctypes.Structure` subclass definitions (RuntimeConfig, HostInterface, HostContractInterface, etc. -- any class with `_fields_`).
       - DELETE all `ctypes.CFUNCTYPE` typedefs (these are now in auto-generated abi.py).
       - ADD import at top: `from ...abi.abi import *` or specific imports for each type used (StringView, RuntimeConfig, HostInterface, GuestContractHandle, etc.).
       - Ensure the Runtime class still works: it uses the imported types for struct field access and FFI calls.
       - Per D-22: The old RuntimeConfig with 24 bytes (retry fields) is GONE. The imported one is 16 bytes (3 fields). If the Runtime class references old fields like `max_retry_count`, remove those references.
       - Per D-23: The old GuestContractHandle with `generation` field is GONE. The imported one has only `index: u32`. Remove any `generation` field references.

    2. PYTHON - sdks/python/polyplug_abi/polyplug_abi/abi.py:
       - Per D-28: This shared package file should import from the auto-generated `sdks/python/abi/abi.py`.
       - Replace its contents with: `from polyplug.abi.abi import *` (or appropriate relative import).
       - Or if the shared package structure requires a different import path, adjust accordingly.

    3. C# - sdks/csharp/host/NativeMethods.cs:
       - READ the full file first.
       - DELETE all `[StructLayout]` struct definitions (StringViewC, ReloadPhaseFfi, RuntimeConfig, RuntimeCreateOptions, HostInterface, etc.).
       - DELETE all `[UnmanagedFunctionPointer]` delegate definitions (now in auto-generated Abi.cs).
       - ADD using directive: `using Polyplug.Abi;` (or appropriate namespace from auto-generated Abi.cs).
       - Ensure the NativeMethods class still works with imported types.
       - Per D-22: Remove any references to old RuntimeConfig fields that don't exist in the new 16-byte version.
       - Per D-23: Remove any references to GuestContractHandle.generation.
       - Per D-24: Ensure NativeDispatch now has function_count field available.
  </action>
  <verify>
    <automated>grep -c '_fields_' sdks/python/host/polyplug/runtime.py && grep -c 'StructLayout' sdks/csharp/host/NativeMethods.cs</automated>
  </verify>
  <done>
    - Python runtime.py has zero ctypes.Structure subclasses
    - Python runtime.py imports from auto-generated abi module
    - Python shared package abi.py imports from auto-generated file
    - C# NativeMethods.cs has zero [StructLayout] structs
    - C# NativeMethods.cs imports from Polyplug.Abi namespace
    - Both files compile without errors
    - No stale field references (generation, max_retry_count, etc.)
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete hand-written FFI structs from Lua, JS, and C++ SDK host files</name>
  <files>
    sdks/lua/host/polyplug/runtime.lua
    sdks/lua/abi/polyplug_abi.lua
    sdks/js/host/polyplug/mod.js
    sdks/js/abi/polyplug_abi.ts
    sdks/cpp/host/polyplug/runtime.hpp
  </files>
  <read_first>
    sdks/lua/host/polyplug/runtime.lua
    sdks/lua/abi/polyplug_abi.lua
    sdks/js/host/polyplug/mod.js
    sdks/js/abi/polyplug_abi.ts
    sdks/cpp/host/polyplug/runtime.hpp
    sdks/lua/abi/abi.lua
    sdks/js/abi/abi.ts
    sdks/cpp/abi/polyplug/abi.hpp
  </read_first>
  <acceptance_criteria>
    - sdks/lua/host/polyplug/runtime.lua has no ffi.cdef struct definitions (imports from abi.lua)
    - sdks/lua/abi/polyplug_abi.lua replaced with imports from abi.lua
    - sdks/js/host/polyplug/mod.js has no hand-written struct offset constants (imports from abi.ts)
    - sdks/js/abi/polyplug_abi.ts replaced with imports from abi.ts
    - sdks/cpp/host/polyplug/runtime.hpp has no hand-written struct definitions (includes abi.hpp)
    - grep -c 'ffi.cdef' sdks/lua/host/polyplug/runtime.lua returns 0
    - grep -c 'HOST_INTERFACE_OFFSETS' sdks/js/host/polyplug/mod.js returns 0 (moved to abi.ts)
    - grep -c 'struct HostInterface' sdks/cpp/host/polyplug/runtime.hpp returns 0
  </acceptance_criteria>
  <action>
    Per D-26, D-27, D-28, D-34:

    1. LUA - sdks/lua/host/polyplug/runtime.lua:
       - READ the full file first.
       - DELETE all `ffi.cdef([[...]])` blocks that define struct types (HostInterface, HostContractInterface, RuntimeConfig, ResolveHandle, etc.).
       - These types are now in the auto-generated abi.lua which uses ffi.cdef.
       - ADD require at top: `local abi = require("polyplug.abi")` or the appropriate module path.
       - Ensure Runtime class still works with imported types.
       - Per D-25: HostContractInterface is now a flat struct (not header+dispatch decomposition). Update any code that assumed the old two-part structure.

    2. LUA - sdks/lua/abi/polyplug_abi.lua:
       - Per D-28: Replace with a simple re-export from abi.lua.
       - Content: `local abi = require("polyplug.abi")` followed by re-exports.

    3. JS - sdks/js/host/polyplug/mod.js:
       - READ the full file first.
       - DELETE the hand-written `HOST_INTERFACE_OFFSETS` constant object (now in auto-generated abi.ts as offset constants).
       - DELETE any hand-written struct field offset computations.
       - ADD import: `import { HOST_INTERFACE_OFFSETS, ... } from "../abi/abi.ts"` or appropriate path.
       - Per D-34: Deno-only host. No QuickJS guest concerns.
       - Ensure Runtime class still works with imported offsets and types.

    4. JS - sdks/js/abi/polyplug_abi.ts:
       - Per D-28: Replace with re-exports from abi.ts.
       - Content: `export * from "./abi.ts"` or specific re-exports.

    5. C++ - sdks/cpp/host/polyplug/runtime.hpp:
       - READ the full file first.
       - DELETE hand-written struct definitions (HostInterface, RuntimeConfig, ArrayResult, ResolveHandle forward declaration, etc.).
       - ADD include: `#include "polyplug/abi.hpp"` (using the subdirectory path per D-13).
       - Ensure the Runtime class and Builder still work with imported types.
       - Per D-23: Remove any `generation` field references from GuestContractHandle usage.
       - Per D-24: Ensure NativeDispatch has function_count field.

    For ALL files: After removing hand-written structs, verify that any remaining code (Runtime class methods, helper functions) still references the correct type names. The auto-generated types use the same names (StringView, RuntimeConfig, HostInterface, etc.) so most references should work as-is after adding the import.
  </action>
  <verify>
    <automated>grep -c 'ffi.cdef' sdks/lua/host/polyplug/runtime.lua && grep -c 'struct HostInterface' sdks/cpp/host/polyplug/runtime.hpp && grep -c '#include.*abi' sdks/cpp/host/polyplug/runtime.hpp</automated>
  </verify>
  <done>
    - Lua runtime.lua has zero ffi.cdef blocks with struct definitions
    - Lua polyplug_abi.lua re-exports from abi.lua
    - JS mod.js imports offset constants from abi.ts
    - JS polyplug_abi.ts re-exports from abi.ts
    - C++ runtime.hpp includes abi.hpp instead of defining structs
    - C++ runtime.hpp has zero hand-written struct definitions
    - All 5 SDK host files import from auto-generated abi.* files
    - No stale field references anywhere
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK host files -> abi.* imports | Runtime code depends on generated types |
| polyplug_abi shared packages -> abi.* | Re-export layer for package conventions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09 | Denial | Missing import paths | mitigate | Build/CI will catch import errors at test time |
| T-19-10 | Tampering | Stale field references | mitigate | Size assertions in generated code catch layout mismatches at runtime |
</threat_model>

<verification>
1. `grep -c '_fields_' sdks/python/host/polyplug/runtime.py` returns 0
2. `grep -c 'StructLayout' sdks/csharp/host/NativeMethods.cs` returns 0
3. `grep -c 'ffi.cdef' sdks/lua/host/polyplug/runtime.lua` returns 0
4. `grep -c 'struct HostInterface' sdks/cpp/host/polyplug/runtime.hpp` returns 0
5. Each host file contains an import/include from its respective abi.* file
6. `grep -r 'generation' sdks/*/host/` returns 0 (stale field removed)
7. `cargo build -p polyplug_abi` succeeds
</verification>

<success_criteria>
- Zero hand-written FFI struct definitions in any SDK host file
- All 5 SDK host files import from auto-generated abi.* files
- Python shared package re-exports from auto-generated abi.py
- No stale field references (generation, max_retry_count, etc.)
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/19-fix-abi-build-script-extractor-rs-to-auto-generate-sdk-struc/19-04-SUMMARY.md`
</output>
