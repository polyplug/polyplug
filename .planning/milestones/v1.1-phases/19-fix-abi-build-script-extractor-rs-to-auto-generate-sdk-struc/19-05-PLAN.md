---
phase: 19
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - sdks/cpp/guest/polyplug/guest.hpp
  - sdks/js/guest/polyplug_guest.js
  - sdks/rust/guest/src/lib.rs
  - sdks/rust/guest/README.md
  - sdks/cpp/README.md
  - sdks/js/README.md
  - sdks/lua/README.md
  - sdks/python/README.md
  - sdks/csharp/README.md
  - docs/ABI_ARCHITECTURE.md
  - docs/abi_types.md
  - PRD.md
  - sdks/python/guest/polyplug_guest.py
  - sdks/csharp/guest/Polyplug.Guest.cs
  - sdks/lua/guest/polyplug_guest.lua
  - tests/fixtures/test_plugin.py
  - tests/fixtures/test_plugin.lua
  - tests/fixtures/test_plugin_lua/test_plugin.lua
autonomous: true
requirements: [D-29, D-30]

must_haves:
  truths:
    - "grep -r PluginRegistrar returns zero matches across entire codebase (excluding .planning/ and .git/)"
    - "All guest SDKs use HostInterface* in polyplug_init() signatures instead of PluginRegistrar*"
    - "All SDK READMEs reference HostInterface instead of PluginRegistrar"
    - "Test fixtures use HostInterface instead of PluginRegistrar"
    - "Documentation references HostInterface instead of PluginRegistrar"
  artifacts:
    - path: "sdks/cpp/guest/polyplug/guest.hpp"
      provides: "C++ guest SDK without PluginRegistrar"
    - path: "sdks/js/guest/polyplug_guest.js"
      provides: "JS guest SDK without PluginRegistrar"
    - path: "sdks/rust/guest/src/lib.rs"
      provides: "Rust guest SDK without PluginRegistrar alias"
    - path: "docs/ABI_ARCHITECTURE.md"
      provides: "Architecture docs referencing HostInterface"
    - path: "docs/abi_types.md"
      provides: "ABI type docs referencing HostInterface"
    - path: "tests/fixtures/test_plugin.py"
      provides: "Python test fixture without PluginRegistrar"
    - path: "tests/fixtures/test_plugin.lua"
      provides: "Lua test fixture without PluginRegistrar"
  key_links:
    - from: "sdks/*/guest/*"
      to: "HostInterface"
      via: "polyplug_init signature"
      pattern: "polyplug_init.*HostInterface"
---

<objective>
Remove the PluginRegistrar type from all guest SDKs, documentation, READMEs, and test fixtures, replacing with HostInterface.

Purpose: PluginRegistrar was just an alias for HostInterface with no semantic value. D-29 mandates its complete removal from all guest SDKs and docs. D-30 requires verification that zero references remain across the ENTIRE codebase (not just a subset of directories).

Output: Clean codebase with no PluginRegistrar references anywhere.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-fix-abi-build-script-extractor-rs-to-auto-generate-sdk-struc/19-CONTEXT.md
@.planning/phases/19-fix-abi-build-script-extractor-rs-to-auto-generate-sdk-struc/19-RESEARCH.md

<interfaces>
PluginRegistrar references found (from grep across entire codebase):

Guest SDK files:
- sdks/cpp/guest/polyplug/guest.hpp:166,168 -- POLYPLUG_GUEST_MAIN macro uses PluginRegistrar*
- sdks/js/guest/polyplug_guest.js:115,131 -- @typedef PluginRegistrar, @param {PluginRegistrar}
- sdks/rust/guest/src/lib.rs:181 -- doc comment references PluginRegistrar::host

SDK READMEs (6 files):
- sdks/rust/guest/README.md:85,90,225,231,233,462,467 -- PluginRegistrar in examples and type docs
- sdks/cpp/README.md:106 -- PluginRegistrar listed in feature table
- sdks/js/README.md:101 -- PluginRegistrar listed in feature table
- sdks/lua/README.md:95 -- PluginRegistrar listed in feature table
- sdks/python/README.md:48,51,94 -- PluginRegistrar in examples and feature table
- sdks/csharp/README.md:61,112 -- PluginRegistrar in examples and feature table

Documentation:
- docs/abi_types.md:226 -- polyplug_init(registrar: *mut PluginRegistrar)
- docs/ABI_ARCHITECTURE.md:29,173 -- AbiError polyplug_init(PluginRegistrar* registrar, ...)
- PRD.md:314,319,324,331,574 -- PluginRegistrar struct definition and usage

Test fixtures (3 files):
- tests/fixtures/test_plugin.py:19,148,149 -- PluginRegistrar import and usage
- tests/fixtures/test_plugin.lua:38 -- PluginRegistrar in comment
- tests/fixtures/test_plugin_lua/test_plugin.lua:38 -- PluginRegistrar in comment

Additional SDK guest files:
- sdks/python/guest/polyplug_guest.py -- check for PluginRegistrar references
- sdks/csharp/guest/Polyplug.Guest.cs -- check for PluginRegistrar references
- sdks/lua/guest/polyplug_guest.lua -- check for PluginRegistrar references
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove PluginRegistrar from guest SDK files and SDK READMEs</name>
  <files>
    sdks/cpp/guest/polyplug/guest.hpp
    sdks/js/guest/polyplug_guest.js
    sdks/rust/guest/src/lib.rs
    sdks/rust/guest/README.md
    sdks/cpp/README.md
    sdks/js/README.md
    sdks/lua/README.md
    sdks/python/README.md
    sdks/csharp/README.md
    sdks/python/guest/polyplug_guest.py
    sdks/csharp/guest/Polyplug.Guest.cs
    sdks/lua/guest/polyplug_guest.lua
  </files>
  <read_first>
    sdks/cpp/guest/polyplug/guest.hpp
    sdks/js/guest/polyplug_guest.js
    sdks/rust/guest/src/lib.rs
    sdks/rust/guest/README.md
    sdks/cpp/README.md
    sdks/js/README.md
    sdks/lua/README.md
    sdks/python/README.md
    sdks/csharp/README.md
    sdks/python/guest/polyplug_guest.py
    sdks/csharp/guest/Polyplug.Guest.cs
    sdks/lua/guest/polyplug_guest.lua
  </read_first>
  <action>
    Per D-29, D-30:

    For EVERY file listed above that contains "PluginRegistrar", replace with "HostInterface":

    1. sdks/cpp/guest/polyplug/guest.hpp:
       - Replace `PluginRegistrar* registrar` with `HostInterface* host` in the POLYPLUG_GUEST_MAIN macro (lines 166-168).
       - Replace any `PluginRegistrar` type usage with `HostInterface`.
       - Ensure the #include for abi.hpp is present (for HostInterface definition).

    2. sdks/js/guest/polyplug_guest.js:
       - Replace `@typedef {Object} PluginRegistrar` with `@typedef {Object} HostInterface` (line 115).
       - Replace `@param {PluginRegistrar} registrar` with `@param {HostInterface} host` (line 131).
       - Update any inline references to use `host` parameter name instead of `registrar`.

    3. sdks/rust/guest/src/lib.rs:
       - Replace `PluginRegistrar` references in doc comments with `HostInterface` (line 181).
       - If there is a type alias `pub type PluginRegistrar = HostInterface;`, delete it entirely.
       - Update any doc examples to use `HostInterface`.

    4. SDK README files (6 files):
       - sdks/rust/guest/README.md: Replace ALL `PluginRegistrar` references with `HostInterface` (lines 85, 90, 225, 231, 233, 462, 467). Update parameter names from `registrar` to `host` where appropriate.
       - sdks/cpp/README.md: Replace PluginRegistrar with HostInterface in feature table (line 106).
       - sdks/js/README.md: Replace PluginRegistrar with HostInterface in feature table (line 101).
       - sdks/lua/README.md: Replace PluginRegistrar with HostInterface in feature table (line 95).
       - sdks/python/README.md: Replace PluginRegistrar with HostInterface in examples and feature table (lines 48, 51, 94). Update parameter names from `registrar` to `host`.
       - sdks/csharp/README.md: Replace PluginRegistrar with HostInterface in examples and feature table (lines 61, 112). Update parameter names from `registrar` to `host`.

    5. Additional guest SDK files:
       - sdks/python/guest/polyplug_guest.py: Search and replace any PluginRegistrar references with HostInterface.
       - sdks/csharp/guest/Polyplug.Guest.cs: Search and replace any PluginRegistrar references with HostInterface.
       - sdks/lua/guest/polyplug_guest.lua: Search and replace any PluginRegistrar references with HostInterface.

    After all replacements, run: `grep -r "PluginRegistrar" sdks/ --include='*.py' --include='*.js' --include='*.ts' --include='*.cs' --include='*.lua' --include='*.hpp' --include='*.h' --include='*.rs' --include='*.md'` and verify ZERO matches.
  </action>
  <verify>
    <automated>grep -r "PluginRegistrar" sdks/ --include='*.py' --include='*.js' --include='*.ts' --include='*.cs' --include='*.lua' --include='*.hpp' --include='*.h' --include='*.rs' --include='*.md' 2>/dev/null; echo "EXIT: $?"</automated>
  </verify>
  <done>
    - Zero PluginRegistrar references in any file under sdks/
    - All guest SDKs use HostInterface in polyplug_init signatures
    - All 6 SDK READMEs reference HostInterface instead of PluginRegistrar
    - Parameter names updated from registrar to host where appropriate
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove PluginRegistrar from documentation, PRD, and test fixtures</name>
  <files>
    docs/ABI_ARCHITECTURE.md
    docs/abi_types.md
    PRD.md
    tests/fixtures/test_plugin.py
    tests/fixtures/test_plugin.lua
    tests/fixtures/test_plugin_lua/test_plugin.lua
  </files>
  <read_first>
    docs/ABI_ARCHITECTURE.md
    docs/abi_types.md
    PRD.md
    tests/fixtures/test_plugin.py
    tests/fixtures/test_plugin.lua
    tests/fixtures/test_plugin_lua/test_plugin.lua
  </read_first>
  <action>
    Per D-29, D-30:

    1. docs/abi_types.md:
       - Line 226: Replace `polyplug_init(registrar: *mut PluginRegistrar)` with `polyplug_init(host: *const HostInterface)`.
       - Search entire file for other PluginRegistrar references and replace.

    2. docs/ABI_ARCHITECTURE.md:
       - Line 29: Replace `AbiError polyplug_init(PluginRegistrar* registrar, const BundleInitContext* ctx)` with `AbiError polyplug_init(const HostInterface* host, const BundleInitContext* ctx)`.
       - Line 173: Replace `PluginRegistrar layout cannot change` with `HostInterface layout cannot change`.
       - Search entire file for other references.

    3. PRD.md:
       - Lines 314, 319, 324, 331, 574: Replace ALL PluginRegistrar references with HostInterface.
       - Update the struct definition if one exists in PRD.md.
       - Update parameter names from `registrar` to `host`.

    4. tests/fixtures/test_plugin.py:
       - Line 19: Replace `PluginRegistrar` import with `HostInterface`.
       - Line 148: Replace `PluginRegistrar` in docstring with `HostInterface`.
       - Line 149: Replace `PluginRegistrar.from_address(registrar_addr)` with `HostInterface.from_address(host_addr)` and update variable name from `registrar` to `host`.

    5. tests/fixtures/test_plugin.lua:
       - Line 38: Replace `PluginRegistrar` in comment with `HostInterface`.

    6. tests/fixtures/test_plugin_lua/test_plugin.lua:
       - Line 38: Replace `PluginRegistrar` in comment with `HostInterface`.

    After all replacements, run a comprehensive codebase-wide search: `grep -r "PluginRegistrar" --exclude-dir=.git --exclude-dir=.planning --exclude-dir=target .` and verify ZERO matches.
  </action>
  <verify>
    <automated>grep -r "PluginRegistrar" --exclude-dir=.git --exclude-dir=.planning --exclude-dir=target . 2>/dev/null; echo "EXIT: $?"</automated>
  </verify>
  <done>
    - Zero PluginRegistrar references in documentation (docs/)
    - Zero PluginRegistrar references in PRD.md
    - Zero PluginRegistrar references in test fixtures (tests/)
    - grep -r "PluginRegistrar" across entire codebase (excluding .git, .planning, target) returns zero matches
    - All test fixtures use HostInterface instead of PluginRegistrar
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guest SDKs -> HostInterface | Plugin init receives HostInterface pointer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-11 | Tampering | Type rename consistency | mitigate | Codebase-wide grep verification ensures no stale references remain in any file |
</threat_model>

<verification>
1. `grep -r "PluginRegistrar" --exclude-dir=.git --exclude-dir=.planning --exclude-dir=target .` returns zero matches (exit code 1)
2. `grep -r "HostInterface" sdks/cpp/guest/polyplug/guest.hpp` finds HostInterface
3. `grep -r "HostInterface" sdks/js/guest/polyplug_guest.js` finds HostInterface
4. `grep -r "HostInterface" docs/ABI_ARCHITECTURE.md` finds HostInterface
5. `grep -r "HostInterface" tests/fixtures/test_plugin.py` finds HostInterface
6. `cargo build -p polyplug_abi` succeeds
7. `cargo test -p polyplug_abi --lib` passes
</verification>

<success_criteria>
- Zero PluginRegistrar references anywhere in codebase (excluding .git, .planning, target)
- All guest SDKs use HostInterface in polyplug_init signatures
- All 6 SDK READMEs updated to use HostInterface terminology
- All test fixtures updated to use HostInterface
- All documentation updated to use HostInterface terminology
- Build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-fix-abi-build-script-extractor-rs-to-auto-generate-sdk-struc/19-05-SUMMARY.md`
</output>
