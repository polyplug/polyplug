---
phase: 01-abi-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polyplug_utils/src/plugin_contract_id.rs
  - crates/polyplug_utils/src/lib.rs
autonomous: true
requirements: [ABI-11]
user_setup: []
must_haves:
  truths:
    - "GuestContractId type exists in polyplug_utils"
    - "GuestContractId uses 'guest_contract:' hash prefix (consistent naming)"
    - "polyplug_utils exports GuestContractId (not PluginContractId)"
  artifacts:
    - path: "crates/polyplug_utils/src/guest_contract_id.rs"
      provides: "GuestContractId type definition"
      contains: "pub struct GuestContractId(u64)"
    - path: "crates/polyplug_utils/src/lib.rs"
      provides: "Crate exports"
      exports: ["GuestContractId"]
  key_links:
    - from: "crates/polyplug_utils/src/lib.rs"
      to: "guest_contract_id module"
      via: "pub use"
      pattern: "pub use guest_contract_id::GuestContractId"
---

<objective>
Rename PluginContractId to GuestContractId in polyplug_utils crate.

Purpose: Establish consistent Guest/Host terminology per user decision D-07.
Output: GuestContractId type with "guest_contract:" hash prefix for naming consistency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key contracts executor needs. Extracted from codebase. -->

From crates/polyplug_utils/src/lib.rs:
```rust
pub use plugin_contract_id::PluginContractId;
```

From crates/polyplug_utils/src/plugin_contract_id.rs:
```rust
#[repr(transparent)]
#[derive(Debug, Clone, Copy, Hash, PartialEq, Eq, PartialOrd, Ord)]
pub struct PluginContractId(u64);

impl PluginContractId {
    pub fn new(name: &str, major_version: u32) -> Self {
        Self(contract_id("plugin_contract:", name, major_version))
    }
    pub fn id(&self) -> u64 { self.0 }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename plugin_contract_id.rs to guest_contract_id.rs</name>
  <files>crates/polyplug_utils/src/guest_contract_id.rs</files>
  <read_first>
    - crates/polyplug_utils/src/plugin_contract_id.rs (current implementation)
    - crates/polyplug_utils/src/lib.rs (current exports)
  </read_first>
  <action>
    1. Rename file: `plugin_contract_id.rs` -> `guest_contract_id.rs`
    2. Rename struct: `PluginContractId` -> `GuestContractId`
    3. Change hash prefix from `"plugin_contract:"` to `"guest_contract:"` for naming consistency
    4. Update doc comments to use "guest contract" terminology
    5. Update all test names and assertions to use `GuestContractId`

    Concrete changes:
    - Line 4: `pub struct PluginContractId(u64)` -> `pub struct GuestContractId(u64)`
    - Line 7: `impl PluginContractId` -> `impl GuestContractId`
    - Line 8: `Self(contract_id("plugin_contract:", name, major_version))` -> `Self(contract_id("guest_contract:", name, major_version))`
    - Line 20: `impl PluginContractId` -> `impl GuestContractId`
    - Doc example: `use polyplug_utils::PluginContractId` -> `use polyplug_utils::GuestContractId`
    - Test assertions: All `PluginContractId::new` -> `GuestContractId::new`

    NOTE: Hash prefix changes from "plugin_contract:" to "guest_contract:" per user decision.
    This is a BREAKING CHANGE - existing contract IDs will have different hash values.
  </action>
  <verify>
    <automated>grep -q "pub struct GuestContractId" crates/polyplug_utils/src/guest_contract_id.rs && grep -q '"guest_contract:"' crates/polyplug_utils/src/guest_contract_id.rs</automated>
  </verify>
  <done>
    - File renamed to guest_contract_id.rs
    - Struct renamed to GuestContractId
    - Hash prefix changed to "guest_contract:" (consistent naming)
    - All tests use GuestContractId naming
  </done>
  <acceptance_criteria>
    - `crates/polyplug_utils/src/guest_contract_id.rs` exists
    - `crates/polyplug_utils/src/plugin_contract_id.rs` does NOT exist
    - File contains `pub struct GuestContractId(u64)`
    - File contains `impl GuestContractId`
    - File contains `"guest_contract:"` (NEW prefix)
    - File contains `fn guest_contract_id_format` test
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update polyplug_utils lib.rs exports</name>
  <files>crates/polyplug_utils/src/lib.rs</files>
  <read_first>
    - crates/polyplug_utils/src/lib.rs (current exports)
    - crates/polyplug_utils/src/guest_contract_id.rs (renamed file from Task 1)
  </read_first>
  <action>
    Update lib.rs to export GuestContractId instead of PluginContractId:

    Concrete changes:
    - Line 6: `mod plugin_contract_id;` -> `mod guest_contract_id;`
    - Line 10: `pub use plugin_contract_id::PluginContractId;` -> `pub use guest_contract_id::GuestContractId;`
    - Add type alias for transition: `pub type PluginContractId = GuestContractId;` (deprecated, for migration)

    Update test imports (line 48):
    - `use crate::{PluginContractId, ...}` -> `use crate::{GuestContractId, ...}`
    - Test assertions use GuestContractId

    Keep HostContractId and BundleId unchanged.
  </action>
  <verify>
    <automated>grep -q "pub use guest_contract_id::GuestContractId" crates/polyplug_utils/src/lib.rs && grep -q "pub type PluginContractId = GuestContractId" crates/polyplug_utils/src/lib.rs</automated>
  </verify>
  <done>
    - lib.rs exports GuestContractId
    - Deprecation alias PluginContractId = GuestContractId added for migration
    - Module reference updated to guest_contract_id
  </done>
  <acceptance_criteria>
    - `crates/polyplug_utils/src/lib.rs` contains `mod guest_contract_id;`
    - `crates/polyplug_utils/src/lib.rs` contains `pub use guest_contract_id::GuestContractId;`
    - `crates/polyplug_utils/src/lib.rs` contains `pub type PluginContractId = GuestContractId;`
    - `crates/polyplug_utils/src/lib.rs` still exports `HostContractId` and `BundleId`
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run polyplug_utils crate tests:
```bash
cargo test -p polyplug_utils
```

All tests must pass with GuestContractId naming.
</verification>

<success_criteria>
- GuestContractId type exists in polyplug_utils/src/guest_contract_id.rs
- Hash prefix "guest_contract:" used (consistent naming)
- polyplug_utils exports GuestContractId
- Deprecation alias PluginContractId = GuestContractId available for transition
- All polyplug_utils tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-abi-types/01-01-SUMMARY.md`
</output>