---
phase: 01-abi-types
plan: 09
type: execute
wave: 7
depends_on: [01-05, 01-06, 01-07, 01-08]
files_modified: [tests/fixtures/test_plugin/src/lib.rs, tests/fixtures/error_plugin/src/lib.rs, tests/fixtures/reload_plugin_v2/src/lib.rs]
autonomous: true
gap_closure: true
requirements: [ABI-12, ABI-13]  # ABI-12: repr(C) types, ABI-13: GuestContractInstance

must_haves:
  truths:
    - "Test fixtures use AbiErrorCode enum directly without u32 cast"
    - "Test fixtures use exported helper functions from polyplug_abi"
    - "Workspace compiles (cargo build --workspace)"
  artifacts:
    - path: "tests/fixtures/test_plugin/src/lib.rs"
      provides: "Test plugin fixture"
      pattern: "AbiErrorCode::Ok" NOT "AbiErrorCode::Ok as u32"
    - path: "tests/fixtures/error_plugin/src/lib.rs"
      provides: "Error plugin fixture"
      pattern: "AbiErrorCode::Generic" NOT "as u32"
    - path: "tests/fixtures/reload_plugin_v2/src/lib.rs"
      provides: "Reload plugin V2 fixture"
      pattern: "AbiErrorCode::Generic" NOT "as u32"
  key_links:
    - from: "test fixtures"
      to: "polyplug_abi::abi_error_ok"
      via: "function call"
      pattern: "abi_error_ok\\(\\)"
---

<objective>
Fix test fixture AbiError.code type usage and verify workspace compiles.

Purpose: Test fixtures incorrectly cast AbiErrorCode to u32 when AbiError.code is now AbiErrorCode enum type. Depends on plans 05-08 for all prerequisites.

Output: Fixed fixtures, workspace compiles - phase 01 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-abi-types/01-VERIFICATION.md
@.planning/phases/01-abi-types/01-05-PLAN.md
@.planning/phases/01-abi-types/01-06-PLAN.md
@.planning/phases/01-abi-types/01-07-PLAN.md
@.planning/phases/01-abi-types/01-08-PLAN.md

<interfaces>
<!-- Current fixture code with type mismatch -->

From tests/fixtures/test_plugin/src/lib.rs:
```rust
// Line 41 - incorrect u32 cast
AbiError {
    code: AbiErrorCode::Generic as u32,  // WRONG: code field is AbiErrorCode, not u32
    message: string_view_null(),
}
```

Expected fixture code:
```rust
AbiError {
    code: AbiErrorCode::Generic,  // CORRECT: no cast needed
    message: StringView::null(),  // OR keep string_view_null() if exported
}
```

From crates/polyplug_abi/src/types/abi_error.rs:
```rust
#[repr(C)]
pub struct AbiError {
    pub code: AbiErrorCode,  // <-- THIS IS THE ENUM TYPE, not u32!
    pub message: StringView,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix test_plugin AbiError.code type usage</name>
  <files>tests/fixtures/test_plugin/src/lib.rs</files>
  <read_first>
    - tests/fixtures/test_plugin/src/lib.rs (current state)
    - crates/polyplug_abi/src/types/abi_error.rs (AbiError struct with AbiErrorCode.code field)
    - crates/polyplug_abi/src/types/mod.rs (helper functions if plan 05 completed)
  </read_first>
  <action>
    Fix test_plugin fixture to use AbiErrorCode enum directly without u32 cast.

    Change line 110-113 (in polyplug_init function):
    ```rust
    // BEFORE (wrong):
    return AbiError {
        code: AbiErrorCode::Generic as u32,  // WRONG CAST
        message: string_view_null(),
    };

    // AFTER (correct):
    return AbiError {
        code: AbiErrorCode::Generic,  // NO CAST - AbiError.code is AbiErrorCode type
        message: StringView::null(),  // Use method directly (helper function may not be exported yet)
    };
    ```

    Also update line 116-119 similarly.

    The `code` field in AbiError struct is `AbiErrorCode` enum, not `u32`. Do NOT cast.
  </action>
  <verify>
    <automated>grep "AbiErrorCode::Generic as u32" tests/fixtures/test_plugin/src/lib.rs | wc -l | grep "^0$" && grep "AbiErrorCode::Generic" tests/fixtures/test_plugin/src/lib.rs | wc -l</automated>
  </verify>
  <done>test_plugin uses AbiErrorCode enum directly without u32 cast</done>
  <acceptance_criteria>
    - 0 matches for `AbiErrorCode::.*as u32` pattern
    - At least 2 matches for `AbiErrorCode::Generic` (no cast)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix error_plugin AbiError.code type usage</name>
  <files>tests/fixtures/error_plugin/src/lib.rs</files>
  <read_first>
    - tests/fixtures/error_plugin/src/lib.rs (current state)
    - crates/polyplug_abi/src/types/abi_error.rs (AbiError struct)
  </read_first>
  <action>
    Fix error_plugin fixture to use AbiErrorCode enum directly without u32 cast.

    Multiple locations to fix:
    1. Lines 62-66: `code: AbiErrorCode::Generic as u32` -> `code: AbiErrorCode::Generic`
    2. Lines 96-99: `code: AbiErrorCode::Panic as u32` -> `code: AbiErrorCode::Panic`
    3. Lines 123-126: `code: AbiErrorCode::NotFound as u32` -> `code: AbiErrorCode::NotFound`
    4. Lines 130-134: `code: AbiErrorCode::FunctionNotAvailable as u32` -> `code: AbiErrorCode::FunctionNotAvailable`
    5. Lines 227-230: `code: AbiErrorCode::Generic as u32` -> `code: AbiErrorCode::Generic`
    6. Lines 234-237: similar fix

    Also update message field to use `StringView::null()` if helper not available.
  </action>
  <verify>
    <automated>grep -c "as u32" tests/fixtures/error_plugin/src/lib.rs | grep "^0$" || echo "FAIL: still has u32 casts"</automated>
  </verify>
  <done>error_plugin uses AbiErrorCode enum directly without any u32 casts</done>
  <acceptance_criteria>
    - 0 matches for `as u32` in error_plugin
    - All AbiError { code: ... } blocks use AbiErrorCode::Variant directly
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Fix reload_plugin_v2 AbiError.code type usage</name>
  <files>tests/fixtures/reload_plugin_v2/src/lib.rs</files>
  <read_first>
    - tests/fixtures/reload_plugin_v2/src/lib.rs (current state)
    - crates/polyplug_abi/src/types/abi_error.rs (AbiError struct)
  </read_first>
  <action>
    Fix reload_plugin_v2 fixture to use AbiErrorCode enum directly without u32 cast.

    Locations to fix:
    1. Lines 80-83: `code: AbiErrorCode::Generic as u32` -> `code: AbiErrorCode::Generic`
    2. Lines 86-89: similar fix

    Also update message field to use `StringView::null()` if helper not available.
  </action>
  <verify>
    <automated>grep -c "as u32" tests/fixtures/reload_plugin_v2/src/lib.rs | grep "^0$"</automated>
  </verify>
  <done>reload_plugin_v2 uses AbiErrorCode enum directly without u32 casts</done>
  <acceptance_criteria>
    - 0 matches for `as u32` in reload_plugin_v2
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update fixtures to use helper functions from polyplug_abi root</name>
  <files>tests/fixtures/test_plugin/src/lib.rs, tests/fixtures/error_plugin/src/lib.rs, tests/fixtures/reload_plugin_v2/src/lib.rs</files>
  <read_first>
    - tests/fixtures/test_plugin/src/lib.rs
    - tests/fixtures/error_plugin/src/lib.rs
    - tests/fixtures/reload_plugin_v2/src/lib.rs
    - crates/polyplug_abi/src/lib.rs (verify helper exports from plan 05)
  </read_first>
  <action>
    If plan 05 added helper function exports to polyplug_abi root, update fixture imports to use them.

    The fixtures currently call:
    - `abi_error_ok()` - should be `polyplug_abi::abi_error_ok()` or imported via `use polyplug_abi::*`
    - `string_view_null()` - should be `polyplug_abi::string_view_null()` or `StringView::null()`
    - `string_view_from_static()` - should be `polyplug_abi::string_view_from_static()` or `StringView::from_static()`

    Since fixtures have `use polyplug_abi::*;`, the helper functions should be available after plan 05 exports them.

    If helpers are not exported (plan 05 not complete), use the methods directly:
    - `StringView::null()` instead of `string_view_null()`
    - `StringView::from_static(b"...")` instead of `string_view_from_static(b"...")`
    - `AbiError::ok()` instead of `abi_error_ok()`

    Check if `string_view_null()` calls need to change to `StringView::null()`.
    Check if `abi_error_ok()` calls need to change to `AbiError::ok()`.
  </action>
  <verify>
    <automated>cargo build -p test_plugin -p error_plugin -p reload_plugin_v2 2>&1 | grep -E "^error" | wc -l || echo "SUCCESS"</automated>
  </verify>
  <done>All fixtures use correct helper function paths or direct method calls</done>
  <acceptance_criteria>
    - All fixtures compile without errors
    - Helper function calls resolve correctly
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Verify workspace compiles</name>
  <files></files>
  <read_first>
    - All modified files from Tasks 1-4
    - crates/polyplug_abi/src/lib.rs (exports from plan 05)
  </read_first>
  <action>
    Run cargo build for entire workspace to verify all gap closure fixes work together.

    Execute: cargo build --workspace
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | grep -E "^error" | wc -l || echo "SUCCESS: workspace compiles"</automated>
  </verify>
  <done>Entire workspace compiles without errors - phase 01 complete</done>
  <acceptance_criteria>
    - 0 compilation errors across workspace
    - cargo build --workspace returns exit code 0
    - All ABI types properly exported and used
  </acceptance_criteria>
</task>

</tasks>

<verification>
Final verification of entire phase:
1. cargo build --workspace succeeds
2. No ABI_* constant imports in SDK
3. No u32 casts in fixtures
4. Helper functions work correctly
</verification>

<success_criteria>
- Workspace compiles successfully
- All deprecated ABI_* imports removed
- All fixtures use AbiErrorCode enum correctly (no u32 cast)
- Helper functions accessible from polyplug_abi
- Phase 01 goal achieved: workspace compiles
</success_criteria>

<output>
After completion, create `.planning/phases/01-abi-types/01-abi-types-09-SUMMARY.md`
</output>