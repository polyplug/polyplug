---
phase: 01-abi-types
plan: 06
type: execute
wave: 5
depends_on: []
files_modified: [crates/polyplug/src/compatibility/capability_graph.rs, crates/polyplug/src/compatibility/contract_capability.rs, crates/polyplug/src/compatibility/dependency_edge.rs, crates/polyplug/src/loader/manifest.rs, crates/polyplug/src/ffi.rs]
autonomous: true
gap_closure: true
requirements: [ABI-11]  # ABI-11: Rename PluginContractId to GuestContractId

must_haves:
  truths:
    - "All polyplug files use GuestContractId instead of PluginContractId"
    - "ffi.rs ReloadPhaseC uses bundle_id.id() for u64 conversion"
    - "polyplug crate compiles without type errors"
  artifacts:
    - path: "crates/polyplug/src/compatibility/capability_graph.rs"
      provides: "Capability graph type definitions"
      contains: "use polyplug_utils::GuestContractId;"
    - path: "crates/polyplug/src/compatibility/contract_capability.rs"
      provides: "Contract capability struct"
      contains: "GuestContractId"
    - path: "crates/polyplug/src/compatibility/dependency_edge.rs"
      provides: "Dependency edge struct"
      contains: "GuestContractId"
    - path: "crates/polyplug/src/loader/manifest.rs"
      provides: "Manifest parsing types"
      contains: "GuestContractId"
    - path: "crates/polyplug/src/ffi.rs"
      provides: "FFI boundary types"
      pattern: "bundle_id.id()"
  key_links:
    - from: "compatibility/*.rs"
      to: "polyplug_utils"
      via: "import"
      pattern: "polyplug_utils::GuestContractId"
    - from: "ffi.rs::ReloadPhaseC"
      to: "BundleId"
      via: ".id() method"
      pattern: "bundle_id\\.id()"
---

<objective>
Replace deprecated PluginContractId with GuestContractId in polyplug crate files and fix ffi.rs bundle_id type mismatch.

Purpose: Phase 1 renamed PluginContractId to GuestContractId in polyplug_utils, but 4 files in polyplug crate still import and use the deprecated type. ffi.rs also has a type mismatch where BundleId needs conversion to u64.

Output: Updated imports and type references in 5 files, polyplug crate compiles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-abi-types/01-VERIFICATION.md

<interfaces>
<!-- Current deprecated usage that needs fixing -->

From crates/polyplug/src/compatibility/capability_graph.rs (line 9):
```rust
use polyplug_utils::PluginContractId;  // DEPRECATED - needs GuestContractId
```

From crates/polyplug/src/compatibility/contract_capability.rs (lines 2, 8, 15):
```rust
use polyplug_utils::PluginContractId;  // DEPRECATED - needs GuestContractId
pub contract_id: PluginContractId,     // needs GuestContractId
PluginContractId::new(&name, version.major)  // needs GuestContractId::new
```

From crates/polyplug/src/compatibility/dependency_edge.rs (lines 1, 8):
```rust
use polyplug_utils::PluginContractId;  // DEPRECATED - needs GuestContractId
pub contract_id: PluginContractId,     // needs GuestContractId
```

From crates/polyplug/src/loader/manifest.rs (line 12, multiple usages):
```rust
use polyplug_utils::{BundleId, PluginContractId};  // needs GuestContractId
// Multiple occurrences in RawManifestDependency and ManifestDependency structs
```

From crates/polyplug/src/ffi.rs (line 81 - current broken code):
```rust
ReloadPhase::Preparing {
    bundle_id,  // This is BundleId, but ReloadPhaseC.bundle_id is u64
    ...
} => ReloadPhaseC {
    bundle_id: *bundle_id,  // WRONG: dereferences BundleId struct, expects u64
    ...
}
```

Expected fix in ffi.rs:
```rust
bundle_id: bundle_id.id(),  // CORRECT: BundleId.id() returns u64
```

From crates/polyplug_utils/src/guest_contract_id.rs (the new type):
```rust
#[repr(transparent)]
pub struct GuestContractId(u64);

impl GuestContractId {
    pub fn new(name: &str, major_version: u32) -> Self { ... }
    pub fn from_u64(id: u64) -> Self { ... }
    pub fn id(&self) -> u64 { ... }
}
```

From crates/polyplug_utils/src/bundle_id.rs (type used in ffi.rs):
```rust
pub struct BundleId(u64);

impl BundleId {
    pub fn id(&self) -> u64 { self.0 }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update capability_graph.rs to use GuestContractId</name>
  <files>crates/polyplug/src/compatibility/capability_graph.rs</files>
  <read_first>
    - crates/polyplug/src/compatibility/capability_graph.rs (current state)
    - crates/polyplug_utils/src/guest_contract_id.rs (GuestContractId definition)
  </read_first>
  <action>
    Replace all PluginContractId usages with GuestContractId in capability_graph.rs.

    Specific changes:
    1. Line 9: Change `use polyplug_utils::PluginContractId;` to `use polyplug_utils::GuestContractId;`
    2. Line 58: Change `HashMap<PluginContractId, ...>` to `HashMap<GuestContractId, ...>`

    The file uses PluginContractId as HashMap key type - update all occurrences.
  </action>
  <verify>
    <automated>grep -c "PluginContractId" crates/polyplug/src/compatibility/capability_graph.rs && echo "Count must be 0" || grep -c "GuestContractId" crates/polyplug/src/compatibility/capability_graph.rs</automated>
  </verify>
  <done>No PluginContractId references remain in capability_graph.rs, all replaced with GuestContractId</done>
  <acceptance_criteria>
    - `grep PluginContractId` returns 0 matches
    - `grep GuestContractId` returns at least 2 matches
    - cargo build -p polyplug succeeds for this file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update contract_capability.rs to use GuestContractId</name>
  <files>crates/polyplug/src/compatibility/contract_capability.rs</files>
  <read_first>
    - crates/polyplug/src/compatibility/contract_capability.rs (current state)
    - crates/polyplug_utils/src/guest_contract_id.rs (GuestContractId definition)
  </read_first>
  <action>
    Replace all PluginContractId usages with GuestContractId in contract_capability.rs.

    Specific changes:
    1. Line 2: Change `use polyplug_utils::PluginContractId;` to `use polyplug_utils::GuestContractId;`
    2. Line 8: Change `pub contract_id: PluginContractId,` to `pub contract_id: GuestContractId,`
    3. Line 15: Change `PluginContractId::new(&name, version.major)` to `GuestContractId::new(&name, version.major)`
  </action>
  <verify>
    <automated>grep -c "PluginContractId" crates/polyplug/src/compatibility/contract_capability.rs | grep "^0$" && grep -c "GuestContractId" crates/polyplug/src/compatibility/contract_capability.rs</automated>
  </verify>
  <done>No PluginContractId references remain in contract_capability.rs, all replaced with GuestContractId</done>
  <acceptance_criteria>
    - `grep PluginContractId` returns 0 matches
    - `grep GuestContractId` returns at least 3 matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update dependency_edge.rs to use GuestContractId</name>
  <files>crates/polyplug/src/compatibility/dependency_edge.rs</files>
  <read_first>
    - crates/polyplug/src/compatibility/dependency_edge.rs (current state)
    - crates/polyplug_utils/src/guest_contract_id.rs (GuestContractId definition)
  </read_first>
  <action>
    Replace all PluginContractId usages with GuestContractId in dependency_edge.rs.

    Specific changes:
    1. Line 1: Change `use polyplug_utils::PluginContractId;` to `use polyplug_utils::GuestContractId;`
    2. Line 8: Change `pub contract_id: PluginContractId,` to `pub contract_id: GuestContractId,`
  </action>
  <verify>
    <automated>grep -c "PluginContractId" crates/polyplug/src/compatibility/dependency_edge.rs | grep "^0$" && grep "GuestContractId" crates/polyplug/src/compatibility/dependency_edge.rs</automated>
  </verify>
  <done>No PluginContractId references remain in dependency_edge.rs, all replaced with GuestContractId</done>
  <acceptance_criteria>
    - `grep PluginContractId` returns 0 matches
    - `grep GuestContractId` returns at least 2 matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update manifest.rs to use GuestContractId</name>
  <files>crates/polyplug/src/loader/manifest.rs</files>
  <read_first>
    - crates/polyplug/src/loader/manifest.rs (current state - see multiple PluginContractId usages)
    - crates/polyplug_utils/src/guest_contract_id.rs (GuestContractId definition)
  </read_first>
  <action>
    Replace all PluginContractId usages with GuestContractId in manifest.rs.

    Specific changes:
    1. Line 12: Change `use polyplug_utils::{BundleId, PluginContractId};` to `use polyplug_utils::{BundleId, GuestContractId};`
    2. Line 119: Change `pub contract_id: PluginContractId,` to `pub contract_id: GuestContractId,`
    3. Line 130-133: Change all PluginContractId to GuestContractId in ByContract variant
    4. Line 143-148: Change all PluginContractId to GuestContractId in ByBundle variant
    5. Line 157-169: Change all PluginContractId to GuestContractId in ManifestDependency enum
    6. Lines 290, 367, 393, 425, 446-447 in tests: Change all PluginContractId to GuestContractId

    Use sed or manual edit to replace all occurrences:
    `PluginContractId` -> `GuestContractId`
  </action>
  <verify>
    <automated>grep -c "PluginContractId" crates/polyplug/src/loader/manifest.rs | grep "^0$" && grep -c "GuestContractId" crates/polyplug/src/loader/manifest.rs</automated>
  </verify>
  <done>No PluginContractId references remain in manifest.rs, all replaced with GuestContractId</done>
  <acceptance_criteria>
    - `grep PluginContractId` returns 0 matches
    - `grep GuestContractId` returns at least 15 matches (multiple struct fields and test usage)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Fix ffi.rs bundle_id type mismatch</name>
  <files>crates/polyplug/src/ffi.rs</files>
  <read_first>
    - crates/polyplug/src/ffi.rs (lines 71-109 - ReloadPhaseC::from_reload_phase function)
    - crates/polyplug_utils/src/bundle_id.rs (BundleId.id() method)
  </read_first>
  <action>
    Fix the bundle_id field in ReloadPhaseC::from_reload_phase. The bundle_id field in ReloadPhaseC is u64, but the ReloadPhase enum variants contain BundleId structs. Use BundleId.id() method to convert.

    Specific changes in the match arms (lines 74-107):
    1. Line 81: Change `bundle_id: *bundle_id,` to `bundle_id: bundle_id.id(),`
    2. Line 91: Change `bundle_id: *bundle_id,` to `bundle_id: bundle_id.id(),`
    3. Line 101: Change `bundle_id: *bundle_id,` to `bundle_id: bundle_id.id(),`

    The BundleId struct has a `.id()` method that returns the inner u64 value.
    Do NOT dereference with `*bundle_id` - that's wrong for a struct type.
  </action>
  <verify>
    <automated>grep -n "bundle_id\\.id()" crates/polyplug/src/ffi.rs | wc -l | grep "^3$" && grep -c "\\*bundle_id" crates/polyplug/src/ffi.rs | grep "^0$"</automated>
  </verify>
  <done>All three ReloadPhaseC match arms use bundle_id.id() for correct u64 conversion</done>
  <acceptance_criteria>
    - 3 occurrences of `bundle_id.id()` in ffi.rs (one per match arm)
    - 0 occurrences of `*bundle_id` (wrong dereference pattern removed)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Verify polyplug crate compiles</name>
  <files></files>
  <read_first>
    - All modified files from Tasks 1-5
  </read_first>
  <action>
    Run cargo build for polyplug crate to verify all type updates compile correctly.

    Execute: cargo build -p polyplug
  </action>
  <verify>
    <automated>cargo build -p polyplug 2>&1 | grep -E "^error" | wc -l | grep "^0$" || echo "FAIL: polyplug has errors"</automated>
  </verify>
  <done>polyplug crate compiles with GuestContractId and fixed ffi.rs</done>
  <acceptance_criteria>
    - No compilation errors in polyplug crate
    - cargo build -p polyplug returns exit code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Verify all type updates:
1. grep confirms no PluginContractId in modified files
2. grep confirms GuestContractId in all modified files
3. grep confirms bundle_id.id() pattern in ffi.rs
4. cargo build -p polyplug succeeds
</verification>

<success_criteria>
- No PluginContractId references in polyplug crate compatibility files
- ffi.rs uses bundle_id.id() for u64 conversion
- polyplug crate compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-abi-types/01-abi-types-06-SUMMARY.md`
</output>