---
phase: 01-abi-types
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - All files importing PluginInterface, HostVTable, PluginContractId, RuntimeConfig, Compatibility
  - Generated code files in examples/guests/*/generated/
  - SDK files in sdks/*/
  - Test files referencing old type names
  - crates/polyplug/src/ffi.rs
  - crates/polyplug/src/runtime.rs
  - crates/polyplug/src/runtime_builder.rs
  - crates/polyplug/src/registry/plugin_registry.rs
  - Loader crates (polyplug_native, polyplug_python, polyplug_lua, polyplug_js, polyplug_dotnet)
autonomous: true
requirements: []  # Integration/validation phase - covers all prior requirements
user_setup: []
must_haves:
  truths:
    - "All imports of PluginInterface use GuestContractInterface (or legacy alias)"
    - "All imports of HostVTable use RuntimeAbi (or legacy alias)"
    - "All imports of PluginContractId use GuestContractId"
    - "All imports of RuntimeConfig use polyplug_abi::RuntimeConfig"
    - "cargo build --workspace succeeds"
    - "cargo test --workspace passes"
  artifacts:
    - path: "crates/polyplug/src/ffi.rs"
      provides: "FFI entry points"
      pattern: "GuestContractInterface|RuntimeAbi"
    - path: "crates/polyplug/src/runtime.rs"
      provides: "Runtime implementation"
      pattern: "polyplug_abi::RuntimeConfig"
  key_links:
    - from: "All crates"
      to: "polyplug_abi types"
      via: "imports"
      pattern: "use polyplug_abi::{GuestContractInterface, RuntimeAbi, RuntimeConfig}"
---

<objective>
Update all imports across the workspace to use renamed types and ensure compilation passes.

Purpose: Complete the type migration by updating all references in loaders, SDKs, tests, and generated code.
Output: Workspace compiles and all tests pass with new type names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-abi-types/01-03-PLAN.md

<interfaces>
<!-- Key imports to update across workspace -->

Files using PluginInterface (142 files) - update to GuestContractInterface:
- crates/polyplug/src/ffi.rs
- crates/polyplug/src/runtime.rs
- crates/polyplug/src/registry/plugin_registry.rs
- crates/polyplug_native/src/loader.rs
- crates/polyplug_python/src/lib.rs
- crates/polyplug_lua/src/loader.rs
- crates/polyplug_js/src/loader.rs
- crates/polyplug_dotnet/src/lib.rs
- All test fixtures in tests/fixtures/*/
- All generated code in examples/guests/*/generated/

Files using HostVTable (121 files) - update to RuntimeAbi:
- crates/polyplug/src/ffi.rs
- crates/polyplug/src/runtime.rs
- crates/polyplug/src/runtime_builder.rs
- crates/polyplug_abi/src/tracking.rs
- All SDK files in sdks/*/host/

Files using PluginContractId (15 files) - update to GuestContractId:
- crates/polyplug_abi/src/plugin/plugin_interface.rs -> guest/guest_contract_interface.rs
- crates/polyplug/src/registry/plugin_registry.rs
- crates/polyplug/src/compatibility/*.rs

Files using RuntimeConfig (50 files) - update imports from polyplug_abi:
- crates/polyplug/src/runtime.rs
- crates/polyplug/src/runtime_builder.rs
- All SDK runtime_config.* files

Legacy aliases available for gradual migration:
- pub type PluginInterface = GuestContractInterface
- pub type HostVTable = RuntimeAbi
- pub type PluginContractId = GuestContractId
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update polyplug crate core files</name>
  <files>
    - crates/polyplug/src/ffi.rs
    - crates/polyplug/src/runtime.rs
    - crates/polyplug/src/runtime_builder.rs
    - crates/polyplug/src/registry/plugin_registry.rs
    - crates/polyplug/src/host_bridge.rs
  </files>
  <read_first>
    - crates/polyplug/src/ffi.rs (FFI entry points)
    - crates/polyplug/src/runtime.rs (Runtime implementation)
    - crates/polyplug/src/runtime_builder.rs (Builder pattern)
    - crates/polyplug/src/registry/plugin_registry.rs (Registry)
    - crates/polyplug/src/host_bridge.rs (Host bridge)
    - crates/polyplug_abi/src/lib.rs (new exports)
  </read_first>
  <action>
    Update imports in polyplug core files:

    1. ffi.rs:
       - `use polyplug_abi::plugin::{PluginInterface, PluginHandle, PluginDescriptor}` 
         -> `use polyplug_abi::{GuestContractInterface, PluginHandle, PluginDescriptor}`
       - `use polyplug_abi::host::HostVTable`
         -> `use polyplug_abi::RuntimeAbi`
       - Update all function signatures using these types
       - Keep using PluginHandle for now (ContractHandle alias in Phase 2)

    2. runtime.rs:
       - `use crate::runtime_config::RuntimeConfig`
         -> `use polyplug_abi::RuntimeConfig`
       - Update HostVTable references to RuntimeAbi

    3. runtime_builder.rs:
       - `RuntimeConfig` import from polyplug_abi
       - Update HostVTable to RuntimeAbi

    4. registry/plugin_registry.rs:
       - `PluginInterface` -> `GuestContractInterface`
       - `PluginContractId` -> `GuestContractId`
       - Update all struct fields and method signatures

    5. host_bridge.rs:
       - `HostVTable` -> `RuntimeAbi`

    Concrete import pattern:
    ```rust
    use polyplug_abi::{
        GuestContractInterface,
        GuestContractInstance,
        RuntimeAbi,
        RuntimeConfig,
        Compatibility,
        PluginHandle,
        PluginDescriptor,
    };
    use polyplug_utils::GuestContractId;
    ```
  </action>
  <verify>
    <automated>grep -q "use polyplug_abi::RuntimeAbi" crates/polyplug/src/ffi.rs && grep -q "use polyplug_abi::GuestContractInterface" crates/polyplug/src/registry/plugin_registry.rs && cargo build -p polyplug 2>&1 | grep -c "error" | grep -q "^0$"</automated>
  </verify>
  <done>
    - All polyplug core files updated with new imports
    - GuestContractInterface used instead of PluginInterface
    - RuntimeAbi used instead of HostVTable
    - GuestContractId used instead of PluginContractId
    - RuntimeConfig imported from polyplug_abi
    - polyplug crate compiles successfully
  </done>
  <acceptance_criteria>
    - `crates/polyplug/src/ffi.rs` contains `use polyplug_abi::RuntimeAbi`
    - `crates/polyplug/src/ffi.rs` contains `GuestContractInterface` (not PluginInterface directly)
    - `crates/polyplug/src/runtime.rs` contains `use polyplug_abi::RuntimeConfig`
    - `crates/polyplug/src/registry/plugin_registry.rs` contains `use polyplug_utils::GuestContractId`
    - `cargo build -p polyplug` exits with 0 errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update loader crates</name>
  <files>
    - crates/polyplug_native/src/loader.rs
    - crates/polyplug_python/src/lib.rs
    - crates/polyplug_lua/src/loader.rs
    - crates/polyplug_js/src/loader.rs
    - crates/polyplug_dotnet/src/lib.rs
  </files>
  <read_first>
    - crates/polyplug_native/src/loader.rs (Native loader)
    - crates/polyplug_python/src/lib.rs (Python loader)
    - crates/polyplug_lua/src/loader.rs (Lua loader)
    - crates/polyplug_js/src/loader.rs (JS loader)
    - crates/polyplug_dotnet/src/lib.rs (.NET loader)
    - crates/polyplug_abi/src/lib.rs (new exports)
  </read_first>
  <action>
    Update imports in all loader crates:

    Pattern for each loader:
    ```rust
    // Before:
    use polyplug_abi::plugin::PluginInterface;
    use polyplug_abi::host::HostVTable;
    
    // After:
    use polyplug_abi::GuestContractInterface;
    use polyplug_abi::RuntimeAbi;
    ```

    Concrete changes per loader:

    1. polyplug_native/src/loader.rs:
       - Update PluginInterface -> GuestContractInterface
       - Update HostVTable -> RuntimeAbi
       - Update PluginContractId -> GuestContractId
       - Update register_contract calls to use new types

    2. polyplug_python/src/lib.rs:
       - Same pattern as native loader

    3. polyplug_lua/src/loader.rs:
       - Same pattern

    4. polyplug_js/src/loader.rs:
       - Same pattern

    5. polyplug_dotnet/src/lib.rs:
       - Same pattern

    Note: The `register_contract` function in RuntimeAbi expects:
    - descriptor: *const PluginDescriptor (unchanged)
    - vtable: *const GuestContractInterface (changed from PluginInterface)
  </action>
  <verify>
    <automated>grep -q "use polyplug_abi::GuestContractInterface" crates/polyplug_native/src/loader.rs && grep -q "use polyplug_abi::RuntimeAbi" crates/polyplug_python/src/lib.rs && cargo build -p polyplug_native -p polyplug_python -p polyplug_lua -p polyplug_js -p polyplug_dotnet 2>&1 | grep -c "error" | grep -q "^0$"</automated>
  </verify>
  <done>
    - All loader crates updated with new type imports
    - register_contract calls use GuestContractInterface
    - All loader crates compile successfully
  </done>
  <acceptance_criteria>
    - `crates/polyplug_native/src/loader.rs` contains `use polyplug_abi::GuestContractInterface`
    - `crates/polyplug_python/src/lib.rs` contains `use polyplug_abi::RuntimeAbi`
    - `crates/polyplug_lua/src/loader.rs` contains `use polyplug_abi::GuestContractInterface`
    - `crates/polyplug_js/src/loader.rs` contains `use polyplug_abi::RuntimeAbi`
    - `crates/polyplug_dotnet/src/lib.rs` contains `use polyplug_abi::GuestContractInterface`
    - `cargo build` for all loader crates exits with 0 errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update test fixture plugins</name>
  <files>
    - tests/fixtures/test_plugin/src/lib.rs
    - tests/fixtures/error_plugin/src/lib.rs
    - tests/fixtures/memory_plugin/src/lib.rs
    - tests/fixtures/depender_plugin/src/lib.rs
    - tests/fixtures/reload_plugin_v1/src/lib.rs
    - tests/fixtures/reload_plugin_v2/src/lib.rs
    - tests/fixtures/test_plugin.rs
    - tests/fixtures/test_plugin.lua
    - tests/fixtures/test_plugin.py
  </files>
  <read_first>
    - tests/fixtures/test_plugin/src/lib.rs (main test fixture)
    - tests/fixtures/error_plugin/src/lib.rs (error handling fixture)
    - crates/polyplug_abi/src/lib.rs (new exports)
    - sdks/rust/guest/src/lib.rs (guest SDK)
  </read_first>
  <action>
    Update test fixture Rust plugins:

    Pattern for Rust fixtures:
    ```rust
    // Before:
    use polyplug_abi::plugin::PluginInterface;
    
    // After:
    use polyplug_abi::GuestContractInterface;
    ```

    Each fixture needs:
    1. Update import: `polyplug_abi::plugin::PluginInterface` -> `polyplug_abi::GuestContractInterface`
    2. Update struct construction if using PluginInterface directly
    3. Update any HostVTable references to RuntimeAbi

    For generated code in fixtures (if any):
    - These may need regeneration via polyplugc
    - Or update imports manually

    For Python/Lua fixtures:
    - These use SDK types, update SDK imports in Phase 5
    - Mark as deferred for now (SDK updates are Phase 5)
  </action>
  <verify>
    <automated>grep -q "use polyplug_abi::GuestContractInterface" tests/fixtures/test_plugin/src/lib.rs && grep -q "use polyplug_abi::GuestContractInterface" tests/fixtures/error_plugin/src/lib.rs && cargo build -p test_plugin -p error_plugin -p memory_plugin 2>&1 | grep -c "error" | grep -q "^0$"</automicated>
  </verify>
  <done>
    - All Rust test fixtures updated with GuestContractInterface
    - Test fixtures compile successfully
    - Python/Lua fixtures deferred to Phase 5 (SDK updates)
  </done>
  <acceptance_criteria>
    - `tests/fixtures/test_plugin/src/lib.rs` contains `use polyplug_abi::GuestContractInterface`
    - `tests/fixtures/error_plugin/src/lib.rs` contains `use polyplug_abi::GuestContractInterface`
    - `tests/fixtures/memory_plugin/src/lib.rs` contains `use polyplug_abi::GuestContractInterface`
    - Test fixture crates compile successfully
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update integration tests</name>
  <files>
    - crates/polyplug/tests/*.rs
    - tests/integration/tests/*.rs
    - crates/polyplug_native/tests/*.rs
    - crates/polyplug_python/tests/*.rs
    - crates/polyplug_lua/tests/*.rs
    - crates/polyplug_js/tests/*.rs
  </files>
  <read_first>
    - crates/polyplug/tests/integration_load.rs (representative test)
    - crates/polyplug/tests/integration_dispatch.rs (dispatch test)
    - tests/integration/tests/integration_rust_host_caller.rs (integration test)
    - crates/polyplug_abi/src/lib.rs (new exports)
  </read_first>
  <action>
    Update integration test imports:

    Pattern for test files:
    ```rust
    // Before:
    use polyplug_abi::plugin::PluginInterface;
    use polyplug_abi::host::HostVTable;
    use polyplug_utils::PluginContractId;
    
    // After:
    use polyplug_abi::{GuestContractInterface, RuntimeAbi};
    use polyplug_utils::GuestContractId;
    ```

    Key test files to update:
    1. crates/polyplug/tests/integration_load.rs
    2. crates/polyplug/tests/integration_dispatch.rs
    3. crates/polyplug/tests/integration_panic.rs
    4. crates/polyplug/tests/integration_context.rs
    5. crates/polyplug/tests/integration_ffi_robustness.rs
    6. crates/polyplug/tests/stress_*.rs
    7. crates/polyplug/tests/hot_reload_safety.rs
    8. tests/integration/tests/integration_rust_host_caller.rs
    9. tests/integration/tests/integration_hot_reload_notification.rs

    Update any test assertions that reference old type names:
    - `PluginInterface` -> `GuestContractInterface`
    - `HostVTable` -> `RuntimeAbi`
  </action>
  <verify>
    <automated>grep -q "use polyplug_abi::GuestContractInterface" crates/polyplug/tests/integration_load.rs && grep -q "use polyplug_abi::RuntimeAbi" crates/polyplug/tests/integration_dispatch.rs && cargo test -p polyplug --lib 2>&1 | tail -20 | grep -q "test result: ok"</automated>
  </verify>
  <done>
    - All integration tests updated with new type imports
    - Test assertions use new type names
    - lib tests pass
  </done>
  <acceptance_criteria>
    - `crates/polyplug/tests/integration_load.rs` contains `use polyplug_abi::GuestContractInterface`
    - `crates/polyplug/tests/integration_dispatch.rs` contains `use polyplug_abi::RuntimeAbi`
    - `crates/polyplug/tests/integration_context.rs` contains `GuestContractInterface`
    - `cargo test -p polyplug --lib` passes (exits 0)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Update polyplugc codegen references</name>
  <files>
    - crates/polyplugc/src/generators/rust.rs
    - crates/polyplugc/src/generators/python.rs
    - crates/polyplugc/src/generators/lua.rs
    - crates/polyplugc/src/generators/csharp.rs
    - crates/polyplugc/src/generators/cpp.rs
  </files>
  <read_first>
    - crates/polyplugc/src/generators/rust.rs (Rust generator)
    - crates/polyplugc/src/generators/python.rs (Python generator)
    - crates/polyplug_abi/src/lib.rs (new exports)
  </read_first>
  <action>
    Update codegen generators to output new type names:

    Pattern for generators:
    1. Replace `PluginInterface` -> `GuestContractInterface` in generated code strings
    2. Replace `HostVTable` -> `RuntimeAbi` in generated code strings
    3. Replace `PluginContractId` -> `GuestContractId` in generated code strings

    Concrete changes per generator:

    1. rust.rs:
       - Generated imports: `polyplug_abi::plugin::PluginInterface` -> `polyplug_abi::GuestContractInterface`
       - Generated struct references update

    2. python.rs:
       - Generated imports: `from polyplug_abi import PluginInterface` -> `from polyplug_abi import GuestContractInterface`

    3. lua.rs:
       - Generated references to `PluginInterface` -> `GuestContractInterface`

    4. csharp.rs:
       - Generated references: `PluginInterface` -> `GuestContractInterface`

    5. cpp.rs:
       - Generated references: `PluginInterface` -> `GuestContractInterface`

    Note: Generated code files will need regeneration after codegen update.
    This can be done via: `cargo run -p polyplugc -- generate ...`
    For Phase 1, just update the generators; regeneration is Phase 5.
  </action>
  <verify>
    <automated>grep -q "GuestContractInterface" crates/polyplugc/src/generators/rust.rs && grep -q "RuntimeAbi" crates/polyplugc/src/generators/python.rs && cargo build -p polyplugc 2>&1 | grep -c "error" | grep -q "^0$"</automated>
  </verify>
  <done>
    - All codegen generators updated to output new type names
    - Generated code will use GuestContractInterface, RuntimeAbi, GuestContractId
    - polyplugc compiles successfully
  </done>
  <acceptance_criteria>
    - `crates/polyplugc/src/generators/rust.rs` contains `GuestContractInterface`
    - `crates/polyplugc/src/generators/python.rs` contains `GuestContractInterface`
    - `crates/polyplugc/src/generators/lua.rs` contains `GuestContractInterface`
    - `crates/polyplugc/src/generators/csharp.rs` contains `GuestContractInterface`
    - `crates/polyplugc/src/generators/cpp.rs` contains `GuestContractInterface`
    - `cargo build -p polyplugc` exits with 0 errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Full workspace build and test verification</name>
  <files>
    No files modified - verification task
  </files>
  <read_first>
    - crates/polyplug_abi/src/lib.rs (verify exports)
    - crates/polyplug/src/lib.rs (verify imports)
  </read_first>
  <action>
    Run full workspace verification:

    1. Build entire workspace:
       ```bash
       cargo build --workspace
       ```

    2. Run all tests:
       ```bash
       cargo test --workspace
       ```

    3. Check for any remaining old type references:
       ```bash
       # Should find no direct usage (only legacy aliases)
       grep -r "use.*PluginInterface" --include="*.rs" crates/
       grep -r "use.*HostVTable" --include="*.rs" crates/
       grep -r "use.*PluginContractId" --include="*.rs" crates/
       ```

    4. Verify new types are properly imported:
       ```bash
       grep -r "use.*GuestContractInterface" --include="*.rs" crates/ | wc -l
       grep -r "use.*RuntimeAbi" --include="*.rs" crates/ | wc -l
       grep -r "use.*GuestContractId" --include="*.rs" crates/ | wc -l
       ```

    Accept any warnings but no errors.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | grep -c "^error" | grep -q "^0$" && cargo test --workspace 2>&1 | tail -30 | grep -q "test result: ok"</automated>
  </verify>
  <done>
    - Workspace builds with 0 errors
    - All workspace tests pass
    - New type imports verified across crate sources
  </done>
  <acceptance_criteria>
    - `cargo build --workspace` exits with 0 errors (warnings acceptable)
    - `cargo test --workspace` passes (test result: ok)
    - No direct `PluginInterface` imports (only legacy alias usage)
    - No direct `HostVTable` imports (only legacy alias usage)
    - `GuestContractInterface` imports present in expected files
    - `RuntimeAbi` imports present in expected files
  </acceptance_criteria>
</task>

</tasks>

<verification>
Full workspace verification:
```bash
cargo build --workspace
cargo test --workspace
```

Check for remaining old type references:
```bash
grep -r "PluginInterface" --include="*.rs" crates/ 2>/dev/null | grep -v "pub type PluginInterface" | grep -v "test" | head -20
grep -r "HostVTable" --include="*.rs" crates/ 2>/dev/null | grep -v "pub type HostVTable" | grep -v "test" | head -20
```
</verification>

<success_criteria>
- All core polyplug files updated with new type imports
- All loader crates updated with GuestContractInterface and RuntimeAbi
- All test fixtures updated (Rust fixtures)
- All integration tests updated and passing
- All codegen generators output new type names
- cargo build --workspace succeeds with 0 errors
- cargo test --workspace passes
- Legacy aliases available for gradual migration
</success_criteria>

<output>
After completion, create `.planning/phases/01-abi-types/01-04-SUMMARY.md`
</output>