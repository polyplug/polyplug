---
phase: 09
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polyplugc/tests/smoke.rs
autonomous: true
requirements: [CLN-04]
user_setup: []

must_haves:
  truths:
    - "smoke.rs lib.rs template declares pub mod interfaces"
    - "smoke.rs imports TEST_ADDER_VTABLE from guest::interfaces"
    - "smoke.rs imports set_test_adder_impl from guest::interfaces"
    - "smoke.rs expects guest/interfaces.hpp in C++ codegen"
    - "Rust codegen E2E test passes"
  artifacts:
    - path: "crates/polyplugc/tests/smoke.rs"
      provides: "Rust and C++ codegen E2E tests"
      min_lines: 500
      contains: "pub mod interfaces;"
  key_links:
    - from: "crates/polyplugc/tests/smoke.rs"
      to: "guest::interfaces"
      via: "use guest::interfaces::TEST_ADDER_VTABLE"
      pattern: "use guest::interfaces::"
---

<objective>
Update smoke.rs test file to use `interfaces.*` naming instead of `vtables.*`, aligning with current generator output.

Purpose: The generators now produce `interfaces.rs` and `interfaces.hpp`, not `vtables.*`. The smoke.rs test has outdated expectations causing test failures.

Output: Updated smoke.rs with correct naming, passing Rust codegen E2E test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-codegen-test-cleanup/09-RESEARCH.md
@.planning/phases/09-codegen-test-cleanup/09-VALIDATION.md
</context>

<interfaces>

<!-- Key patterns from integration_codegen_rust.rs (correct reference) -->
<!-- Executor should use these patterns directly. -->

From crates/polyplugc/tests/integration_codegen_rust.rs (lines 104-108, 123-124):
```rust
mod guest {
    pub mod types;
    pub mod contracts;
    pub mod interfaces;  // CORRECT pattern
}

use guest::interfaces::TEST_ADDER_VTABLE;  // CORRECT pattern
use guest::interfaces::set_test_adder_impl;  // CORRECT pattern
```

From crates/polyplugc/tests/integration_codegen_cpp.rs (correct expected files):
```rust
let expected_files: [&str; 5] = [
    "guest/types.hpp",
    "guest/contracts.hpp",
    "guest/interfaces.hpp",  // CORRECT - not vtables.hpp
    "guest/init.hpp",
    "manifest.toml",
];
```

Generator output paths (from crates/polyplugc/src/generators/*.rs):
- Rust generator: path `guest/interfaces.rs`
- C++ generator: path `guest/interfaces.hpp`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update smoke.rs lib.rs template to use interfaces naming</name>
  <files>crates/polyplugc/tests/smoke.rs</files>
  <read_first>
    - crates/polyplugc/tests/smoke.rs (file being modified)
    - crates/polyplugc/tests/integration_codegen_rust.rs (correct pattern reference)
  </read_first>
  <behavior>
    - Test 1: lib.rs template contains `pub mod interfaces;` not `pub mod vtables;`
    - Test 2: Imports use `guest::interfaces::TEST_ADDER_VTABLE` not `guest::vtables::TEST_ADDER_VTABLE`
    - Test 3: Imports use `guest::interfaces::set_test_adder_impl` not `guest::vtables::set_test_adder_impl`
  </behavior>
  <action>
    In the `write_plugin_lib_rs` function (around line 104-184), update the lib.rs template content:
    
    1. Line 110: Change `pub mod vtables;` to `pub mod interfaces;`
    
    2. Line 125: Change `use guest::vtables::TEST_ADDER_VTABLE;` to `use guest::interfaces::TEST_ADDER_VTABLE;`
    
    3. Line 126: Change `use guest::vtables::set_test_adder_impl;` to `use guest::interfaces::set_test_adder_impl;`
    
    These changes align the test with the actual generator output which produces `interfaces.rs` not `vtables.rs`.
  </action>
  <verify>
    <automated>grep -n "pub mod interfaces" crates/polyplugc/tests/smoke.rs && grep -n "use guest::interfaces::" crates/polyplugc/tests/smoke.rs</automated>
  </verify>
  <done>
    smoke.rs contains `pub mod interfaces;` in lib.rs template
    smoke.rs imports TEST_ADDER_VTABLE from guest::interfaces
    smoke.rs imports set_test_adder_impl from guest::interfaces
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update smoke.rs C++ codegen expected files and variable names</name>
  <files>crates/polyplugc/tests/smoke.rs</files>
  <read_first>
    - crates/polyplugc/tests/smoke.rs (file being modified)
  </read_first>
  <behavior>
    - Test 1: expected_guest_files array contains `"interfaces.hpp"` not `"vtables.hpp"`
    - Test 2: Variable renamed from `vtables_hpp` to `interfaces_hpp`
    - Test 3: Print statements reference `interfaces.hpp` not `vtables.hpp`
  </behavior>
  <action>
    In the `smoke_cpp_codegen_dispatch` test function (around line 440-519):
    
    1. Line 463: In the `expected_guest_files` array, change `"vtables.hpp"` to `"interfaces.hpp"`
    
    2. Line 490: Rename variable `vtables_hpp` to `interfaces_hpp`
    
    3. Line 498: Update reference from `&vtables_hpp` to `&interfaces_hpp`
    
    4. Line 507-511: Update error messages from `"vtables.hpp did not compile"` to `"interfaces.hpp did not compile"` and `"interfaces.hpp compiled successfully"`
    
    These changes align the C++ codegen test with the generator output which produces `interfaces.hpp`.
  </action>
  <verify>
    <automated>grep -n '"guest/interfaces.hpp"' crates/polyplugc/tests/smoke.rs && grep -n "interfaces_hpp" crates/polyplugc/tests/smoke.rs</automated>
  </verify>
  <done>
    smoke.rs C++ expected files array contains `"guest/interfaces.hpp"`
    Variable `interfaces_hpp` replaces `vtables_hpp`
    Print statements reference interfaces.hpp
  </done>
</task>

<task type="auto">
  <name>Task 3: Run smoke tests to verify Rust codegen E2E passes</name>
  <files></files>
  <read_first>
    - crates/polyplugc/tests/smoke.rs (verify changes)
  </read_first>
  <action>
    Run the smoke test suite to verify all changes compile and pass:
    
    ```bash
    cargo test -p polyplugc --test smoke
    ```
    
    Both `smoke_rust_codegen_dispatch` and `smoke_cpp_codegen_dispatch` should pass. The Rust test generates bindings, compiles a plugin, loads it, and dispatches add(3,5) expecting result 8. The C++ test generates bindings and verifies file existence.
  </action>
  <verify>
    <automated>cargo test -p polyplugc --test smoke -- --nocapture 2>&1 | grep -E "(smoke_rust_codegen_dispatch|smoke_cpp_codegen_dispatch).*ok"</automated>
  </verify>
  <done>
    cargo test -p polyplugc --test smoke exits with code 0
    smoke_rust_codegen_dispatch test passes
    smoke_cpp_codegen_dispatch test passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test execution | No external trust boundaries - test code only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | N/A | Test file update | accept | No security implications - pure test file naming update |
</threat_model>

<verification>
After all tasks:
1. grep confirms `pub mod interfaces;` present in lib.rs template
2. grep confirms `guest::interfaces::` imports present  
3. grep confirms `"guest/interfaces.hpp"` in expected files
4. cargo test -p polyplugc --test smoke passes
</verification>

<success_criteria>
- [ ] smoke.rs lib.rs template uses `pub mod interfaces;`
- [ ] smoke.rs imports from `guest::interfaces::*`
- [ ] smoke.rs C++ expected files includes `"guest/interfaces.hpp"`
- [ ] `cargo test -p polyplugc --test smoke` passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-codegen-test-cleanup/09-01-SUMMARY.md`
</output>