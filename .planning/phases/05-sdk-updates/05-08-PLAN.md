---
phase: 05-sdk-updates
plan: 08
type: execute
wave: 1
depends_on: [05-07]  # C++ SDK rename depends on 05-07 completing first
files_modified: [sdks/python/host/polyplug/runtime.py, sdks/csharp/host/NativeMethods.cs, sdks/lua/host/polyplug/runtime.lua, sdks/cpp/host/polyplug/runtime.hpp]
autonomous: true
requirements: [SDK-02, SDK-03, SDK-04, CLN-02]
gap_closure: true
must_haves:
  truths:
    - "Python SDK uses RuntimeConfig class (not RuntimeConfigC)"
    - "C# SDK uses RuntimeConfig struct (not RuntimeConfigC)"
    - "Lua SDK uses RuntimeConfig in ffi.cdef (not RuntimeConfigC)"
    - "C++ SDK uses RuntimeConfig struct (not RuntimeConfigC)"
    - "All SDK FFI types match polyplug_abi naming exactly"
  artifacts:
    - path: "sdks/python/host/polyplug/runtime.py"
      contains: "class RuntimeConfig"
      excludes: "RuntimeConfigC"
    - path: "sdks/csharp/host/NativeMethods.cs"
      contains: "struct RuntimeConfig"
      excludes: "RuntimeConfigC"
    - path: "sdks/lua/host/polyplug/runtime.lua"
      contains: "} RuntimeConfig;"
      excludes: "RuntimeConfigC"
    - path: "sdks/cpp/host/polyplug/runtime.hpp"
      contains: "struct RuntimeConfig"
      excludes: "RuntimeConfigC"
  key_links:
    - pattern: "RuntimeConfigC"
      issue: "Legacy C suffix naming violates CLN-02"
---

<objective>
Rename RuntimeConfigC to RuntimeConfig in all four native SDKs (Python, C#, Lua, C++) for naming consistency with polyplug_abi.

Purpose: The "*C" suffix is a legacy naming anti-pattern. All types should match the canonical polyplug_abi naming exactly.
Output: All SDKs use RuntimeConfig (no RuntimeConfigC references).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Source of truth: RuntimeConfig naming from polyplug_abi -->
<!-- All SDKs must match this exact naming -->
<!-- From crates/polyplug_abi/src/runtime/runtime_config.rs -->
```rust
pub struct RuntimeConfig { ... }  // NOT RuntimeConfigC
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename RuntimeConfigC to RuntimeConfig in Python SDK</name>
  <files>sdks/python/host/polyplug/runtime.py</files>
  <read_first>
    - sdks/python/host/polyplug/runtime.py (current RuntimeConfigC class at line 35)
  </read_first>
  <behavior>
    - Class named RuntimeConfig (not RuntimeConfigC)
    - All references updated throughout file
  </behavior>
  <action>
    Rename the ctypes.Structure class from RuntimeConfigC to RuntimeConfig in runtime.py.
    
    Change line 35:
    ```python
    class RuntimeConfigC(ctypes.Structure):
    ```
    to:
    ```python
    class RuntimeConfig(ctypes.Structure):
    ```
    
    Update all references in the file:
    - Line 63: RuntimeCreateOptionsC._fields_ uses `ctypes.POINTER(RuntimeConfigC)` → change to `RuntimeConfig`
    - Line 518: `config_c = RuntimeConfigC(...)` → change to `RuntimeConfig(...)`
    - Update any other RuntimeConfigC references
    
    Search pattern: `RuntimeConfigC` and replace all occurrences with `RuntimeConfig`.
  </action>
  <verify>
    <automated>grep -c "RuntimeConfigC" sdks/python/host/polyplug/runtime.py || echo "0"</automated>
    <expected_output>0</expected_output>
    <automated2>grep -c "class RuntimeConfig" sdks/python/host/polyplug/runtime.py</automated2>
    <expected_output2>1</expected_output2>
  </verify>
  <acceptance_criteria>
    - runtime.py does NOT contain "RuntimeConfigC"
    - runtime.py contains "class RuntimeConfig(ctypes.Structure):"
    - runtime.py uses RuntimeConfig in RuntimeCreateOptionsC._fields_
    - runtime.py uses RuntimeConfig in _create_runtime_with_options
  </acceptance_criteria>
  <done>Python SDK uses RuntimeConfig class (no RuntimeConfigC references).</done>
</task>

<task type="auto">
  <name>Task 2: Rename RuntimeConfigC to RuntimeConfig in C# SDK</name>
  <files>sdks/csharp/host/NativeMethods.cs</files>
  <read_first>
    - sdks/csharp/host/NativeMethods.cs (current RuntimeConfigC struct at line 68)
  </read_first>
  <behavior>
    - Struct named RuntimeConfig (not RuntimeConfigC)
    - PolyplugRuntimeSetConfig parameter uses RuntimeConfig
  </behavior>
  <action>
    Rename the struct from RuntimeConfigC to RuntimeConfig in NativeMethods.cs.
    
    Change line 68:
    ```csharp
    internal struct RuntimeConfigC
    ```
    to:
    ```csharp
    internal struct RuntimeConfig
    ```
    
    Update the PolyplugRuntimeSetConfig signature (line 190):
    ```csharp
    public static partial uint PolyplugRuntimeSetConfig(ref RuntimeConfigC config);
    ```
    to:
    ```csharp
    public static partial uint PolyplugRuntimeSetConfig(ref RuntimeConfig config);
    ```
    
    Search pattern: `RuntimeConfigC` and replace all occurrences with `RuntimeConfig`.
  </action>
  <verify>
    <automated>grep -c "RuntimeConfigC" sdks/csharp/host/NativeMethods.cs || echo "0"</automated>
    <expected_output>0</expected_output>
    <automated2>grep -c "struct RuntimeConfig" sdks/csharp/host/NativeMethods.cs</automated2>
    <expected_output2>1</expected_output2>
  </verify>
  <acceptance_criteria>
    - NativeMethods.cs does NOT contain "RuntimeConfigC"
    - NativeMethods.cs contains "internal struct RuntimeConfig"
    - NativeMethods.cs PolyplugRuntimeSetConfig uses "ref RuntimeConfig"
  </acceptance_criteria>
  <done>C# SDK uses RuntimeConfig struct (no RuntimeConfigC references).</done>
</task>

<task type="auto">
  <name>Task 3: Rename RuntimeConfigC to RuntimeConfig in Lua SDK</name>
  <files>sdks/lua/host/polyplug/runtime.lua</files>
  <read_first>
    - sdks/lua/host/polyplug/runtime.lua (current RuntimeConfigC in ffi.cdef at line 43)
  </read_first>
  <behavior>
    - FFI cdef struct named RuntimeConfig (not RuntimeConfigC)
    - RuntimeCreateOptions uses RuntimeConfig pointer
  </behavior>
  <action>
    Rename the FFI cdef struct from RuntimeConfigC to RuntimeConfig in runtime.lua.
    
    Change line 43 in the ffi.cdef block:
    ```lua
    } RuntimeConfigC;
    ```
    to:
    ```lua
    } RuntimeConfig;
    ```
    
    Also update the struct name in the typedef (line 35-43):
    ```lua
    typedef struct {
        uint8_t hot_reload_enabled;
        ...
        uint32_t compatibility;
    } RuntimeConfigC;
    ```
    to:
    ```lua
    typedef struct {
        uint8_t hot_reload_enabled;
        ...
        uint32_t compatibility;
    } RuntimeConfig;
    ```
    
    Update RuntimeCreateOptions (line 45-48):
    ```lua
    typedef struct {
        const RuntimeConfigC* config;
        ...
    } RuntimeCreateOptions;
    ```
    to:
    ```lua
    typedef struct {
        const RuntimeConfig* config;
        ...
    } RuntimeCreateOptions;
    ```
    
    Update the usage in Runtime.new (line 167):
    ```lua
    config_c = ffi.new("RuntimeConfigC", {...})
    ```
    to:
    ```lua
    config_c = ffi.new("RuntimeConfig", {...})
    ```
    
    Search pattern: `RuntimeConfigC` and replace all occurrences with `RuntimeConfig`.
  </action>
  <verify>
    <automated>grep -c "RuntimeConfigC" sdks/lua/host/polyplug/runtime.lua || echo "0"</automated>
    <expected_output>0</expected_output>
    <automated2>grep "} RuntimeConfig;" sdks/lua/host/polyplug/runtime.lua | wc -l</automated2>
    <expected_output2>1</expected_output2>
  </verify>
  <acceptance_criteria>
    - runtime.lua does NOT contain "RuntimeConfigC"
    - runtime.lua ffi.cdef contains "} RuntimeConfig;"
    - runtime.lua RuntimeCreateOptions uses "const RuntimeConfig* config"
    - runtime.lua uses "ffi.new("RuntimeConfig", {...})"
  </acceptance_criteria>
  <done>Lua SDK uses RuntimeConfig in FFI cdef (no RuntimeConfigC references).</done>
</task>

<task type="auto">
  <name>Task 4: Verify RuntimeConfig naming in C++ SDK (from plan 05-07)</name>
  <files>sdks/cpp/host/polyplug/runtime.hpp</files>
  <read_first>
    - sdks/cpp/host/polyplug/runtime.hpp (verify RuntimeConfig from plan 05-07)
  </read_first>
  <behavior>
    - Struct named RuntimeConfig (not RuntimeConfigC)
    - Plan 05-07 already renamed it, verify no regression
  </behavior>
  <action>
    Verify that plan 05-07 successfully renamed RuntimeConfigC to RuntimeConfig.
    
    If any RuntimeConfigC references remain (should not happen after 05-07):
    - Change "struct RuntimeConfigC" to "struct RuntimeConfig"
    - Update all usages in set_config and other methods
    
    This task is primarily verification. If 05-07 completed correctly, no changes needed.
  </action>
  <verify>
    <automated>grep -c "RuntimeConfigC" sdks/cpp/host/polyplug/runtime.hpp || echo "0"</automated>
    <expected_output>0</expected_output>
    <automated2>grep -c "struct RuntimeConfig" sdks/cpp/host/polyplug/runtime.hpp</automated2>
    <expected_output2>1</expected_output2>
  </verify>
  <acceptance_criteria>
    - runtime.hpp does NOT contain "RuntimeConfigC"
    - runtime.hpp contains "struct RuntimeConfig"
  </acceptance_criteria>
  <done>C++ SDK confirmed using RuntimeConfig struct (no RuntimeConfigC).</done>
</task>

<task type="auto">
  <name>Task 5: Verify no RuntimeConfigC in any SDK</name>
  <files>N/A (verification only)</files>
  <read_first>
    - All four SDK runtime files (to verify no RuntimeConfigC)
  </read_first>
  <behavior>
    - grep -r RuntimeConfigC across sdks/ returns 0 matches
    - All four SDKs verified clean
  </behavior>
  <action>
    Run cross-SDK verification to ensure no RuntimeConfigC references remain.
    
    Execute:
    ```bash
    grep -r "RuntimeConfigC" sdks/python sdks/csharp sdks/lua sdks/cpp 2>/dev/null
    ```
    
    Expected: empty output (no matches found).
    
    If any matches are found, investigate and fix the remaining RuntimeConfigC references before proceeding.
  </action>
  <verify>
    <automated>grep -r "RuntimeConfigC" sdks/python sdks/csharp sdks/lua sdks/cpp 2>/dev/null | wc -l</automated>
    <expected_output>0</expected_output>
  </verify>
  <acceptance_criteria>
    - grep -r "RuntimeConfigC" sdks/python sdks/csharp sdks/lua sdks/cpp returns 0 matches
    - grep output is empty (clean)
  </acceptance_criteria>
  <done>Cross-SDK verification confirms zero RuntimeConfigC references.</done>
</task>

</tasks>

<verification>
1. Python SDK: RuntimeConfig class (no RuntimeConfigC)
2. C# SDK: RuntimeConfig struct (no RuntimeConfigC)
3. Lua SDK: RuntimeConfig in ffi.cdef (no RuntimeConfigC)
4. C++ SDK: RuntimeConfig struct (no RuntimeConfigC)
5. Cross-SDK grep confirms zero RuntimeConfigC references
</verification>

<success_criteria>
- `grep -r "RuntimeConfigC" sdks/` returns 0 matches
- All four SDK files contain RuntimeConfig (not RuntimeConfigC)
- Naming matches polyplug_abi::RuntimeConfig exactly
</success_criteria>

<output>
After completion, create `.planning/phases/05-sdk-updates/05-08-SUMMARY.md`
</output>