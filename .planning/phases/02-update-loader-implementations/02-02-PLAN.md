---
phase: 02-update-loader-implementations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polyplug_python/src/lib.rs
  - crates/polyplug_python/src/error.rs
autonomous: true
requirements: [ERR-06]
must_haves:
  truths:
    - "PythonLoader uses LoaderError::InitFailed directly at all error sites"
    - "PythonLoaderError type no longer exists"
    - "Hot-reload returns RuntimeError::HotReloadDisabled"
  artifacts:
    - path: crates/polyplug_python/src/lib.rs
      provides: PythonLoader implementation
      contains: "LoaderError::InitFailed"
      not_contains: "PythonLoaderError"
      not_contains: "PythonModuleImportFailed"
      not_contains: "PythonInitRaisedException"
      not_contains: "PythonInitFailed"
    - path: crates/polyplug_python/src/error.rs
      exists: false
  key_links:
    - from: crates/polyplug_python/src/lib.rs
      to: crates/polyplug/src/error.rs
      via: "use polyplug::error::{LoaderError, RuntimeError}"
---

<objective>
Update PythonLoader to use LoaderError::InitFailed directly at all error sites and remove the PythonLoaderError type.

Purpose: Per D-01, use unified InitFailed pattern for consistency with other loaders.
Output: PythonLoader without local error type, using core InitFailed directly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-update-loader-implementations/02-CONTEXT.md
@.planning/phases/02-update-loader-implementations/02-RESEARCH.md
</context>

<interfaces>
<!-- Key types and contracts from codebase -->

From crates/polyplug/src/error.rs:
```rust
pub enum RuntimeError {
    #[error(transparent)]
    Loader(#[from] LoaderError),
    #[error("hot-reload is disabled in runtime config")]
    HotReloadDisabled,
}

pub enum LoaderError {
    #[error("init failed for bundle `{bundle}`: {error}")]
    InitFailed { bundle: String, error: String },
    
    #[error("manifest missing file field for bundle `{bundle}`")]
    ManifestMissingFile { bundle: String },
    
    #[error("init symbol missing in assembly `{bundle}`: expected `[UnmanagedCallersOnly] polyplug_init`")]
    InitSymbolMissing { bundle: String },
}
```

Target pattern (per D-01):
```rust
Err(RuntimeError::Loader(LoaderError::InitFailed {
    bundle: manifest.name.clone(),
    error: format!("descriptive message with context: {}", details),
}))
```

Hot-reload pattern (per D-03):
```rust
fn reload(...) -> Result<(), RuntimeError> {
    Err(RuntimeError::HotReloadDisabled)
}
```

Generic errors (use directly from core):
- `LoaderError::ManifestMissingFile { bundle }`
- `LoaderError::InitSymbolMissing { bundle }`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete PythonLoaderError and remove module exports</name>
  <files>crates/polyplug_python/src/error.rs, crates/polyplug_python/src/lib.rs</files>
  <read_first>
    - crates/polyplug_python/src/error.rs (file to delete)
    - crates/polyplug_python/src/lib.rs (module exports to modify, also contains PythonLoader)
  </read_first>
  <action>
    Per D-04: Remove unused PythonLoaderError.
    
    1. Delete file `crates/polyplug_python/src/error.rs` entirely
    
    2. Modify `crates/polyplug_python/src/lib.rs`:
       - Remove line: `pub mod error;`
       - Remove line: `pub use error::PythonLoaderError;`
       - Keep all other modules and exports (bridge, config, ffi)
    
    Note: The PythonLoader implementation is in lib.rs itself, so we need to update the error sites in the same file.
  </action>
  <acceptance_criteria>
    - `test -f crates/polyplug_python/src/error.rs` returns non-zero (file deleted)
    - `grep "pub mod error" crates/polyplug_python/src/lib.rs` returns empty
    - `grep "PythonLoaderError" crates/polyplug_python/src/lib.rs` returns empty
  </acceptance_criteria>
  <verify>
    <automated>grep -c "PythonLoaderError" crates/polyplug_python/src/lib.rs 2>/dev/null || echo "0"</automated>
  </verify>
  <done>PythonLoaderError deleted, module exports removed from lib.rs</done>
</task>

<task type="auto">
  <name>Task 2: Replace all loader-specific error variants with InitFailed</name>
  <files>crates/polyplug_python/src/lib.rs</files>
  <read_first>
    - crates/polyplug_python/src/lib.rs (current PythonLoader with removed error variants)
    - crates/polyplug/src/error.rs (LoaderError::InitFailed definition)
  </read_first>
  <action>
    Per D-01: Replace all loader-specific error variants with `LoaderError::InitFailed` directly.
    
    The PythonLoader currently uses variants that were REMOVED from core in Phase 1:
    - `LoaderError::PythonModuleImportFailed` (no longer exists)
    - `LoaderError::PythonInitRaisedException` (no longer exists)
    - `LoaderError::PythonInitFailed` (no longer exists)
    
    Replace each with `LoaderError::InitFailed { bundle, error }`:
    
    | Line | Current Error | Target |
    |------|---------------|--------|
    | 79-84 | PythonModuleImportFailed | InitFailed with format!("failed to import Python module at {}: {}", path, reason) |
    | 89-94 | PythonModuleImportFailed | InitFailed with format!("failed to canonicalize path {}: {}", path, reason) |
    | 121-126 | PythonInitRaisedException | InitFailed with format!("Python sys import failed: {}", e) |
    | 129-133 | PythonInitRaisedException | InitFailed with format!("Python sys.path get failed: {}", e) |
    | 136-141 | PythonInitRaisedException | InitFailed with format!("sys.path insert failed: {}", e) |
    | 144-152 | PythonInitRaisedException | InitFailed with format!("site-packages path insert failed: {}", e) |
    | 156-160 | PythonModuleImportFailed | InitFailed with format!("importlib.util import failed: {}", e) |
    | 166-169 | PythonInitFailed | InitFailed with format!("spec_from_file_location not found: {}", e) |
    | 171-175 | PythonModuleImportFailed | InitFailed with format!("spec_from_file_location call failed: {}", e) |
    | 178-184 | PythonInitFailed | InitFailed with format!("module_from_spec not found: {}", e) |
    | 186-190 | PythonModuleImportFailed | InitFailed with format!("module_from_spec call failed: {}", e) |
    | 198-203 | PythonModuleImportFailed | InitFailed with format!("exec_module failed: {}", e) |
    | 231-236 | PythonInitRaisedException | InitFailed with format!("polyplug_init call failed: {}", e) |
    
    Keep generic errors unchanged:
    - `LoaderError::ManifestMissingFile { bundle }` at line 73-75 - use directly
    - `LoaderError::InitSymbolMissing { bundle }` at line 207-211 - use directly
    
    Bundle field: Use `manifest.name.clone()` for consistency (or `bundle_name.clone()` where that's the derived name).
    
    IMPORTANT: The code is currently BROKEN because these variants don't exist in core. This fix makes it compile.
  </action>
  <acceptance_criteria>
    - `grep "PythonModuleImportFailed" crates/polyplug_python/src/lib.rs` returns empty
    - `grep "PythonInitRaisedException" crates/polyplug_python/src/lib.rs` returns empty
    - `grep "PythonInitFailed" crates/polyplug_python/src/lib.rs` returns empty
    - `grep "LoaderError::InitFailed" crates/polyplug_python/src/lib.rs` returns at least 10 matches
    - `cargo check -p polyplug_python` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo check -p polyplug_python 2>&1 | head -20</automated>
  </verify>
  <done>All Python-specific error variants replaced with InitFailed, compilation succeeds</done>
</task>

<task type="auto">
  <name>Task 3: Verify hot-reload uses RuntimeError::HotReloadDisabled</name>
  <files>crates/polyplug_python/src/lib.rs</files>
  <read_first>
    - crates/polyplug_python/src/lib.rs (reload method)
  </read_first>
  <action>
    Per D-03: Verify the reload() method returns RuntimeError::HotReloadDisabled.
    
    The current implementation at lines 243-249 already uses:
    ```rust
    fn reload(...) -> Result<(), RuntimeError> {
        Err(RuntimeError::HotReloadDisabled)
    }
    ```
    
    This is CORRECT per D-03. Just verify it remains unchanged.
    
    If any change is needed (e.g., it was using a different error), update it to:
    ```rust
    Err(RuntimeError::HotReloadDisabled)
    ```
  </action>
  <acceptance_criteria>
    - `grep "RuntimeError::HotReloadDisabled" crates/polyplug_python/src/lib.rs` returns exactly 1 match
    - `cargo check -p polyplug_python` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c "RuntimeError::HotReloadDisabled" crates/polyplug_python/src/lib.rs</automated>
  </verify>
  <done>reload() verified using RuntimeError::HotReloadDisabled</done>
</task>

</tasks>

<verification>
Verify compilation succeeds and PythonLoader uses only core error types:
- `cargo check -p polyplug_python` passes
- No references to PythonLoaderError or Python-specific variants
- All error sites use LoaderError::InitFailed or core generic variants
- Hot-reload returns RuntimeError::HotReloadDisabled
</verification>

<success_criteria>
- PythonLoader compiles without PythonLoaderError
- All Python-specific error variants replaced with InitFailed
- Hot-reload disabled returns RuntimeError::HotReloadDisabled
- error.rs file deleted, module exports removed from lib.rs
</success_criteria>

<output>
After completion, create `.planning/phases/02-update-loader-implementations/02-02-SUMMARY.md`
</output>