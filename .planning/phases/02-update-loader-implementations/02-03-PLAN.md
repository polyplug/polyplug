---
phase: 02-update-loader-implementations
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polyplug_lua/src/loader.rs
  - crates/polyplug_lua/src/error.rs
  - crates/polyplug_lua/src/lib.rs
autonomous: true
requirements: [ERR-06]
must_haves:
  truths:
    - "LuaLoader uses LoaderError::InitFailed directly at all error sites"
    - "LuaLoaderError type no longer exists"
    - "Hot-reload returns RuntimeError::HotReloadDisabled"
  artifacts:
    - path: crates/polyplug_lua/src/loader.rs
      provides: LuaLoader implementation
      contains: "LoaderError::InitFailed"
      not_contains: "LuaLoaderError"
      not_contains: "LuaScriptLoadFailed"
      not_contains: "LuaVmInitFailed"
      not_contains: "LuaInitFunctionMissing"
      not_contains: "LuaInitRaisedError"
    - path: crates/polyplug_lua/src/error.rs
      exists: false
    - path: crates/polyplug_lua/src/lib.rs
      not_contains: "pub mod error"
      not_contains: "LuaLoaderError"
  key_links:
    - from: crates/polyplug_lua/src/loader.rs
      to: crates/polyplug/src/error.rs
      via: "use polyplug::error::{LoaderError, RuntimeError}"
---

<objective>
Update LuaLoader to use LoaderError::InitFailed directly at all error sites and remove the LuaLoaderError type.

Purpose: Per D-01, use unified InitFailed pattern for consistency with other loaders.
Output: LuaLoader without local error type, using core InitFailed directly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-update-loader-implementations/02-CONTEXT.md
@.planning/phases/02-update-loader-implementations/02-RESEARCH.md
</context>

<interfaces>
<!-- Key types and contracts from codebase -->

From crates/polyplug/src/error.rs:
```rust
pub enum RuntimeError {
    #[error(transparent)]
    Loader(#[from] LoaderError),
    #[error("hot-reload is disabled in runtime config")]
    HotReloadDisabled,
}

pub enum LoaderError {
    #[error("init failed for bundle `{bundle}`: {error}")]
    InitFailed { bundle: String, error: String },
    
    #[error("manifest missing file field for bundle `{bundle}`")]
    ManifestMissingFile { bundle: String },
    
    #[error("init symbol missing in assembly `{bundle}`: expected `[UnmanagedCallersOnly] polyplug_init`")]
    InitSymbolMissing { bundle: String },
}
```

Target pattern (per D-01):
```rust
Err(RuntimeError::Loader(LoaderError::InitFailed {
    bundle: manifest.name.clone(),
    error: format!("descriptive message with context: {}", details),
}))
```

Hot-reload pattern (per D-03):
```rust
fn reload(...) -> Result<(), RuntimeError> {
    Err(RuntimeError::HotReloadDisabled)
}
```

Generic errors (use directly from core):
- `LoaderError::ManifestMissingFile { bundle }`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete LuaLoaderError and remove module exports</name>
  <files>crates/polyplug_lua/src/error.rs, crates/polyplug_lua/src/lib.rs</files>
  <read_first>
    - crates/polyplug_lua/src/error.rs (file to delete)
    - crates/polyplug_lua/src/lib.rs (module exports to modify)
    - crates/polyplug_lua/src/loader.rs (uses loader-specific variants - must update)
  </read_first>
  <action>
    Per D-04: Remove unused LuaLoaderError.
    
    1. Delete file `crates/polyplug_lua/src/error.rs` entirely
    
    2. Modify `crates/polyplug_lua/src/lib.rs`:
       - Remove line: `pub mod error;`
       - Remove line: `pub use error::LuaLoaderError;`
       - Keep: `pub mod bridge;`, `pub mod config;`, `pub mod ffi;`, `pub mod loader;`
       - Keep: `pub use bridge::LuaHostBridge;`, `pub use config::LuaConfig;`, `pub use loader::LuaLoader;`
    
    Note: LuaLoader implementation is in loader.rs, separate file.
  </action>
  <acceptance_criteria>
    - `test -f crates/polyplug_lua/src/error.rs` returns non-zero (file deleted)
    - `grep "pub mod error" crates/polyplug_lua/src/lib.rs` returns empty
    - `grep "LuaLoaderError" crates/polyplug_lua/src/lib.rs` returns empty
  </acceptance_criteria>
  <verify>
    <automated>grep -c "LuaLoaderError" crates/polyplug_lua/src/lib.rs crates/polyplug_lua/src/loader.rs 2>/dev/null | grep ":0$" || echo "FAIL: LuaLoaderError still referenced"</automated>
  </verify>
  <done>LuaLoaderError deleted, module exports removed from lib.rs</done>
</task>

<task type="auto">
  <name>Task 2: Replace all Lua-specific error variants with InitFailed</name>
  <files>crates/polyplug_lua/src/loader.rs</files>
  <read_first>
    - crates/polyplug_lua/src/loader.rs (current LuaLoader with removed error variants)
    - crates/polyplug/src/error.rs (LoaderError::InitFailed definition)
  </read_first>
  <action>
    Per D-01: Replace all loader-specific error variants with `LoaderError::InitFailed` directly.
    
    The LuaLoader currently uses variants that were REMOVED from core in Phase 1:
    - `LoaderError::LuaScriptLoadFailed` (no longer exists)
    - `LoaderError::LuaVmInitFailed` (no longer exists)
    - `LoaderError::LuaInitFunctionMissing` (no longer exists)
    - `LoaderError::LuaInitRaisedError` (no longer exists)
    
    Replace each with `LoaderError::InitFailed { bundle, error }`:
    
    | Line | Current Error | Target |
    |------|---------------|--------|
    | 127-131 | LuaScriptLoadFailed | InitFailed with format!("Lua script load failed at {}: {}", path, reason) |
    | 149-155 | LuaVmInitFailed | InitFailed with format!("Lua VM init failed: {}", reason) |
    | 163-166 | LuaVmInitFailed | InitFailed with format!("Lua VM init failed: {}", reason) |
    | 170-175 | LuaScriptLoadFailed | InitFailed with format!("Lua script load failed: {}", reason) |
    | 182-186 | LuaVmInitFailed | InitFailed with format!("Lua VM init failed: {}", reason) |
    | 193-196 | LuaVmInitFailed | InitFailed with format!("Lua VM init failed: {}", reason) |
    | 198-203 | LuaScriptLoadFailed | InitFailed with format!("Lua script load failed at {}: {}", path, reason) |
    | 215-219 | LuaInitFunctionMissing | InitFailed with format!("Lua plugin missing polyplug_init function: bundle={}", bundle) |
    | 248-254 | LuaInitRaisedError | InitFailed with format!("Lua polyplug_init raised error: {}", message) |
    | 260-264 | LuaInitFunctionMissing | InitFailed with format!("Lua plugin missing _polyplug_handlers: bundle={}", bundle) |
    | 281-285 | LuaInitRaisedError | InitFailed with format!("Lua handlers error: {}", message) |
    | 308-313 | LuaInitRaisedError | InitFailed with format!("Lua function slot error: {}", message) |
    | 381-384 | LuaInitRaisedError | InitFailed with format!("register_plugin error: code={}", code) |
    
    Keep generic errors unchanged:
    - `LoaderError::ManifestMissingFile { bundle }` at line 122-125 - use directly
    
    Bundle field: Use `manifest.name.clone()` or `bundle_name.clone()` where that's the derived name.
    
    IMPORTANT: The code is currently BROKEN because these variants don't exist in core. This fix makes it compile.
  </action>
  <acceptance_criteria>
    - `grep "LuaScriptLoadFailed" crates/polyplug_lua/src/loader.rs` returns empty
    - `grep "LuaVmInitFailed" crates/polyplug_lua/src/loader.rs` returns empty
    - `grep "LuaInitFunctionMissing" crates/polyplug_lua/src/loader.rs` returns empty
    - `grep "LuaInitRaisedError" crates/polyplug_lua/src/loader.rs` returns empty
    - `grep "LoaderError::InitFailed" crates/polyplug_lua/src/loader.rs` returns at least 10 matches
    - `cargo check -p polyplug_lua` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo check -p polyplug_lua 2>&1 | head -20</automated>
  </verify>
  <done>All Lua-specific error variants replaced with InitFailed, compilation succeeds</done>
</task>

<task type="auto">
  <name>Task 3: Verify hot-reload uses RuntimeError::HotReloadDisabled</name>
  <files>crates/polyplug_lua/src/loader.rs</files>
  <read_first>
    - crates/polyplug_lua/src/loader.rs (reload method)
  </read_first>
  <action>
    Per D-03: Verify the reload() method returns RuntimeError::HotReloadDisabled.
    
    The current implementation at lines 390-396 already uses:
    ```rust
    fn reload(...) -> Result<(), RuntimeError> {
        Err(RuntimeError::HotReloadDisabled)
    }
    ```
    
    This is CORRECT per D-03. Just verify it remains unchanged.
  </action>
  <acceptance_criteria>
    - `grep "RuntimeError::HotReloadDisabled" crates/polyplug_lua/src/loader.rs` returns exactly 1 match
    - `cargo check -p polyplug_lua` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c "RuntimeError::HotReloadDisabled" crates/polyplug_lua/src/loader.rs</automated>
  </verify>
  <done>reload() verified using RuntimeError::HotReloadDisabled</done>
</task>

</tasks>

<verification>
Verify compilation succeeds and LuaLoader uses only core error types:
- `cargo check -p polyplug_lua` passes
- No references to LuaLoaderError or Lua-specific variants
- All error sites use LoaderError::InitFailed or core generic variants
- Hot-reload returns RuntimeError::HotReloadDisabled
</verification>

<success_criteria>
- LuaLoader compiles without LuaLoaderError
- All Lua-specific error variants replaced with InitFailed
- Hot-reload disabled returns RuntimeError::HotReloadDisabled
- error.rs file deleted, module exports removed from lib.rs
</success_criteria>

<output>
After completion, create `.planning/phases/02-update-loader-implementations/02-03-SUMMARY.md`
</output>