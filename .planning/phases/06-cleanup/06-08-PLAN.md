---
phase: 06-cleanup
plan: 08
type: execute
wave: 3
depends_on: [06-05, 06-06, 06-07]
files_modified:
  - crates/polyplugc/tests/vtable_factories_tests.rs
  - crates/polyplugc/tests/smoke.rs
  - crates/polyplugc/tests/integration_codegen_rust.rs
  - crates/polyplugc/tests/generator_correctness.rs
  - crates/polyplug/tests/*.rs
autonomous: true
gap_closure: true
requirements: [CLN-01, CLN-04]
user_setup: []

must_haves:
  truths:
    - "Test file renamed from vtable_factories_tests.rs to interface_factories_tests.rs"
    - "Test imports use GuestContractInterface/RuntimeAbi (not GuestContractInterface/HostInterface)"
    - "All tests pass after import updates"
  artifacts:
    - path: "crates/polyplugc/tests/interface_factories_tests.rs"
      provides: "Renamed test file"
    - path: "crates/polyplug/tests/*.rs"
      provides: "Updated test imports"
  key_links:
    - from: "test files"
      to: "polyplug_abi types"
      via: "use statements"
---

# Plan 06-08: Rename Test File and Update Test Imports

**Objective:** Rename the test file and update all test imports to use correct type names (GuestContractInterface, RuntimeAbi) instead of removed aliases (GuestContractInterface, HostInterface).

## Tasks

### Task 1: Rename vtable_factories_tests.rs to interface_factories_tests.rs

<read_first>
- crates/polyplugc/tests/vtable_factories_tests.rs
</read_first>

<action>
Rename the test file and update its content:

1. Rename the file:
   ```bash
   git mv crates/polyplugc/tests/vtable_factories_tests.rs crates/polyplugc/tests/interface_factories_tests.rs
   ```

2. Update function names inside the file:
   - Test functions named `test_*_vtable_*` -> `test_*_interface_*`

3. Update imports if any:
   - `GuestContractInterface` -> `GuestContractInterface`
   - `HostInterface` -> `RuntimeAbi`

4. Update assertions checking generated output:
   - Check for `interface_factories` file names instead of `vtable_factories`
</action>

<acceptance_criteria>
- `ls crates/polyplugc/tests/vtable_factories_tests.rs` fails (file does not exist)
- `ls crates/polyplugc/tests/interface_factories_tests.rs` succeeds
- `cargo test -p polyplugc --test interface_factories_tests` passes
</acceptance_criteria>

---

### Task 2: Update polyplugc Test Imports

<read_first>
- crates/polyplugc/tests/smoke.rs
- crates/polyplugc/tests/integration_codegen_rust.rs
- crates/polyplugc/tests/generator_correctness.rs
</read_first>

<action>
Update imports in all polyplugc test files:

1. In smoke.rs:
   - Line 16: `use polyplug_abi::HostInterface;` -> `use polyplug_abi::RuntimeAbi;`
   - Line 20: `use polyplug_abi::GuestContractInterface;` -> `use polyplug_abi::GuestContractInterface;`
   - Update all type references throughout the file
   - Update CAPTURED_VTABLE to CAPTURED_INTERFACE
   - Update variable names: `vtable` -> `interface`

2. In integration_codegen_rust.rs:
   - Same import updates
   - Update type references in mock functions
   - Update assertions

3. In generator_correctness.rs:
   - Update imports
   - Update assertions checking generated content

4. Remove references to `vtable_factories_tests` module (if any).
</action>

<acceptance_criteria>
- `grep -c "use polyplug_abi::GuestContractInterface" crates/polyplugc/tests/*.rs` returns 0
- `grep -c "use polyplug_abi::HostInterface" crates/polyplugc/tests/*.rs` returns 0
- `grep -c "GuestContractInterface" crates/polyplugc/tests/*.rs` returns >= 10
- `cargo test -p polyplugc` passes
</acceptance_criteria>

---

### Task 3: Update polyplug Core Test Imports

<read_first>
- crates/polyplug/tests/integration_dispatch.rs
- crates/polyplug/tests/integration_graph.rs
- crates/polyplug/tests/stress_memory.rs
- crates/polyplug/tests/stress_error.rs
- crates/polyplug/tests/integration_ffi_robustness.rs
- crates/polyplug/tests/integration_cross_plugin.rs
</read_first>

<action>
Update imports in all polyplug test files:

1. For each test file, update imports:
   - `use polyplug_abi::GuestContractInterface;` -> `use polyplug_abi::GuestContractInterface;`
   - `use polyplug_abi::HostInterface;` -> `use polyplug_abi::RuntimeAbi;`
   - `use polyplug_abi::PluginDispatch;` -> `use polyplug_abi::DispatchMechanisms;`

2. Update type references in mock functions and fixtures:
   - `GuestContractInterface` -> `GuestContractInterface`
   - `HostInterface` -> `RuntimeAbi`

3. Update variable names:
   - `vtable` -> `interface`
   - `host_vtable` -> `runtime_abi`
   - `CAPTURED_VTABLE` -> `CAPTURED_INTERFACE`

4. Update struct literals for mock types.

5. Update comments referencing vtable terminology.
</action>

<acceptance_criteria>
- `grep -c "use polyplug_abi::GuestContractInterface" crates/polyplug/tests/*.rs` returns 0
- `grep -c "use polyplug_abi::HostInterface" crates/polyplug/tests/*.rs` returns 0
- `grep -c "GuestContractInterface" crates/polyplug/tests/*.rs` returns >= 20
- `cargo test -p polyplug` passes
</acceptance_criteria>

---

### Task 4: Update Remaining Test Files in polyplug

<read_first>
- crates/polyplug/tests/integration_load.rs
- crates/polyplug/tests/hot_reload_safety.rs
- crates/polyplug/tests/stress_hot_reload.rs
- crates/polyplug/tests/integration_host_contracts.rs
- crates/polyplug/tests/integration_context.rs
- crates/polyplug/tests/integration_panic.rs
- crates/polyplug/tests/stress_concurrent_registry.rs
- crates/polyplug/tests/registry_edge_cases.rs
- crates/polyplug/tests/library_lifetime.rs
- crates/polyplug/tests/integration_ffi_null.rs
- crates/polyplug/tests/ffi_edge_cases.rs
</read_first>

<action>
Update imports in remaining polyplug test files:

1. For each test file:
   - Check for `GuestContractInterface` or `HostInterface` imports
   - Update to new type names
   - Update type references in test code

2. Run tests to verify:
   ```bash
   cargo test -p polyplug
   ```

3. Fix any remaining compilation errors.
</action>

<acceptance_criteria>
- `grep -rn "GuestContractInterface\|HostInterface" crates/polyplug/tests/*.rs` returns 0 (excluding comments about rename history)
- `cargo test -p polyplug` passes
</acceptance_criteria>

---

### Task 5: Update Loader Crate Tests

<read_first>
- crates/polyplug_native/tests/*.rs
- crates/polyplug_python/tests/*.rs
- crates/polyplug_lua/tests/*.rs
- crates/polyplug_js/tests/*.rs
- crates/polyplug_dotnet/tests/*.rs
</read_first>

<action>
Update imports in loader crate test files:

1. For each loader crate, check and update test imports:
   - `GuestContractInterface` -> `GuestContractInterface`
   - `HostInterface` -> `RuntimeAbi`

2. Run tests for each loader:
   ```bash
   cargo test -p polyplug_native -p polyplug_python -p polyplug_lua -p polyplug_js -p polyplug_dotnet
   ```
</action>

<acceptance_criteria>
- `grep -rn "GuestContractInterface\|HostInterface" crates/polyplug_native/tests/*.rs crates/polyplug_python/tests/*.rs` returns 0
- Loader tests pass
</acceptance_criteria>

---

### Task 6: Run Full Workspace Test Suite

<read_first>
- Cargo.toml (workspace definition)
</read_first>

<action>
Run the full test suite to verify all changes:

1. Build workspace first:
   ```bash
   cargo build --workspace
   ```

2. Run all tests:
   ```bash
   cargo test --workspace
   ```

3. If tests fail:
   - Analyze failure messages
   - Fix remaining type mismatches
   - Re-run until all pass

4. Run clippy:
   ```bash
   cargo clippy --workspace
   ```
</action>

<acceptance_criteria>
- `cargo build --workspace` exits with code 0
- `cargo test --workspace` exits with code 0
- `cargo clippy --workspace` exits with code 0 (or only warnings)
</acceptance_criteria>

---

## Verification

```bash
# Verify test file renamed
ls crates/polyplugc/tests/vtable_factories_tests.rs
# Expected: file not found

ls crates/polyplugc/tests/interface_factories_tests.rs
# Expected: file exists

# Verify no old type imports in tests
grep -rn "use polyplug_abi::GuestContractInterface\|use polyplug_abi::HostInterface" crates/polyplugc/tests/ crates/polyplug/tests/

# Expected: 0 matches

# Full test suite
cargo test --workspace

# Expected: all tests pass
```

## must_haves

This plan delivers:
- Test file renamed to interface_factories_tests.rs
- All test imports use GuestContractInterface/RuntimeAbi
- All tests pass after import updates
- Full workspace test suite passes

---

<output>
After completion, create `.planning/phases/06-cleanup/06-08-SUMMARY.md`
</output>