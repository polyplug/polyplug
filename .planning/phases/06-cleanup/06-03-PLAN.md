---
phase: 06-cleanup
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - docs/ABI_ARCHITECTURE.md
  - docs/HOT_RELOAD_DESIGN.md
  - docs/HOST_CONTRACTS.md
  - docs/HOST_CONTRACTS_API.md
  - docs/PERFORMANCE.md
  - docs/PLUGIN_INTERFACE_DESIGN.md
  - docs/abi_types.md
  - docs/ARCHITECTURE_CLARIFICATIONS.md
autonomous: true
requirements: [CLN-03]
user_setup: []

must_haves:
  truths:
    - "Documentation uses Guest Contract / Host Contract terminology"
    - "No GuestContractInterface or HostInterface mentioned in docs (except rename history)"
    - "Diagrams and examples use new naming"
  artifacts:
    - path: "docs/ABI_ARCHITECTURE.md"
      provides: "Updated terminology"
      contains: "GuestContractInterface"
    - path: "docs/PERFORMANCE.md"
      provides: "Updated benchmark references"
      contains: "contract dispatch"
  key_links: []
---

# Plan 06-03: Update Documentation to Guest/Host Terminology

**Objective:** Update all documentation files to use Guest Contract / Host Contract terminology consistently, replacing old GuestContractInterface, HostInterface, and vtable references.

## Tasks

### Task 1: Update ABI_ARCHITECTURE.md

<read_first>
- docs/ABI_ARCHITECTURE.md
- crates/polyplug_abi/src/guest/guest_contract_interface.rs
- crates/polyplug_abi/src/host/runtime_abi.rs
</read_first>

<action>
1. Update terminology throughout the file:
   - "GuestContractInterface" -> "GuestContractInterface"
   - "HostInterface" -> "RuntimeAbi"
   - "vtable" -> "interface" (when referring to the contract interface)
   - "vtables" -> "interfaces" (plural)
   - "plugin vtable" -> "guest contract interface"
   - "host vtable" -> "runtime ABI"

2. Update code examples in the doc:
   - Change `PluginRegistrar` references to clarify it registers "guest contracts"
   - Update function parameter descriptions:
     - `registrar`: "Bridge for plugin to register its interfaces" (not "vtables")
   - Update `polyplug_init` description:
     - "Plugin constructor - registers contracts with the runtime" (not "registers vtables")

3. Update the execution flow diagram (lines 129-160):
   - Change "Plugin builds vtables" -> "Plugin builds interfaces"
   - Change "Vtables stored in registry" -> "Interfaces stored in registry"
   - Change "polyplug_runtime_resolve_plugin() -- Get vtable" -> "Get interface"
   - Change "Call plugin functions via vtable" -> "Call plugin functions via interface"

4. Update ABI Stability section (lines 162-176):
   - Change "Extension vtables via HostInterface.get_extension()" -> "Extension interfaces via RuntimeAbi.get_extension()"

5. Add a note at the top explaining terminology changes:
   ```markdown
   ## Terminology Note
   
   This document uses the following terminology (renamed in v1.1):
   - **GuestContractInterface**: Previously called "GuestContractInterface" or "vtable"
   - **RuntimeAbi**: Previously called "HostInterface"
   - **Host Contract**: A contract provided by the host to plugins
   - **Guest Contract**: A contract implemented by plugins
   ```
</action>

<acceptance_criteria>
- `grep -c "GuestContractInterface" docs/ABI_ARCHITECTURE.md` returns 0 (or only in terminology note)
- `grep -c "HostInterface" docs/ABI_ARCHITECTURE.md` returns 0 (or only in terminology note)
- `grep -c "GuestContractInterface" docs/ABI_ARCHITECTURE.md` returns >= 2
- `grep -c "RuntimeAbi" docs/ABI_ARCHITECTURE.md` returns >= 2
- File contains terminology note explaining the rename
</acceptance_criteria>

---

### Task 2: Update HOT_RELOAD_DESIGN.md

<read_first>
- docs/HOT_RELOAD_DESIGN.md
- crates/polyplug/src/reload.rs
</read_first>

<action>
1. Update terminology:
   - "VTableSlot" -> "interface slot" (or describe that VTableSlot was removed)
   - "vtable swap" -> "interface swap"
   - "swap vtables" -> "swap interfaces"

2. Update any code examples to use new naming:
   - If doc shows code snippets, update type names
   - Update callback descriptions

3. Add context about the change:
   - Note that VTableSlot wrapper was removed in the refactor
   - Clarify the hot-reload now swaps interfaces directly

4. Check for any references to "quiescence" or "Arc::strong_count" and update:
   - Phase 4 removed quiescence waiting
   - Update to describe callback-based model instead
</action>

<acceptance_criteria>
- `grep -c "VTableSlot" docs/HOT_RELOAD_DESIGN.md` returns 0 (or only in context note)
- `grep -c "vtable swap|vtable pointer" docs/HOT_RELOAD_DESIGN.md` returns 0
- `grep -c "interface swap|interface pointer" docs/HOT_RELOAD_DESIGN.md` returns >= 1
- Doc describes callback-based hot-reload model (not quiescence)
</acceptance_criteria>

---

### Task 3: Update HOST_CONTRACTS.md and HOST_CONTRACTS_API.md

<read_first>
- docs/HOST_CONTRACTS.md
- docs/HOST_CONTRACTS_API.md
- crates/polyplug_abi/src/host/host_contract_interface.rs
</read_first>

<action>
For both files:

1. Update terminology:
   - "host vtable" -> "host contract interface"
   - "HostInterface" -> "RuntimeAbi"
   - "host contract vtable" -> "host contract interface"

2. Update code examples and type names:
   - `HostInterface` type -> `RuntimeAbi`
   - `HostContractVTable` -> `HostContractInterface` (if used)

3. Clarify the naming distinction:
   - RuntimeAbi: The runtime's ABI provided to guests (host's functions)
   - HostContractInterface: A contract the host implements for guests to call

4. Update HOST_CONTRACTS_API.md specifically:
   - Update any FFI function descriptions
   - Update struct field descriptions
</action>

<acceptance_criteria>
- `grep -c "HostInterface" docs/HOST_CONTRACTS.md` returns 0 (or only in terminology note)
- `grep -c "HostInterface" docs/HOST_CONTRACTS_API.md` returns 0 (or only in terminology note)
- `grep -c "RuntimeAbi|host contract interface" docs/HOST_CONTRACTS.md` returns >= 1
</acceptance_criteria>

---

### Task 4: Update PERFORMANCE.md

<read_first>
- docs/PERFORMANCE.md
- crates/polyplug/benches/contract_dispatch.rs (after rename in 06-01)
</read_first>

<action>
1. Update benchmark references:
   - "vtable dispatch" -> "contract dispatch"
   - "vtable call overhead" -> "interface call overhead"
   - "VTable dispatch" -> "Contract interface dispatch"

2. Update benchmark names if referenced:
   - `vtable_dispatch` benchmark -> `contract_dispatch` benchmark

3. Update any performance tables/graphs:
   - Rename column headers if they use old terminology
   - Update descriptions

4. Update code examples:
   - Change type names in snippets
   - Update variable names in examples
</action>

<acceptance_criteria>
- `grep -c "vtable dispatch|VTable dispatch" docs/PERFORMANCE.md` returns 0
- `grep -c "contract dispatch|interface dispatch" docs/PERFORMANCE.md` returns >= 1
- Benchmark name reference uses `contract_dispatch` (not `vtable_dispatch`)
</acceptance_criteria>

---

### Task 5: Update PLUGIN_INTERFACE_DESIGN.md

<read_first>
- docs/PLUGIN_INTERFACE_DESIGN.md
- crates/polyplug_abi/src/guest/guest_contract_interface.rs
</read_first>

<action>
1. This file likely needs significant updates since it's about the "plugin interface":

2. Rename title and sections:
   - "Plugin Interface Design" -> "Guest Contract Interface Design"
   - Update all section headers

3. Update terminology throughout:
   - "GuestContractInterface" -> "GuestContractInterface"
   - "plugin interface" -> "guest contract interface"
   - "plugin vtable" -> "guest contract interface"

4. Update code examples:
   - Struct definitions should use `GuestContractInterface`
   - Function signatures should use new types

5. Add a note explaining the rename:
   - Why it was renamed (Host/Guest separation)
   - What the old name was
</action>

<acceptance_criteria>
- `grep -c "GuestContractInterface" docs/PLUGIN_INTERFACE_DESIGN.md` returns 0 (or only in rename note)
- `grep -c "GuestContractInterface" docs/PLUGIN_INTERFACE_DESIGN.md` returns >= 5
- File title/sections use "Guest Contract" terminology
</acceptance_criteria>

---

### Task 6: Update abi_types.md and ARCHITECTURE_CLARIFICATIONS.md

<read_first>
- docs/abi_types.md
- docs/ARCHITECTURE_CLARIFICATIONS.md
</read_first>

<action>
For both files:

1. Update type name references:
   - "GuestContractInterface" -> "GuestContractInterface"
   - "HostInterface" -> "RuntimeAbi"
   - "PluginDispatch" -> "DispatchMechanisms"

2. Update terminology in descriptions:
   - "vtable" -> "interface"
   - "plugin contract" -> "guest contract"

3. Update any diagrams or ASCII art:
   - Change labels to use new terminology
   - Update arrows/connections descriptions

4. For ARCHITECTURE_CLARIFICATIONS.md specifically:
   - This file may explain architecture decisions
   - Update decision rationales to use new terminology
   - Add context about the rename decisions from STATE.md
</action>

<acceptance_criteria>
- `grep -c "GuestContractInterface" docs/abi_types.md` returns 0 (or only in rename context)
- `grep -c "HostInterface" docs/abi_types.md` returns 0 (or only in rename context)
- `grep -c "GuestContractInterface" docs/ARCHITECTURE_CLARIFICATIONS.md` returns 0 (or only in rename context)
- `grep -c "GuestContractInterface|RuntimeAbi" docs/abi_types.md` returns >= 1
</acceptance_criteria>

---

## Verification

After all tasks complete:

```bash
# Verify documentation uses new terminology
grep -r "GuestContractInterface|HostInterface|PluginDispatch" docs/ --include="*.md"

# Expected: Only matches in explanatory notes about rename history

grep -r "GuestContractInterface|RuntimeAbi" docs/ --include="*.md"

# Expected: Multiple matches showing new terminology in use
```

## must_haves

This plan delivers:
- All documentation files updated to Guest/Host terminology
- Terminology notes explaining the rename
- No GuestContractInterface/HostInterface in current descriptions (only in history notes)
- Benchmark references updated

---

<output>
After completion, create `.planning/phases/06-cleanup/06-03-SUMMARY.md`
</output>