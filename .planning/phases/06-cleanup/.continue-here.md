---
context: phase
phase: 06-cleanup
task: 13
total_tasks: 13
status: in_progress
last_updated: 2026-04-05T08:45:02.063Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [x] CONSTRAINT: AtomicPtr for static storage — `OnceLock<*mut c_void>` fails because raw pointers don't implement Send/Sync — Use `AtomicPtr<c_void>` with `Ordering::SeqCst` instead
- [x] CONSTRAINT: AbiErrorCode typed errors — Error code fields changed from `u32` to `AbiErrorCode` — Remove `as u32` casts, use enum directly

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Raw pointer in OnceLock | `OnceLock<*mut c_void>` doesn't compile - raw pointers lack Send/Sync | blocking | Use `AtomicPtr<c_void>` with `Ordering::SeqCst` |

<current_state>
Executing gap closure plans from VERIFICATION.md. Plans 06-10, 06-11, 06-12 completed. Plan 06-13 partially complete with 3 build errors remaining in `pipeline_host`.

Build errors are in generated code:
- `host_callers.rs:64` - VmDispatch.call signature mismatch
- `host_callers.rs:111` - AbiErrorCode vs u32 in error construction
- `host_callers.rs:135` - AbiErrorCode vs u32 comparison
</current_state>

<completed_work>

Completed Plans:
- 06-10: Fix Rust Generator Host Interface Factory Issues (commit: `39dc8ad`)
- 06-11: Export VmDispatch from polyplug_abi and Regenerate (commit: `63084c8`)
- 06-12: Update Host Example to Use New Interface Names (commit: `c1236ee`)

Partial Plan:
- 06-13: Update Test Files to Use New Type Names (commit: `ad9b9a8`) - generator fixes done, 3 errors remain
</completed_work>

<remaining_work>

- Fix VmDispatch.call signature in generator (requires GuestContractInstance parameter)
- Fix AbiErrorCode/u32 mismatches in generated error handling
- Update test files across crates to use new type names (many files)
- Fix NativeLoader::new() to take 2 arguments (config + RuntimeAbi)
</remaining_work>

<decisions_made>

- Decided to use `AtomicPtr<c_void>` for static storage in generated factories because `OnceLock<*mut c_void>` doesn't compile (raw pointers lack Send/Sync)
- Changed `ContractError.code` and `HostContractError.code` from `u32` to `AbiErrorCode` for type safety
- Added `UpperHex`/`LowerHex` impl to `BundleId` for `{:016X}` formatting
- Exported `VmDispatch` and `HostContractId` from `polyplug_abi` crate root
</decisions_made>

<blockers>
- Build error: VmDispatch.call function signature changed - now requires GuestContractInstance parameter
</blockers>

## Required Reading (in order)
1. `.planning/phases/06-cleanup/06-VERIFICATION.md` — Gap analysis showing what needs fixing
2. `.planning/phases/06-cleanup/06-13-PLAN.md` — Current plan being executed
3. `crates/polyplugc/src/generators/rust.rs` — Generator code needing fixes

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: Using `OnceLock<*mut c_void>` for static storage → Use `AtomicPtr<c_void>` instead

## Infrastructure State
- No running services
- Build has 3 errors in `pipeline_host` package

<context>
Executing gap closure plans. The generator has been partially fixed but VmDispatch.call signature changed in the ABI and the generated code doesn't match. Need to update the generator to pass `GuestContractInstance` to the dispatch call function.

The remaining AbiErrorCode/u32 mismatches are in generated error construction code.
</context>

<next_action>
1. Regenerate code: `cargo run -p polyplugc -- generate --api examples/api.toml --lang rust --out examples/hosts/rust/src/generated`
2. Check errors: `cargo build -p pipeline_host 2>&1 | grep -E "^error"`
3. Fix VmDispatch.call signature in `crates/polyplugc/src/generators/rust.rs` around line 1100-1200 where VM dispatch is generated
</next_action>