---
phase: 13-cpp-codegen-modernization
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified: [crates/polyplugc/tests/integration_codegen_cpp.rs]
autonomous: true
requirements: [CG-02, CG-03, CG-04, D-08, D-09]  # CG-02/03/04: guest contract instance wrapper (already implemented); D-08/D-09: test decisions
user_setup: []
must_haves:
  truths:
    - "Integration test verifies C++ codegen produces correct naming"
    - "Integration test checks generated files exist"
    - "Integration test checks HostContractInterface is used (not HostContractVTable)"
    - "Integration test checks _INTERFACE suffix is used (not _VTABLE)"
    - "Integration test verifies guest contract instance wrapper pattern exists"
    - "sdk_validator passes for C++ SDK"
  artifacts:
    - path: "crates/polyplugc/tests/integration_codegen_cpp.rs"
      provides: "C++ codegen integration tests"
      min_lines: 50
  key_links:
    - from: "integration_codegen_cpp.rs"
      to: "polyplugc::generate"
      via: "GenerateConfig with Lang::Cpp"
---

<objective>
Create integration test file for C++ codegen that verifies the naming modernization from Plan 01. The test should check that generated C++ code uses HostContractInterface, _INTERFACE suffix, and correct ABI structures. Additionally, verify that the guest contract instance wrapper pattern exists (create_instance/destroy_instance lifecycle, instance_ member, instance passed to dispatch).

Purpose: Automated verification that C++ codegen produces correct output matching polyplug_abi types.
Output: integration_codegen_cpp.rs test file with passing tests.

Note: INST-05/INST-06 (dispatch signature verification for host contract caller) depend on Plan 01's checkpoint decision. If deferred, host contract caller will not pass HostContractInstance yet. Guest contract dispatch (lines 1229-1250 in cpp.rs) already passes instance_ to all dispatch calls - CG-02/03/04 satisfied for guest contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cpp-codegen-modernization/13-CONTEXT.md
@.planning/phases/13-cpp-codegen-modernization/13-RESEARCH.md
@.planning/phases/13-cpp-codegen-modernization/13-VALIDATION.md
</context>

<interfaces>
<!-- Reference patterns from existing integration tests -->

From crates/polyplugc/tests/integration_codegen_python.rs:
- Pattern: `GenerateConfig { api_toml, lang: Lang::Python, side: Side::Host, out_dir }`
- Pattern: `generate(config).expect("polyplugc::generate failed")`
- Pattern: Write generated files to out_dir, then assert file existence and content

From crates/polyplugc/tests/integration_codegen_csharp.rs:
- Pattern: Same config pattern with Lang::CSharp and Side::Guest
- Pattern: Check for expected files like "guest/Contracts.cs", "guest/Vtables.cs" (will be Interfaces.cs after rename)

Expected C++ files (from cpp.rs structure):
- Guest side: guest/interfaces.hpp, guest/contracts.hpp, guest/host_contracts.hpp, guest/init.hpp
- Host side: host/types.hpp, host/callers.hpp, host/interface_factories.hpp

Guest contract instance wrapper (lines 1034-1165 in cpp.rs):
- Class stores `interface_` and `instance_` members
- Constructor calls `create_instance`
- Destructor calls `destroy_instance`
- Dispatch methods pass `instance_` to function calls
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create integration_codegen_cpp.rs test file</name>
  <files>crates/polyplugc/tests/integration_codegen_cpp.rs</files>
  <read_first>
    - crates/polyplugc/tests/integration_codegen_python.rs (full file for pattern)
    - crates/polyplugc/tests/integration_codegen_csharp.rs (full file for pattern)
    - crates/polyplugc/src/generators/cpp.rs (lines 30-80 for file generation structure)
    - crates/polyplugc/src/generators/cpp.rs (lines 1034-1165 for guest instance wrapper pattern)
  </read_first>
  <behavior>
    - Test file exists at crates/polyplugc/tests/integration_codegen_cpp.rs
    - Test file follows pattern from Python/C# integration tests
    - Test verifies generated files exist
    - Test verifies HostContractInterface is used (not HostContractVTable)
    - Test verifies _INTERFACE suffix is used (not _VTABLE)
    - Test verifies guest contract instance wrapper pattern exists (create_instance call, destroy_instance call, instance_ member, dispatch passes instance)
    - Test verifies factory functions use inline HostContractInterface fields (no HostContractVTableHeader)
  </behavior>
  <action>
    Create integration_codegen_cpp.rs following the pattern from integration_codegen_python.rs and integration_codegen_csharp.rs.

    The test file should include:

    1. Module doc comment: `//! Integration test: use polyplug_codegen library to generate C++ bindings.`
    2. Standard imports: `#![allow(clippy::expect_used)]`, `use polyplug_codegen::{GenerateConfig, Lang, Side}`, `use polyplugc::generate`, `use std::path::{Path, PathBuf}`
    3. Helper function `workspace_root()` - same pattern as other integration tests
    4. Helper function `generate_cpp_bindings(bundle_toml: &Path, out_dir: &Path)` with GenerateConfig for Lang::Cpp

    5. Test `test_generate_cpp_guest_files_exist`:
       - Generate guest-side bindings (Side::Guest)
       - Check for files: guest/interfaces.hpp, guest/contracts.hpp, guest/host_contracts.hpp, guest/init.hpp

    6. Test `test_generate_cpp_host_files_exist`:
       - Generate host-side bindings (Side::Host)
       - Check for files: host/types.hpp, host/callers.hpp, host/interface_factories.hpp

    7. Test `test_cpp_codegen_uses_interface_naming`:
       - Generate guest-side bindings
       - Read guest/interfaces.hpp
       - Assert: content contains `_INTERFACE` (not `_VTABLE`)
       - Assert: content contains `GuestContractInterface` (not `PluginVTable` or similar legacy naming)

    8. Test `test_cpp_codegen_host_contract_uses_interface`:
       - Generate guest-side bindings
       - Read guest/host_contracts.hpp
       - Assert: content contains `HostContractInterface` (not `HostContractVTable`)
       - Assert: content contains `interface_` member (not `vtable_`)

    9. Test `test_cpp_codegen_guest_instance_wrapper_exists`:
       - Generate guest-side bindings
       - Read guest/interfaces.hpp (contains the ContractWrapper class)
       - Assert: content contains `create_instance` method call in factory
       - Assert: content contains `destroy_instance` method call in destructor
       - Assert: content contains `instance_` member
       - Assert: content contains `fn_(instance_, args_ptr,` pattern (dispatch passes instance)

    10. Test `test_cpp_codegen_factory_uses_inline_fields`:
        - Generate host-side bindings
        - Read host/interface_factories.hpp
        - Assert: content contains `HostContractInterface` (not `HostContractVTable`)
        - Assert: content contains `create_instance` stub
        - Assert: content does NOT contain `HostContractVTableHeader`

    Use test fixtures from tests/fixtures/test_bundle.toml (for guest) and tests/fixtures/test_api.toml (for host).

    The file should be approximately 120-150 lines to include all verification tests.
  </action>
  <verify>
    <automated>cargo test -p polyplugc integration_codegen_cpp -- --test-threads=1</automated>
  </verify>
  <done>
    - File exists: crates/polyplugc/tests/integration_codegen_cpp.rs
    - File contains `test_generate_cpp_guest_files_exist` function
    - File contains `test_generate_cpp_host_files_exist` function
    - File contains `test_cpp_codegen_uses_interface_naming` function
    - File contains `test_cpp_codegen_guest_instance_wrapper_exists` function with assertions for:
      - create_instance call
      - destroy_instance call
      - instance_ member
      - dispatch passing instance
    - File contains assertion for `HostContractInterface`
    - File contains assertion for `_INTERFACE`
    - File contains assertion for absence of `HostContractVTableHeader`
    - cargo test passes for integration_codegen_cpp tests
  </done>
</task>

<task type="auto">
  <name>Task 2: Run sdk_validator and verify C++ SDK consistency</name>
  <files>None (verification only)</files>
  <read_first>
    - sdk_validator.yaml (C++ SDK rules)
    - .planning/phases/13-cpp-codegen-modernization/13-CONTEXT.md (D-10, D-11 decisions)
  </read_first>
  <behavior>
    - sdk_validator C++ rules pass
    - No errors or warnings for C++ SDK files
  </behavior>
  <action>
    Per D-10, run sdk_validator to ensure C++ SDK consistency after Plan 01 changes.

    Execute: `just validate-sdks` (or equivalent command that runs sdk_validator)

    Verify that C++ SDK rules pass without errors. If there are failures, document them and report back.

    The sdk_validator checks consistency patterns defined in sdk_validator.yaml. For C++, this typically checks:
    - Naming conventions (interface vs vtable)
    - Type usage patterns
    - File structure

    If the validation fails, this task should report the specific errors so they can be addressed.
  </action>
  <verify>
    <automated>just validate-sdks 2>&1 | grep -E "(cpp|C\+\+)" | head -20</automated>
  </verify>
  <done>
    - sdk_validator runs successfully
    - C++ SDK rules pass (no errors for cpp/C++ section)
    - Validation output shows C++ SDK is consistent
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure test creation and verification — no security-relevant changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | N/A | test infrastructure | accept | No security-relevant changes — test creation and validation only |

No security-relevant changes — test creation and SDK validation only.
</threat_model>

<verification>
After all tasks complete:
1. Run `cargo test -p polyplugc integration_codegen_cpp` - all tests must pass
2. Run `just validate-sdks` - C++ SDK rules must pass
3. Verify file exists: crates/polyplugc/tests/integration_codegen_cpp.rs has at least 100 lines
4. Verify test includes assertions for guest instance wrapper pattern (CG-02/03/04)
</verification>

<success_criteria>
- integration_codegen_cpp.rs test file exists with proper structure
- All integration tests pass
- Test verifies HostContractInterface naming
- Test verifies _INTERFACE suffix
- Test verifies guest contract instance wrapper (create_instance/destroy_instance, instance_ member, dispatch passes instance)
- Test verifies factory uses inline HostContractInterface fields (no HostContractVTableHeader)
- sdk_validator passes for C++ SDK
</success_criteria>

<output>
After completion, create `.planning/phases/13-cpp-codegen-modernization/13-02-SUMMARY.md`
</output>