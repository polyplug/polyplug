---
phase: 15-final-cleanup
plan: 05
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - sdks/csharp/host/ReloadPhase.cs
  - sdks/python/host/polyplug/runtime.py
  - sdks/python/guest/polyplug_guest/__init__.py
  - sdks/cpp/host/polyplug/error.hpp
  - sdks/cpp/guest/polyplug/guest.hpp
  - sdks/lua/host/polyplug/reload_phase.lua
  - sdks/lua/guest/polyplug_guest.lua
  - sdks/js/host/polyplug/reload_phase.js
  - sdks/js/host/polyplug/mod.js
  - sdks/js/guest/polyplug_guest.js
  - sdks/rust/guest/src/lib.rs
autonomous: true
requirements: [CLN-01]
user_setup: []
must_haves:
  truths:
    - "SDK source uses interface terminology"
    - "Comments and error messages updated"
    - "SDK helper functions may preserve vtable naming if FFI-related"
    - "All SDKs remain functional"
  artifacts:
    - path: "sdks/*/host/**/*.rs"
      provides: "Host SDK libraries"
    - path: "sdks/*/guest/**/*.rs"
      provides: "Guest SDK libraries"
  key_links:
    - from: "SDK source"
      to: "polyplug_abi"
      via: "FFI imports"
      pattern: "store_host_vtable, get_host_vtable"
---

<objective>
Update all SDK source files to use interface terminology. Focus on comments, error messages, and documentation. Preserve FFI function names like `store_host_vtable` as they may be ABI imports.

Purpose: Clean up SDK terminology to match project conventions while preserving FFI compatibility.
Output: 21 SDK files updated with interface terminology in comments and messages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-final-cleanup/15-RESEARCH.md

<interfaces>
<!-- From RESEARCH.md - SDK patterns -->

FFI function names to PRESERVE (these are ABI imports):
- `store_host_vtable` - FFI function from polyplug ABI
- `get_host_vtable` - FFI function from polyplug ABI
- `HostVTable` type in Python ctypes - may need to stay if it's ABI struct name

Patterns to REPLACE:
- Comments: "Before vtable swap" → "Before interface swap"
- Comments: "After vtable swap" → "After interface swap"
- Error messages: "vtable is null" → "interface is null"
- Comments: "Host vtable pointer" → "Host interface pointer"
- Comments: "vtable dispatch" → "interface dispatch"

SDK files needing updates:
- C#: ReloadPhase.cs (comments)
- Python: runtime.py (comments), __init__.py (preserve FFI names, update comments)
- C++: error.hpp (comment), guest.hpp (preserve FFI, update comments)
- Lua: reload_phase.lua (comments), polyplug_guest.lua (preserve FFI, update comments)
- JS: reload_phase.js (comments), mod.js (error message), polyplug_guest.js (comments)
- Rust: lib.rs (comments)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update C# and Python SDK files</name>
  <files>sdks/csharp/host/ReloadPhase.cs, sdks/python/host/polyplug/runtime.py, sdks/python/guest/polyplug_guest/__init__.py</files>
  <read_first>
    - sdks/csharp/host/ReloadPhase.cs (current state)
    - sdks/python/host/polyplug/runtime.py (current state)
    - sdks/python/guest/polyplug_guest/__init__.py (current state)
  </read_first>
  <behavior>
    - C# ReloadPhase.cs comments: "vtable swap" → "interface swap"
    - Python runtime.py comments updated, preserve FFI function names
    - Python __init__.py preserve FFI imports (store_host_vtable, get_host_vtable), update other comments
  </behavior>
  <action>
    1. Read ReloadPhase.cs:
       - Line 11: "Before vtable swap" → "Before interface swap"
       - Line 16: "After vtable swap" → "After interface swap"
    
    2. Read runtime.py:
       - Line 310: Comment about `vtable` parameter - check if this is ABI struct reference, preserve if needed
       - Lines 545-546: Comments "vtable swap" → "interface swap"
    
    3. Read __init__.py:
       - Lines 47-48: Preserve `store_host_vtable`, `get_host_vtable` exports (FFI functions)
       - Lines 54-66: Preserve `_host_vtable_ptr` internal variable (related to FFI)
       - Lines 57-67: Comments can be updated but preserve FFI function names
       - Line 66: Comment "Initialize the allocator with host vtable" → "Initialize the allocator with host interface"
  </action>
  <verify>
    <automated>grep -n "vtable" sdks/csharp/host/ReloadPhase.cs sdks/python/host/polyplug/runtime.py sdks/python/guest/polyplug_guest/__init__.py | grep -v "store_host_vtable" | grep -v "get_host_vtable" | grep -v "_host_vtable" | wc -l</automated>
    Expected: near-zero (comments should be updated, FFI names preserved)
  </verify>
  <done>C# and Python SDK files updated. FFI function names preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update C++ and Lua SDK files</name>
  <files>sdks/cpp/host/polyplug/error.hpp, sdks/cpp/guest/polyplug/guest.hpp, sdks/lua/host/polyplug/reload_phase.lua, sdks/lua/guest/polyplug_guest.lua</files>
  <read_first>
    - sdks/cpp/host/polyplug/error.hpp (current state)
    - sdks/cpp/guest/polyplug/guest.hpp (current state)
    - sdks/lua/host/polyplug/reload_phase.lua (current state)
    - sdks/lua/guest/polyplug_guest.lua (current state)
  </read_first>
  <behavior>
    - C++ error.hpp comment: "vtable dispatch" → "interface dispatch"
    - C++ guest.hpp preserve FFI functions, update comments
    - Lua reload_phase.lua comments updated
    - Lua polyplug_guest.lua preserve FFI functions, update comments
  </behavior>
  <action>
    1. Read error.hpp:
       - Line 80: "vtable dispatch" → "interface dispatch"
    
    2. Read guest.hpp:
       - Lines 134-146: These are FFI helper functions (`host_vtable_storage`, `store_host_vtable`, `get_host_vtable`)
         - PRESERVE function names (FFI ABI)
         - Update comments if they say "vtable" in explanatory text
    
    3. Read reload_phase.lua:
       - Lines 7-8: "vtable swap" → "interface swap"
       - Line 54: Check if `vtable_version` is ABI field - PRESERVE
    
    4. Read polyplug_guest.lua:
       - Lines 22-32: Preserve `_host_vtable_ptr` and function names (FFI)
       - Update non-FFI comments
  </action>
  <verify>
    <automated>grep -n "vtable" sdks/cpp/host/polyplug/error.hpp sdks/cpp/guest/polyplug/guest.hpp sdks/lua/host/polyplug/reload_phase.lua sdks/lua/guest/polyplug_guest.lua | grep -v "vtable_version" | grep -v "store_host_vtable" | grep -v "get_host_vtable" | grep -v "host_vtable_storage" | grep -v "_host_vtable" | wc -l</automated>
    Expected: near-zero
  </verify>
  <done>C++ and Lua SDK files updated. FFI function names preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Update JS and Rust SDK files</name>
  <files>sdks/js/host/polyplug/reload_phase.js, sdks/js/host/polyplug/mod.js, sdks/js/guest/polyplug_guest.js, sdks/rust/guest/src/lib.rs</files>
  <read_first>
    - sdks/js/host/polyplug/reload_phase.js (current state)
    - sdks/js/host/polyplug/mod.js (current state)
    - sdks/js/guest/polyplug_guest.js (current state)
    - sdks/rust/guest/src/lib.rs (current state)
  </read_first>
  <behavior>
    - JS reload_phase.js comments updated
    - JS mod.js error message updated
    - JS polyplug_guest.js comments updated
    - Rust lib.rs comments updated
  </behavior>
  <action>
    1. Read reload_phase.js:
       - Lines 18-20: "vtable swap" → "interface swap"
    
    2. Read mod.js:
       - Line 290: Error message "null runtime or vtable pointer" → "null runtime or interface pointer"
    
    3. Read polyplug_guest.js:
       - Line 95: Comment "functions in the vtable" → "functions in the interface"
       - Line 116: Comment "Host vtable pointer" → "Host interface pointer"
       - Line 191: Comment "Extension vtable pointer" → "Extension interface pointer"
    
    4. Read lib.rs:
       - Line 23: Comment "Declare static vtable" → "Declare static interface"
  </action>
  <verify>
    <automated>grep -n "vtable" sdks/js/host/polyplug/reload_phase.js sdks/js/host/polyplug/mod.js sdks/js/guest/polyplug_guest.js sdks/rust/guest/src/lib.rs | wc -l</automated>
    Expected: 0
  </verify>
  <done>JS and Rust SDK files updated with interface terminology.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK source → FFI imports | Must preserve ABI function names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-01 | Tampering | FFI function names | mitigate | Explicitly list preserved names |
| T-15-05-02 | Denial | SDK compilation | mitigate | Verify SDKs compile after changes |
</threat_model>

<verification>
Run grep audit on SDK files excluding preserved FFI names:
```bash
grep -rn "vtable" sdks/ --include="*.rs" --include="*.py" --include="*.lua" --include="*.js" --include="*.ts" --include="*.cs" --include="*.hpp" | grep -v "vtable_version" | grep -v "store_host_vtable" | grep -v "get_host_vtable" | grep -v "host_vtable_storage" | grep -v "_host_vtable" | wc -l
```
Expected: near-zero
</verification>

<success_criteria>
- SDK files updated with interface terminology in comments and messages
- FFI function names preserved: store_host_vtable, get_host_vtable, host_vtable_storage
- ABI field vtable_version preserved
- SDKs remain functional
</success_criteria>

<output>
After completion, create `.planning/phases/15-final-cleanup/15-05-SUMMARY.md`
</output>