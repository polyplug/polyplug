---
phase: 15-final-cleanup
plan: 06
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - tests/fixtures/test_plugin/src/lib.rs
  - tests/fixtures/memory_plugin/src/lib.rs
  - tests/fixtures/error_plugin/src/lib.rs
  - tests/fixtures/depender_plugin/src/lib.rs
  - tests/fixtures/reload_plugin_v1/src/lib.rs
  - tests/fixtures/reload_plugin_v2/src/lib.rs
  - tests/fixtures/test_plugin_python/test_plugin.py
  - tests/fixtures/test_plugin_js/bundle.js
  - tests/fixtures/deno_host_test.ts
autonomous: true
requirements: [CLN-01, CLN-04]
user_setup: []
must_haves:
  truths:
    - "Test fixture source uses interface terminology"
    - "Comments updated to describe interface concepts"
    - "Fixture plugins still work"
    - "Tests pass with updated fixtures"
  artifacts:
    - path: "tests/fixtures/*/src/lib.rs"
      provides: "Test fixture plugin implementations"
    - path: "tests/fixtures/*/test_plugin.py"
      provides: "Python test fixtures"
    - path: "tests/fixtures/*/bundle.js"
      provides: "JavaScript test fixtures"
  key_links:
    - from: "fixtures"
      to: "polyplug_abi"
      via: "plugin registration"
      pattern: "GuestContractInterface"
---

<objective>
Update test fixture files to use interface terminology. These are source files used for testing, not generated code. Update comments and variable names.

Purpose: Ensure test fixtures match project terminology conventions.
Output: 19 fixture files updated, tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-final-cleanup/15-RESEARCH.md

<interfaces>
<!-- From RESEARCH.md - fixture patterns -->

Key patterns in Rust fixtures:
- Line comments: "Wrapper for a function pointer stored in a static vtable array" → "...static interface array"
- Comments: "The function pointer array for the test.add vtable" → "...test.add interface"
- Comments: "Static VTable" → "Static Interface"

Key patterns in Python fixtures:
- Comments: "Plugin interface (vtable)" → "Plugin interface"
- Class HostVTable may need special handling (check if it's ABI reference)
- Comments about host_vtable parameter

Key patterns in JS fixtures:
- Variable `vtable` in bundle.js → `interface`
- Comments and property names
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Rust test fixture source files</name>
  <files>tests/fixtures/test_plugin/src/lib.rs, tests/fixtures/memory_plugin/src/lib.rs, tests/fixtures/error_plugin/src/lib.rs, tests/fixtures/depender_plugin/src/lib.rs, tests/fixtures/reload_plugin_v1/src/lib.rs, tests/fixtures/reload_plugin_v2/src/lib.rs</files>
  <read_first>
    - tests/fixtures/test_plugin/src/lib.rs (current state)
    - tests/fixtures/memory_plugin/src/lib.rs (current state)
    - tests/fixtures/error_plugin/src/lib.rs (current state)
  </read_first>
  <behavior>
    - Comments: "static vtable array" → "static interface array"
    - Comments: "test.add vtable" → "test.add interface"
    - Comments: "NOT registered in the vtable" → "NOT registered in the interface"
    - Tests still pass
  </behavior>
  <action>
    1. Read test_plugin/src/lib.rs:
       - Line 68: Comment "Wrapper for function pointer stored in static vtable array" → "...static interface array"
       - Line 80: Comment "function pointer array for test.add vtable" → "...test.add interface"
       - Line 181: Comment "NOT registered in the vtable" → "NOT registered in the interface"
    
    2. Read memory_plugin/src/lib.rs:
       - Line 219: Similar comment update
       - Line 231: Comment "memory.test vtable" → "memory.test interface"
    
    3. Read error_plugin/src/lib.rs:
       - Line 8: Comment about "host vtable" → "host interface"
       - Line 181, 193: Similar comment updates
    
    4. Read remaining fixture files (depender_plugin, reload_plugin_v1/v2):
       - Update similar comments
    
    5. Verify fixtures compile:
       ```bash
       cargo build --workspace
       ```
  </action>
  <verify>
    <automated>grep -rn "vtable" tests/fixtures/*/src/lib.rs | grep -v "vtable_version" | wc -l</automated>
    Expected: 0
  </verify>
  <done>Rust test fixture files updated with interface terminology.</done>
</task>

<task type="auto">
  <name>Task 2: Update Python and JS test fixtures</name>
  <files>tests/fixtures/test_plugin_python/test_plugin.py, tests/fixtures/test_plugin_js/bundle.js, tests/fixtures/deno_host_test.ts</files>
  <read_first>
    - tests/fixtures/test_plugin_python/test_plugin.py (current state)
    - tests/fixtures/test_plugin_js/bundle.js (current state)
    - tests/fixtures/deno_host_test.ts (current state)
  </read_first>
  <behavior>
    - Python test_plugin.py: Comments updated, preserve HostVTable if it's ABI type
    - JS bundle.js: Variable vtable → interface
    - deno_host_test.ts: Comments and test names updated
  </behavior>
  <action>
    1. Read test_plugin.py:
       - Line 130: Comment "Host vtable passed to polyplug_init" - check if HostVTable is ABI type name (preserve if so)
       - Line 154: Comment "Plugin interface (vtable)" → "Plugin interface"
       - Line 196-202: Comments about host_vtable parameter - preserve if it's FFI parameter name
    
    2. Read bundle.js:
       - Lines 46-59: Variable `vtable` → `interface` throughout the function
       - This is a local variable, not FFI name
    
    3. Read deno_host_test.ts:
       - Line 80: Test name "guard_vtable_nonnull" → "guard_interface_nonnull"
       - Lines 89-90: Variable `vt` and error message "vtable is null" → "interface is null"
    
    4. Verify changes don't break fixture functionality.
  </action>
  <verify>
    <automated>grep -n "vtable" tests/fixtures/test_plugin_python/test_plugin.py tests/fixtures/test_plugin_js/bundle.js tests/fixtures/deno_host_test.ts | grep -v "HostVTable" | grep -v "host_vtable" | wc -l</automated>
    Expected: 0
  </verify>
  <done>Python and JS test fixtures updated with interface terminology.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixtures → Test execution | Changes must not break fixture functionality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-01 | Tampering | Fixture source | mitigate | Verify fixtures compile and tests pass |
| T-15-06-02 | Tampering | ABI type names | accept | Preserve HostVTable if it's ABI type reference |
</threat_model>

<verification>
Run workspace tests to verify fixtures still work:
```bash
cargo test --workspace -q 2>&1 | head -50
```
</verification>

<success_criteria>
- Test fixture files updated with interface terminology
- Comments and variable names changed to interface
- ABI type names preserved where appropriate (HostVTable, host_vtable parameter)
- Tests pass with updated fixtures
</success_criteria>

<output>
After completion, create `.planning/phases/15-final-cleanup/15-06-SUMMARY.md`
</output>