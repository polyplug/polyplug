---
phase: 15-final-cleanup
plan: 07
type: execute
wave: 4
depends_on: [15-03, 15-04, 15-04b, 15-05, 15-06]
files_modified:
  - docs/ABI_ARCHITECTURE.md
  - docs/abi_types.md
  - docs/ARCHITECTURE_CLARIFICATIONS.md
  - docs/HOST_CONTRACTS_API.md
  - docs/HOST_CONTRACTS.md
  - docs/HOT_RELOAD_DESIGN.md
  - docs/PERFORMANCE.md
  - docs/PLUGIN_INTERFACE_DESIGN.md
autonomous: true
requirements: [CLN-01]
user_setup: []
must_haves:
  truths:
    - "Documentation uses interface terminology consistently"
    - "Conceptual C++ vtable pattern explanations may remain"
    - "No confusing mixing of vtable/interface in same doc"
    - "Docs remain accurate"
  artifacts:
    - path: "docs/*.md"
      provides: "Project documentation"
  key_links:
    - from: "docs"
      to: "codebase terminology"
      via: "reader understanding"
      pattern: "GuestContractInterface"
---

<objective>
Update documentation files to consistently use interface terminology. Some conceptual references to "C++ vtable pattern" may remain as established programming concepts, but type names should use interface terminology.

Purpose: Ensure documentation matches codebase terminology for clarity.
Output: 8 documentation files updated with consistent interface terminology.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-final-cleanup/15-RESEARCH.md

<interfaces>
<!-- From RESEARCH.md - doc patterns -->

Key insight from RESEARCH.md:
"Some documentation may legitimately use 'vtable' when explaining the C++ virtual table pattern (an established programming concept). The distinction is between 'vtable' as a C++ pattern and 'VTable' as our specific type naming."

Current docs already note previous names:
- docs/ABI_ARCHITECTURE.md: "Previously called 'vtable'"
- docs/abi_types.md: "Previously called 'vtable'"
- docs/*.md: Similar "Previously called" notes

Patterns to REPLACE (type naming):
- "GuestContractInterface (was 'vtable')" → clean up notes
- Remove redundant "Previously called vtable" notes if terminology is now established

Patterns to PRESERVE (conceptual):
- "C++ vtable pattern" - established programming concept
- References to how C++ implements virtual dispatch via vtables
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update documentation files to use interface terminology</name>
  <files>docs/ABI_ARCHITECTURE.md, docs/abi_types.md, docs/ARCHITECTURE_CLARIFICATIONS.md, docs/HOST_CONTRACTS_API.md, docs/HOST_CONTRACTS.md, docs/HOT_RELOAD_DESIGN.md, docs/PERFORMANCE.md, docs/PLUGIN_INTERFACE_DESIGN.md</files>
  <read_first>
    - docs/ABI_ARCHITECTURE.md (current state)
    - docs/abi_types.md (current state)
    - docs/PERFORMANCE.md (current state)
  </read_first>
  <behavior>
    - Remove redundant "Previously called 'vtable'" notes where terminology is now established
    - Keep conceptual "C++ vtable" references for clarity about the pattern
    - Ensure type names use interface terminology
    - Docs remain accurate and clear
  </behavior>
  <action>
    1. Read documentation files to see current state.
    
    2. For each doc file:
       - Identify "Previously called vtable" notes that can be removed
       - Identify type names that should use "Interface" not "VTable"
       - PRESERVE conceptual explanations like "C++ vtable pattern" (these explain the programming concept)
    
    3. Specific updates based on RESEARCH.md findings:
       - docs/ABI_ARCHITECTURE.md line 6: "Previously called GuestContractInterface or vtable" - consider simplifying
       - docs/abi_types.md line 8: "Interface: Previously called vtable" - clean up
       - docs/ARCHITECTURE_CLARIFICATIONS.md: Similar cleanup
       - docs/PERFORMANCE.md lines 8-9: "Contract dispatch: Previously called vtable dispatch" - clean up
       - docs/PLUGIN_INTERFACE_DESIGN.md line 53: Contains vtable.functions[0] diagram - PRESERVE as conceptual C++ pattern
    
    4. Verify docs remain coherent and accurate.
    
    Key rule: If "vtable" explains the C++ virtual dispatch mechanism (an established concept), preserve it. If "VTable" refers to our specific type names, update to "Interface".
  </action>
  <verify>
    <automated>grep -rn "VTable\|Vtable" docs/*.md | wc -l</automated>
    Expected: 0 (type names should use Interface)
    
    <automated>grep -rn "vtable" docs/*.md | wc -l</automated>
    Expected: >0 (conceptual C++ pattern references may remain)
  </verify>
  <done>Documentation files updated. Type names use Interface. Conceptual C++ vtable pattern references preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → Reader understanding | Must be clear about type names vs concepts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07-01 | Information | Doc accuracy | mitigate | Preserve conceptual C++ pattern explanations |
| T-15-07-02 | Information | Terminology confusion | mitigate | Clear distinction between type names and concepts |
</threat_model>

<verification>
Grep audit to verify type names use Interface while conceptual references may remain:
```bash
grep -rn "VTable" docs/*.md | wc -l  # Should be 0
grep -rn "vtable" docs/*.md | wc -l  # May be >0 for conceptual references
```
</verification>

<success_criteria>
- Documentation type names use Interface terminology
- "Previously called vtable" notes cleaned up where terminology is now established
- Conceptual "C++ vtable" pattern explanations preserved
- Docs remain accurate and coherent
</success_criteria>

<output>
After completion, create `.planning/phases/15-final-cleanup/15-07-SUMMARY.md`
</output>