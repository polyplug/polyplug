---
phase: 15-final-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/polyplugc/src/generators/cpp.rs
  - crates/polyplugc/src/generators/rust.rs
  - crates/polyplugc/src/generators/python.rs
  - crates/polyplugc/src/generators/lua.rs
  - crates/polyplugc/src/generators/csharp.rs
  - crates/polyplugc/src/generators/js_quickjs.rs
autonomous: true
requirements: [CLN-01]
user_setup: []
must_haves:
  truths:
    - "Generator files contain no 'vtable' terminology (except vtable_version ABI field)"
    - "Generators compile after updates"
    - "String templates use 'interface' naming"
  artifacts:
    - path: "crates/polyplugc/src/generators/cpp.rs"
      provides: "C++ code generation with interface terminology"
    - path: "crates/polyplugc/src/generators/rust.rs"
      provides: "Rust code generation with interface terminology"
    - path: "crates/polyplugc/src/generators/python.rs"
      provides: "Python code generation with interface terminology"
    - path: "crates/polyplugc/src/generators/lua.rs"
      provides: "Lua code generation with interface terminology"
    - path: "crates/polyplugc/src/generators/csharp.rs"
      provides: "C# code generation with interface terminology"
    - path: "crates/polyplugc/src/generators/js_quickjs.rs"
      provides: "JS code generation with interface terminology"
  key_links:
    - from: "generators/*.rs"
      to: "examples/*/generated/*"
      via: "polyplugc generate command"
      pattern: "polyplugc generate --bundle"
---

<objective>
Update all 6 generator files to use "interface" terminology instead of "vtable" in comments, variable names, and string templates. The ABI field `vtable_version` must remain unchanged as it is an FFI field name.

Purpose: Eliminate ~800 occurrences in generated code by fixing source generators, avoiding hand-editing 95 generated files.
Output: 6 generator files with interface terminology, compiling successfully.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-final-cleanup/15-RESEARCH.md
@.planning/phases/15-final-cleanup/15-VALIDATION.md

<interfaces>
<!-- Key patterns from RESEARCH.md - DO NOT rename these ABI fields -->

ABI fields that MUST remain unchanged:
- `vtable_version` - This is an FFI field name, not our terminology

String template replacements in generators:
- Comments: "vtable dispatch" → "interface dispatch"
- Comments: "Static VTable" → "Static Interface"
- Variable names: `vtable` → `interface` (except when referring to ABI struct fields)
- Function names in templates: `create{}Vtable` → `create{}Interface`
- Error messages: "function not available in vtable" → "function not available in interface"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update cpp.rs generator - comments and string templates</name>
  <files>crates/polyplugc/src/generators/cpp.rs</files>
  <read_first>
    - crates/polyplugc/src/generators/cpp.rs (current state before editing)
  </read_first>
  <behavior>
    - Comments at lines 4-5: "vtable dispatch" → "interface dispatch", "vtable statics" → "interface statics"
    - Comment at line 683: "Store host vtable" → "Store host interface"
    - Error messages at lines 1221, 1240: "function not available in vtable" → "function not available in interface"
  </behavior>
  <action>
    1. Read the current cpp.rs file to see exact line content.
    2. Update module doc comment (lines 4-5):
       - Change "vtable dispatch" to "interface dispatch"
       - Change "vtable statics" to "interface statics"
    3. Update comment at line 683:
       - Change "Store host vtable for later access" to "Store host interface for later access"
    4. Update error messages at lines 1221 and 1240:
       - Change "function not available in vtable" to "function not available in interface"
    5. DO NOT rename `vtable_version` - this is an ABI field name.
  </action>
  <verify>
    <automated>grep -n "vtable" crates/polyplugc/src/generators/cpp.rs | grep -v "vtable_version" | wc -l</automated>
    Expected: 0 (after excluding vtable_version)
  </verify>
  <done>cpp.rs contains no vtable terminology except vtable_version ABI field. Generator compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Update rust.rs generator - comments and string templates</name>
  <files>crates/polyplugc/src/generators/rust.rs</files>
  <read_first>
    - crates/polyplugc/src/generators/rust.rs (current state)
  </read_first>
  <behavior>
    - Comment at line 5: "vtable stubs" → "interface stubs"
    - Comment at line 608: "static vtable array" → "static interface array"
    - Comments at lines 651-652: "vtable code" → "interface code"
    - Comment at line 1092: "Register all plugin vtables" → "Register all plugin interfaces"
    - Comment at line 1161: "desc and vtable are 'static" → "desc and interface are 'static"
    - Error message at line 1437: "function not available in vtable" → "function not available in interface"
  </behavior>
  <action>
    1. Read rust.rs to see current content.
    2. Update all comments containing "vtable" to use "interface":
       - Line 5: "vtable stubs" → "interface stubs"
       - Line 608: "static vtable array" → "static interface array"
       - Lines 651-652: function comments about "vtable code"
       - Line 1092: "Register all plugin vtables" → "Register all plugin interfaces"
       - Line 1066: "store_host_vtable" call comment
       - Line 1161, 1197: SAFETY comments referencing vtable → interface
       - Line 1437: error message "function not available in vtable"
    3. Update doc comments for host contract factory functions (lines 1931, 1935, 1939, 2048, 2053):
       - "Create a host contract vtable" → "Create a host contract interface"
    4. DO NOT rename `vtable_version` ABI field.
    5. Keep `store_host_vtable` and `get_host_vtable` as these may be SDK function names - check context.
  </action>
  <verify>
    <automated>grep -n "vtable" crates/polyplugc/src/generators/rust.rs | grep -v "vtable_version" | grep -v "store_host_vtable" | grep -v "get_host_vtable" | wc -l</automated>
    Expected: 0 or near-zero
  </verify>
  <done>rust.rs uses interface terminology in comments and templates. SDK helper function names preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Update python.rs generator - comments and string templates</name>
  <files>crates/polyplugc/src/generators/python.rs</files>
  <read_first>
    - crates/polyplugc/src/generators/python.rs (current state)
  </read_first>
  <behavior>
    - Comments at lines 416, 480: preserve SDK function imports (store_host_vtable)
    - Error message at line 719: "function not available in vtable" → "function not available in interface"
    - Variable names in templates: vtable_ptr → interface_ptr (where appropriate)
    - Doc comments at lines 2174-2182: "vtable" → "interface"
  </behavior>
  <action>
    1. Read python.rs to see current content.
    2. Update error message at line 719: "function not available in vtable" → "function not available in interface"
    3. Update doc comments for factory functions (lines 2174-2182):
       - "Create a host contract vtable" → "Create a host contract interface"
    4. Keep `store_host_vtable` imports as these are SDK function names.
    5. DO NOT rename `vtable_version` ABI field.
    6. Update variable names in string templates where `vtable` refers to interface pointer.
  </action>
  <verify>
    <automated>grep -n "vtable" crates/polyplugc/src/generators/python.rs | grep -v "vtable_version" | grep -v "store_host_vtable" | grep -v "_vtable" | wc -l</automated>
    Expected: near-zero (some `_vtable` member names may be ABI-related)
  </verify>
  <done>python.rs uses interface terminology in comments and templates. SDK imports preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generator → Generated Code | Terminology changes affect downstream generated output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | String templates | mitigate | Verify generated code compiles after changes |
| T-15-01-02 | Tampering | ABI field names | accept | Explicitly exclude vtable_version from changes |
</threat_model>

<verification>
After completing all generator updates:
1. Run `cargo build -p polyplugc` to verify generators compile
2. Run `grep -ri "vtable" crates/polyplugc/src/generators/*.rs | grep -v "vtable_version" | wc -l` to count remaining occurrences
</verification>

<success_criteria>
- All 6 generator files updated with interface terminology
- Generators compile successfully
- No "vtable" terminology remains except:
  - `vtable_version` ABI field (preserved)
  - SDK helper function names like `store_host_vtable` (preserved if they're FFI function names)
</success_criteria>

<output>
After completion, create `.planning/phases/15-final-cleanup/15-01-SUMMARY.md`
</output>