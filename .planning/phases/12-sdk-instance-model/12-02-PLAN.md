---
phase: 12-sdk-instance-model
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [sdks/js/abi/polyplug_abi.ts]
autonomous: false
requirements: [SDK-05]
user_setup: []

must_haves:
  truths:
    - "JS SDK TypeScript types use GuestContractInterface naming (not PluginInterface)"
    - "JS SDK TypeScript types use HostInterface naming (not HostVTable)"
    - "JS SDK mod.ts exports all ABI types"
  artifacts:
    - path: "sdks/js/abi/polyplug_abi.ts"
      provides: "TypeScript ABI type definitions"
      contains: "interface GuestContractInterface"
      min_lines: 400
    - path: "sdks/js/mod.ts"
      provides: "Main SDK entry point"
      contains: "export * from"
  key_links:
    - from: "sdks/js/mod.ts"
      to: "sdks/js/abi/polyplug_abi.ts"
      via: "export * from"
      pattern: "export.*polyplug_abi"
---

<objective>
Update JS SDK TypeScript types to use current polyplug_abi naming conventions, satisfying SDK-05.

Purpose: Align TypeScript interface naming with Rust ABI types (GuestContractInterface, HostInterface).
Output: Updated polyplug_abi.ts with renamed interfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current TypeScript types that need renaming -->

From sdks/js/abi/polyplug_abi.ts:
- `PluginInterface` (lines 205-220) - should be `GuestContractInterface`
- `HostVTable` (lines 231-244) - should be `HostInterface`
- `PluginDispatch` (line 171) - should be `GuestContractDispatch` or keep as dispatch union
- `HostContext` (lines 105-110) - should match current Rust layout

Expected Rust ABI types (from polyplug_abi/src/lib.rs):
- GuestContractInterface: contract_id, contract_version, function_count, dispatch_type, create_instance, destroy_instance, dispatch
- HostInterface: register_contract, alloc, free, find_contract, resolve_contract, get_host_contract, call_method
- GuestContractInstance: opaque handle with data field
- HostContractInstance: opaque handle with data field
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename PluginInterface to GuestContractInterface</name>
  <files>sdks/js/abi/polyplug_abi.ts</files>
  <read_first>
    - sdks/js/abi/polyplug_abi.ts (current TypeScript definitions)
    - crates/polyplug_abi/src/guest/guest_contract_interface.rs (Rust source for interface struct)
  </read_first>
  <behavior>
    - Test 1: TypeScript file contains `interface GuestContractInterface` after update
    - Test 2: TypeScript file does NOT contain `interface PluginInterface` after update
    - Test 3: All references to PluginInterface are updated to GuestContractInterface
  </behavior>
  <action>
    1. Rename `interface PluginInterface` (line 205) to `interface GuestContractInterface`
    2. Update all references to `PluginInterface` in comments and type unions:
       - Line 167: "PluginInterface::dispatch_type" - "GuestContractInterface::dispatch_type"
       - Line 171: type union comment if needed
    3. Add `create_instance` and `destroy_instance` fields to match Rust GuestContractInterface:
       ```typescript
       create_instance: (host: bigint, args: bigint) => GuestContractInstance;
       destroy_instance: (host: bigint, instance: GuestContractInstance) => void;
       ```
    4. Add GuestContractInstance interface:
       ```typescript
       export interface GuestContractInstance {
           data: bigint;
           contract_id: bigint;
       }
       ```
    5. Verify ABI_EXPECTED_SIZES has entry for GuestContractInterface (update from PluginInterface)
  </action>
  <verify>
    <automated>grep -c "interface GuestContractInterface" sdks/js/abi/polyplug_abi.ts | grep "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - sdks/js/abi/polyplug_abi.ts contains `interface GuestContractInterface` (1 match)
    - sdks/js/abi/polyplug_abi.ts does NOT contain `interface PluginInterface` (0 matches)
    - sdks/js/abi/polyplug_abi.ts contains `GuestContractInstance` interface
    - sdks/js/abi/polyplug_abi.ts contains `create_instance` field in GuestContractInterface
    - sdks/js/abi/polyplug_abi.ts contains `destroy_instance` field in GuestContractInterface
  </acceptance_criteria>
  <done>TypeScript uses GuestContractInterface naming matching polyplug_abi.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rename HostVTable to HostInterface</name>
  <files>sdks/js/abi/polyplug_abi.ts</files>
  <read_first>
    - sdks/js/abi/polyplug_abi.ts (current TypeScript definitions)
    - crates/polyplug_abi/src/host/host_interface.rs (Rust source for HostInterface)
  </read_first>
  <behavior>
    - Test 1: TypeScript file contains `interface HostInterface` after update
    - Test 2: TypeScript file does NOT contain `interface HostVTable` after update
    - Test 3: HostInterface has correct function signatures including call_method
  </behavior>
  <action>
    1. Rename `interface HostVTable` (line 231) to `interface HostInterface`
    2. Update function signatures to match Rust HostInterface:
       - `register_plugin` - `register_contract` (signature: (rt_ctx: bigint, descriptor: bigint, interface: bigint) => AbiError)
       - Keep: `alloc`, `free`
       - Update: `find_by_contract` - `find_contract` (rt_ctx: bigint, contract_id: bigint, min_version: number) => PluginHandle
       - Remove: `find_by_bundle` (not in current ABI)
       - Add: `resolve_contract` (rt_ctx: bigint, handle: PluginHandle) => bigint
       - Update: `get_host_contract` signature
       - Add: `call_method` (rt_ctx: bigint, instance: bigint, fn_id: number, args: bigint, out: bigint) => AbiError
    3. Update ABI_EXPECTED_SIZES: rename HostVTable - HostInterface, update size if changed
    4. Remove obsolete functions (find_by_bundle) that are not in current Rust ABI
  </action>
  <verify>
    <automated>grep -c "interface HostInterface" sdks/js/abi/polyplug_abi.ts | grep "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - sdks/js/abi/polyplug_abi.ts contains `interface HostInterface` (1 match)
    - sdks/js/abi/polyplug_abi.ts does NOT contain `interface HostVTable` (0 matches)
    - sdks/js/abi/polyplug_abi.ts contains `register_contract` in HostInterface
    - sdks/js/abi/polyplug_abi.ts contains `find_contract` in HostInterface
    - sdks/js/abi/polyplug_abi.ts contains `resolve_contract` in HostInterface
    - sdks/js/abi/polyplug_abi.ts contains `call_method` in HostInterface
    - sdks/js/abi/polyplug_abi.ts does NOT contain `find_by_bundle` in HostInterface (removed)
  </acceptance_criteria>
  <done>TypeScript uses HostInterface naming matching polyplug_abi.</done>
</task>

<task type="auto">
  <name>Task 3: Update HostContext and add RuntimeInterface</name>
  <files>sdks/js/abi/polyplug_abi.ts</files>
  <read_first>
    - sdks/js/abi/polyplug_abi.ts (current TypeScript definitions)
    - crates/polyplug_abi/src/host/runtime_interface.rs (Rust source for RuntimeInterface)
  </read_first>
  <action>
    1. Update HostContext interface to match Rust:
       - `runtime` field is correct (RuntimeContext opaque handle)
       - `bundle_id` field is correct
    2. Add RuntimeInterface interface (for host-side implementation):
       ```typescript
       export interface RuntimeInterface {
           register_contract: (self: bigint, descriptor: bigint, interface: bigint) => AbiError;
           alloc: (self: bigint, size: number, align: number) => bigint;
           free: (self: bigint, ptr: bigint, size: number, align: number) => void;
           find_contract: (self: bigint, contract_id: bigint, min_version: number) => PluginHandle;
           resolve_contract: (self: bigint, handle: PluginHandle) => bigint;
           get_host_contract: (self: bigint, contract_id: bigint, min_version: number) => bigint;
           call_method: (self: bigint, instance: bigint, fn_id: number, args: bigint, out: bigint) => AbiError;
           list_bundles: (self: bigint, out: bigint, out_cap: number) => number;
           get_dependencies: (self: bigint, bundle_id: bigint, out: bigint, out_cap: number) => number;
       }
       ```
    3. Add HostContractInstance interface if not present
    4. Update ABI_EXPECTED_SIZES with RuntimeInterface entry
  </action>
  <verify>
    <automated>grep -c "interface RuntimeInterface" sdks/js/abi/polyplug_abi.ts | grep "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - sdks/js/abi/polyplug_abi.ts contains `interface RuntimeInterface`
    - sdks/js/abi/polyplug_abi.ts contains `HostContractInstance` interface
    - ABI_EXPECTED_SIZES object contains RuntimeInterface entry
  </acceptance_criteria>
  <done>TypeScript has complete ABI type coverage matching polyplug_abi.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: Verify TypeScript compilation</name>
  <files>N/A - verification only</files>
  <action>Manual verification of TypeScript compilation and naming correctness.</action>
  <what-built>Updated TypeScript ABI types (GuestContractInterface, HostInterface, RuntimeInterface, instance handles)</what-built>
  <how-to-verify>
    1. Run `deno check sdks/js/mod.ts` - should pass with no errors
    2. Check TypeScript types compile: `deno check sdks/js/abi/polyplug_abi.ts`
    3. Verify naming: grep should show GuestContractInterface and HostInterface, not PluginInterface/HostVTable
  </how-to-verify>
  <verify>
    <automated>deno check sdks/js/mod.ts</automated>
  </verify>
  <resume-signal>Type "approved" or describe TypeScript compilation issues</resume-signal>
  <done>User confirms TypeScript compilation passes and naming is correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TypeScript user - ABI types | Type definitions are compile-time, no runtime validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering | TypeScript type definitions | accept | Compile-time only, FFI boundary handles validation |
| T-12-02-02 | Information Disclosure | ABI sizes in comments | accept | Public ABI information, not sensitive |

Type naming updates are compile-time only, no security concerns.
</threat_model>

<verification>
- All TypeScript interfaces renamed to match polyplug_abi
- deno check passes without errors
- ABI_EXPECTED_SIZES updated with new type names
</verification>

<success_criteria>
- TypeScript uses GuestContractInterface (not PluginInterface)
- TypeScript uses HostInterface (not HostVTable)
- TypeScript has RuntimeInterface, GuestContractInstance, HostContractInstance
- All expected sizes match Rust ABI
- deno check passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-sdk-instance-model/12-02-SUMMARY.md`
</output>