---
phase: 16-milestone-gap-closure
plan: 04
type: execute
wave: 4
depends_on: [16-03]
files_modified: [docs/PLUGIN_INTERFACE_DESIGN.md]
autonomous: true
gap_closure: true
requirements: [CLN-03]

must_haves:
  truths:
    - "Documentation uses Guest/Host terminology consistently"
    - "Code examples in documentation use interface terminology"
  artifacts:
    - path: "docs/PLUGIN_INTERFACE_DESIGN.md"
      contains: "interface.functions[0]"
      not_contains: "vtable.functions[0]"
  key_links:
    - from: "docs/PLUGIN_INTERFACE_DESIGN.md"
      to: "GuestContractInterface naming"
      via: "documentation"
      pattern: "interface terminology in code examples"
---

<objective>
Update PLUGIN_INTERFACE_DESIGN.md code example to use interface terminology instead of vtable. This addresses CLN-03 requirement for consistent Guest/Host terminology in documentation.

Purpose: Complete documentation terminology cleanup - the OLD ARCHITECTURE diagram still uses "vtable.functions[0]"
Output: Documentation with consistent interface terminology in code examples
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/PLUGIN_INTERFACE_DESIGN.md

<interfaces>
<!-- Specific line needing fix -->
PLUGIN_INTERFACE_DESIGN.md line 53: `vtable.functions[0] = trampoline_0` → `interface.functions[0] = trampoline_0`

Note: "Previously called" notes are acceptable (historical context). Only the code example needs updating.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix vtable code example in PLUGIN_INTERFACE_DESIGN.md</name>
  <files>docs/PLUGIN_INTERFACE_DESIGN.md</files>
  <read_first>
    - docs/PLUGIN_INTERFACE_DESIGN.md (current state at line ~53)
  </read_first>
  <action>
    In docs/PLUGIN_INTERFACE_DESIGN.md, update the code example in the OLD ARCHITECTURE diagram:

    Line 53:
    Change: `│  vtable.functions[0] = trampoline_0  (static Rust function)                 │`
    To: `│  interface.functions[0] = trampoline_0  (static Rust function)                 │`

    This is the only code example using "vtable" that needs updating. The "Previously called" notes at lines 8-9 are acceptable historical context and should NOT be changed.
  </action>
  <verify>
    <automated>grep "interface.functions\[0\]" docs/PLUGIN_INTERFACE_DESIGN.md | wc -l | grep -q "^1$"</automated>
    <automated>grep "vtable.functions\[0\]" docs/PLUGIN_INTERFACE_DESIGN.md | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>
    PLUGIN_INTERFACE_DESIGN.md code example uses interface terminology
    Historical "Previously called" notes preserved
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → Users | Documentation terminology affects user understanding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-01 | N/A | Documentation only | accept | No security impact - terminology consistency |
</threat_model>

<verification>
grep confirms no "vtable.functions" in documentation code examples
</verification>

<success_criteria>
- PLUGIN_INTERFACE_DESIGN.md line 53 uses "interface.functions[0]"
- grep "vtable.functions[0]" returns 0 matches
- Historical "Previously called" notes preserved
</success_criteria>

<output>
After completion, create `.planning/phases/16-milestone-gap-closure/16-04-SUMMARY.md`
</output>