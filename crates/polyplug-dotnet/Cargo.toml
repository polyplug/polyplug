[package]
name             = "polyplug-dotnet"
version          = "0.1.0"
edition.workspace     = true
license.workspace     = true
rust-version.workspace = true

[lib]
name       = "polyplug_dotnet"
path       = "src/lib/mod.rs"
crate-type = ["rlib"]

[dependencies]
polyplug = { path = "../polyplug" }

[lints]
workspace = true
