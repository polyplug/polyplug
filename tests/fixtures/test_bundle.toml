[bundle]
name = "test_bundle"
version = "1.0.0"
api = "test_api.toml"
runtime = "native"
file = "libtest.so"

[[plugin]]
name = "test_adder"
version = "1.0.0"
implements = ["test.add@1"]
requires = []
optional = []
