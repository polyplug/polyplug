[package]
name = "integration"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[dev-dependencies]
polyplug_abi = { workspace = true }
polyplug_common = { workspace = true }
polyplug = { workspace = true }
polyplug_native = { workspace = true }
polyplug_dotnet = { workspace = true }
polyplug_python = { workspace = true }
polyplug_lua = { workspace = true }
polyplug_js = { workspace = true }
polyplug_utils = { workspace = true }
# polyplugc is a bin-only CLI (no lib); tests drive the built binary via the
# `cli_support` helper instead of depending on it as a library.
polyplug_codegen = { workspace = true }
tempfile = { workspace = true }
libloading = { workspace = true }
toml = { workspace = true }

[lints]
workspace = true

